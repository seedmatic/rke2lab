---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cicd-render-pipeline-operators
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
  - name: runtime-seed-incluster-crds
  - name: runtime-seed-incluster-operators
  - name: runtime-seed-incluster-workloads
  force: true
  interval: 5m
  path: ./operators/cicd/render-pipeline
  prune: true
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: rke2lab
  wait: true
