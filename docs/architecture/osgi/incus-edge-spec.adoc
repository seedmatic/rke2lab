= incus-edge — the Incus-host contacts, put in their place
:toc: left
:toclevels: 3

[NOTE]
====
*Status (re-baselined):* the IMAGE-BUILD contact is IMPLEMENTED and re-shaped for the NixOS substrate;
PREPARE ships as `IncusProvisionScenario` (`IncusScenario.PROVISION`) grown by `IncusRunbookHandler` —
image-build + manifests graft (which synthesises the cloud-config content) + secrets (#10) +
`IncusHarvest` store + the `InstanceGrowPlan` publish. Two shifts from the original sweep:

* *The image build is nix, not distrobuilder.* It ships as TWO paired `@Component`s in `incus-edge` —
  `CultivatingNixosImageBuilder` (cultivate = run) and `SurveyingImageBuilder` (survey = preview) — that
  run `nix build …#nixosConfigurations.rke2-node-base` to produce `incus.tar.xz` + `rootfs.squashfs`; the
  recipe is owned by `BuildRecipe` (edge). `DistrobuilderImageBuilder` / distrobuilder are GONE.
  `ImageBuildException` lives in `incus-edge`.
* *The reachability contact is pulled back to VISION.* `IncusInstanceContact` /
  `ProcessBuilderIncusInstanceContact` do NOT exist in the code today; the post-grow VERIFY probe that
  would use them is designed-not-coded (see § the scenarios). `IncusExecRequest` remains as a
  contract record.

Remaining VISION: the RECONCILE/PROMOTE beat (only the unwired `IncusScenario.RECONCILE` enum value
exists — `IncusReconcileScenario`, `HostTreePromoter`, `HostSlotSelector`, `HostStagingEntry`,
`HostLiveEntry`, `HostDriftEntry`, `NocloudSeedWriter` are NOT built), and the post-grow VERIFY beat.
The pure-host GROW beat IS shipped, host-side, as `InstanceGrow` in `host/pulumi/incus-ingress` (§ the
GROW enablement). The incus domain of the frontier-playability model, alongside
link:pulumi-edge-handoff.adoc[pulumi-edge], link:ssh-to-age-edge-handoff.adoc[ssh-to-age-edge],
link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge], link:cluster-edge-spec.adoc[cluster-edge],
link:bbox-edge-spec.adoc[bbox-edge] and link:auth-edge-spec.adoc[auth-edge].

*One domain, one bundle pair.* An Incus instance needs an Incus image — reachability and image-build
are the same concern (talking to an Incus host), so they share `incus-contract` / `incus-edge` rather
than splitting into a second module pair. Of the two contacts this doc drafted, only the image-build
one has shipped; the reachability one is still vision.

The working unit each time: *one external contact, extracted atomically* — the port, the edge, AND
the re-wiring of the consumer, in one change. The criterion: a type migrates to a bundle only if a
bundle `@Component` contacts an external system (→ edge) or rides its verb (→ port). "Pulumi-free" is
not enough, and neither is "lives in the incus package".
====

== Contact 1 — instance reachability

=== The contact today — inlined in the host readiness gate

`SeedSystemdAdapterEndpointGate` (seed-master) waited on two axes: the dbus runtime status (already an
OSGi service, `SystemdRuntimeProbe`) and *instance reachability* — running `ssh … incus exec <node> --
true` to check a launched Incus instance answers. The second was still host I/O: a `ProcessBuilder`
inlined in the gate, marked `@Transitional(to="incus-edge")` precisely because it awaited this
extraction. The gate's retry loop, deadline, adaptive intervals, and the `ObservationView`/`SymptomKind`
projection are host orchestration that genuinely stays.

=== Resolution (VISION — not shipped)

NOTE: This resolution is DESIGNED, not coded. `IncusInstanceContact` /
`ProcessBuilderIncusInstanceContact` do not exist in the code today; the reachability probe was pulled
back to vision and now belongs to the post-grow VERIFY beat (§ the scenarios). Only the
`IncusExecRequest` contract record is present. The shape below is the target.

* *incus-contract* declares `IncusInstanceContact` — the verb `isReachable(IncusExecRequest) →
  Optional<String>` (empty = reachable, else the failure summary); and `IncusExecRequest`, the flat
  contract record (`sshHost`, `incusProject`, `nodeName`) the host projects out of its config at the call
  site.
* *incus-edge* provides a reachability `@Component` — shells `ssh … incus exec` and nothing else.
* the host keeps the orchestration and resolves the contact from the registry to drive its readiness
  gate. (In the shipped substrate the reachability probe is a VERIFY-beat scion step, not a host gate.)

== Contact 2 — seed image build

=== The contact today — inlined in the host image provider

`PulumiIncusImageProvider` (seed-master) ensures the seed image exists. Most of it is host
orchestration that stays: the `getImagePlain` reuse lookup, the `com.pulumi.incus.Image` resource it
schedules, the artifact-directory resolution across NFS mirror candidates, the `.image.checksum.sha256`
cache marker, and the NixOS path translation. (The distrobuilder-config materialisation is NO LONGER
host — the config is an edge bundle resource the edge materialises itself; see § the recipe is
edge-owned.)

Buried inside was one external contact — *building the artifacts*: probe `PATH` for `distrobuilder`,
run `distrobuilder build-incus` (under `sudo` if not root), or, failing a local binary, stream a
`remote-build-incus-image.sh` recipe over `ssh` to the builder host. All `ProcessBuilder`, plus the
recipe script as a classpath resource.

=== Resolution

* *incus-contract* declares `ImageBuilder` — `build(ImageBuildRequest)` (produce the artifacts, throwing
  an unchecked exception on failure) and `recipeDigest()` (a stable digest of the edge-owned recipe).
  `ImageBuildRequest` is the flat contract record: the builder binary, `incusProject`, and two path
  families — `workspaceDir`/`localArtifactDir` (drive a local build) and `remoteHost`/`remoteWorkspaceDir`/
  `remoteArtifactDir` (NixOS-translated by the host, drive the ssh recipe). The recipe is NOT a field —
  the edge owns it (see below), so only artifact/workspace placement crosses.
* *incus-edge* provides TWO paired `@Component`s implementing `ImageBuilder`: `CultivatingNixosImageBuilder`
  (cultivate = run — it builds) and `SurveyingImageBuilder` (survey = preview — it plans only and returns
  without touching anything), selected at the collaborator frontier by the gardening mode. The cultivating
  builder owns the local-vs-remote decision, runs `nix build …#nixosConfigurations.rke2-node-base` to
  produce `incus.tar.xz` + `rootfs.squashfs` locally when `nix` and `incus` both resolve on `PATH`, else
  streams the recipe over `ssh` to the builder host. The recipe is owned by `BuildRecipe` (a bundle
  resource — this edge is its single source). `ImageBuildException` (unchecked) lives HERE in `incus-edge`,
  not in the contract.
* *the host* keeps the Pulumi `Image` resource, the reuse lookup, the artifact cache, and the path
  translation — shipped as `InstanceGrow` in `host/pulumi/incus-ingress` (§ the GROW enablement), which
  composes `ImageBuilder` from the registry and calls `build` only when a rebuild is needed. (The former
  seed-master `PulumiIncusImageProvider` is gone.)

==== The recipe is edge-owned end to end (nix substrate)

The recipe is the NixOS build (`nix build …#nixosConfigurations.rke2-node-base`), owned entirely by
`BuildRecipe` as a bundle resource of `incus-edge` — the single source. There is no distrobuilder config
(`incus-distrobuilder.yaml`) and no distrobuilder invocation any more; the earlier two-channel
distrobuilder scheme (an NFS-pathed yaml + an ssh-streamed driver) is fully superseded by the substrate.
A local build runs nix in place; a remote build streams the recipe over the one `ssh` channel.

`recipeDigest()` is a stable digest of that edge-owned recipe, so a recipe change invalidates the host's
image cache. The host still holds only the digest, never the recipe — it folds `recipeDigest()` into its
`buildChecksum`. The scion composes `buildChecksum` from `recipeDigest` + the image amendment's fields;
the recipe bytes it never sees are already covered by the digest. One owner, one channel.

== The shared shape

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>Incus host: nix build / ssh builder ('ssh incus exec' = VISION)"]
  edge["incus domain — EDGE (incus-edge)<br/>@Component CultivatingNixosImageBuilder + SurveyingImageBuilder<br/>recipe: BuildRecipe · ImageBuildException"]
  iport["incus domain — PORT (incus-contract)<br/>ImageBuilder, ImageBuildRequest<br/>(IncusInstanceContact, IncusExecRequest = reachability, VISION)"]
  gate["control-plane — HOST<br/>InstanceGrow (host/pulumi/incus-ingress):<br/>Pulumi Image + cache + path xlat"]
  gate -->|composes the image-build contact| iport
  gate -->|folds recipeDigest into cache key| iport
  edge -.->|implements| iport
  edge -->|nix build · ssh recipe| world
  gate -->|schedules com.pulumi.incus.Image| gate
  classDef e fill:#eef,stroke:#88a,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class edge e;
  class iport p;
  class gate h;
  class world w;
....

== The incus SCION scenarios (target — the metier definition)

In the ClusterSeed vision (link:../bdd/bdd.adoc#clusterseed-scenario-map[the scenario map]) the incus
WHEN splits into THREE beats, ordered by the mount (settled 2026-07-15, see
link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] § the crux): PREPARE (scion) →
GROW (pure-host) → VERIFY (scion). *The incus domain owns TWO of the three — prepare and verify; the GROW
between them is pure-host `com.pulumi` and is NOT a scion* (a scion has no Pulumi engine around it; the
control flow cannot enter Felix — shape (A)). The host speaks gardening; these scenarios speak the incus
metier (image · host-tree · exec).

*Why the instance-creation is NOT here.* An earlier draft of this section had a single scenario whose
`WHEN … the instance is created from that image (IncusInstanceContact)` — that was WRONG on two counts:
`IncusInstanceContact.isReachable` only PROBES, it never creates; and the creation is the Pulumi graph
(`Project/Network/Profile/Image/Instance`), host-side, deleted with `IncusResourceBootstrap`
(`342b7c327`) and restored as the pure-host GROW beat, not a scion step. The scion prepares the material
and probes the result; the host grows the VM between the two.

The scenario skeleton below tracks the codebase (2026-07-16). Step names are the jGiven methods where
they exist; `[CODED]`/`[VISION]` mark what ships vs what is designed-not-yet-coded. It will eventually
be superseded by a rendered runbook (more faithful), but stays the readable target meanwhile.

[listing]
----
SCENARIO: the instance is prepared   (PREPARE beat — sow-and-graft, in-container)  [IncusScenario.PROVISION — CODED, grown by IncusRunbookHandler]
  GIVEN the image build request (recipe + digest), the run gate, the broker, the cellar   [CODED]
    AND the seed node + worktree/host tree (the staging-slot selection HostSlotSelector is VISION, not built)  [partial]
  WHEN the_image_is_built — built, or found current by its recipe-digest cache key       [CODED]
    AND the_manifests_are_cultivated — the manifests SUB-SOW (broker.sow AMEND+RUNBOOK → the SOIL, passing the Cellar that carries the tx),
        then GRAFTS the reaped manifests runbook under this step (ScenarioGraft.rebuild + graftUnder) — a scion grafting a sub-scion in-world,
        so the manifests synthesis renders as a nested sub-tree, faithful in the runbook (§ decision a: every scion played is rendered).
        The cloud-config content (nocloud user-data/meta-data/network-config) is synthesised by manifests here — there is NO separate
        nocloud-unwrap step in this scenario                                                              [CODED]
    AND the_secrets_are_written (#10 — resolve the auth token contact, upsert gh/flox into <worktree>/.secrets
        via LaunchSecretsWriter (incus-core); gated on cultivating)                                        [CODED]
    AND the_network_and_identity_are_resolved — resolve NetplanSynthesisService (@Component), assemble the
        GrowNetworkView (2 NIC hwaddrs + the dnsmasqConfig map) AND the GrowIdentityView (nodeName,
        <cluster>-<node> hostname, NodeType.kind(), nodeId) from the one blueprint, OSGi-side           [CODED]
  THEN the_instance_is_prepared — the prep verdict is folded                                             [CODED]
    AND the_prep_is_stored — IncusHarvest (recipe digest + soil) stored, the request the host fetches to GROW  [CODED]
    AND the_instance_grow_plan_is_published — GrowPlanAssembler (incus-core) assembles ONE immutable
        InstanceGrowPlan {GrowNetworkView, GrowImageView (imageAlias, artifact paths, buildChecksum computed
        OSGi-side), GrowIdentityView} stored at the plan coordinate — the single record the host GROW fetches
        (views + plan in incus-ingress-contract)                                                          [CODED]
    NOTE the split-image content fingerprint (sha256(incus.tar.xz ++ rootfs.squashfs), metadata first) is the
        one pure util SplitImageFingerprint (incus-ingress-contract) — shared by the host GROW (which
        references the image by it, no alias) and the incus scion (which folds it, with the nix-emitted
        rke2.version, into the IMAGE_STATE amendment so the workload LXCMachineTemplate pins the same image) [CODED]
    AND a build/graft failure surfaces, optionally consulting the doctor                  [CODED]
    AND the instance is NOT started here — GROW is the separate, later pure-host beat

SCENARIO: the live tree is reconciled   (RECONCILE beat — sow-and-graft, in-container)  [VISION — only IncusScenario.RECONCILE enum value exists, unwired]
  GIVEN the cellar's host-tree entries fetched (fold → pivot liveSyncedFrom + source staging), the run gate
  WHEN change = diff(pivot, source) on checksums is read
  THEN empty → NO-OP ; gate closed → PENDING ; else promote (jsync staging → host.live.d)
    AND the live + drift entries stored, a PROMOTED tag posted
  NOTE none of HostTreePromoter / HostSlotSelector / Host{Staging,Live,Drift}Entry / IncusReconcileScenario
       is built — only the IncusScenario.RECONCILE enum constant is present, unwired  [VISION]

  [ ── the pure-host GROW beat (com.pulumi) — NOT a scion, sits HERE, after reconcile/promote ── ]

SCENARIO: the instance grows   (GROW beat — PURE-HOST com.pulumi, NOT a scion)  [host InstanceGrow SHIPPED + wired via ClusterSeedScenario the_instance_grows → InstanceGrow.grow]
  GIVEN the InstanceGrowPlan fetched from the cellar (Cellar.fetch(INSTANCE_GROW_PLAN, InstanceGrowPlan.class)
        — one immutable record decoded host-side via the dual-realm SeedCodec; no metier, no sow)
    AND the Pulumi Context threaded from Main (SessionSeed<Context> + a receiver)
  WHEN the image resource is resolved (reuse-lookup getImagePlain(alias) — a provider invoke, host-pur;
        else new Image from the plan's artifact paths → fingerprint Output)
    AND the existing project/network are import-looked-up (IncusImportLookup — provider invokes
        getProject/getNetworkPlain, NOT ssh; adopt via importId, skip the unmanaged canonical LAN bridge)
    AND the Pulumi graph is declared (#1: Project root → Network/Profile dependsOn, Image fingerprint Output,
        Instance via Output refs, replaceOnChanges(config,config.*), deleteBeforeReplace)
    AND the 13 mounts are bound (#8: sources derived from the FIXED host.live.d via asAutomountView →
        target HostPathCatalog) onto the ALREADY-PROMOTED host.live.d, running=true
    AND the nocloud→replace wire is armed (#7: the plan's cloudInitChecksum + buildChecksum posed on
        user.rke2lab.provisioning.slice.* / imageBuildChecksum — the scion computed them, the host only poses)
  THEN the VM is grown, mounting the current host.live.d
  NOTE the host does ONLY com.pulumi + provider invokes — zero domain computation (all projected in the plan)

SCENARIO: the instance is verified   (VERIFY beat — sow-and-graft, in-container, AFTER grow)  [VISION, not coded]
  GIVEN the instance contact (edge) resolved from the registry — inert if the RunGate is closed
  WHEN the readiness exec lands — the agent answers (IncusInstanceContact.isReachable / IncusExecRequest)
  THEN the instance is verified reachable and its verdict is reaped
    AND an unreachable instance surfaces (a failed exec), optionally consulting the doctor
----

*Preview inertness:* under a closed `RunGate` the edges do not ssh/build/promote — each scion narrates the
plan (PENDING, E9) without touching the Incus host. *The recipe-digest subtlety* (see above) is metier
detail owned here, not host-visible. Each scion grafts back under its host step; on failure the transversal
doctor-consult follows the same pattern as the other scions.

*Migration note (re-baselined).* PREPARE's materialisations are CODED: `IncusProvisionScenario`
(grown by `IncusRunbookHandler`) ships image-build + manifests-sub-sow + secrets (#10) + `IncusHarvest`
store + the `InstanceGrowPlan` publish. The materialisation WHEN steps (they FABRICATE material —
§ bdd.adoc, the G/W/T phase rule) delegate to incus-core instances — the secrets write to
`LaunchSecretsWriter`; the cloud-config (nocloud) content is synthesised by the manifests sub-sow, so
there is NO standalone `NocloudSeedWriter` type and no separate nocloud-unwrap step. The PROMOTE beat is
NOT built: `IncusScenario.RECONCILE` is only an unwired enum constant, no `IncusReconcileScenario` exists.

*The GROW enablement:* PREPARE has `the_network_and_identity_are_resolved` (WHEN)
+ `the_instance_grow_plan_is_published` (THEN) — the scion resolves `NetplanSynthesisService`, computes
`buildChecksum` OSGi-side, and assembles ONE immutable `InstanceGrowPlan` (network view + image view +
identity view) it stores at `INSTANCE_GROW_PLAN`. The plan is a flat record in a NEW `type=dual-realm`
dual-realm module `incus-ingress-contract` (the host face of the incus contract — flat value records only,
no logic; the first `type=dual-realm` of a domain). The contract also carries `IngressConfig` — the flat
config the host fills to actualise (incus project/remote + node/profile/bridge names) — so the actualiser
reads ONE authoritative vocabulary and names no seed-master type. The GROW between reconcile and verify is
pure-host (`com.pulumi` + provider invokes only — it fetches the plan and actualises, computes nothing of
the domain), NOT a scion; the import-lookup it keeps is a provider invoke, not ssh. Its actualiser
(`InstanceGrow` + `IncusProviderContext` + `IncusImportLookup`) lives in the host module `incus-ingress`
(`host/pulumi/`, package `io.seedmatic.rke2lab.controlplane.incus`): the run fetches the plan and DELEGATES
the grow to it, filling an `IngressConfig` from its `BootstrapConfig` — no `com.pulumi.incus` in
seed-master itself. The probe pulls out to a VERIFY beat (post-grow). See link:host-cellar-realisation-spec.adoc#the-grow-anatomy[host-cellar §
the-grow-anatomy] (the scion-projects/host-actualises rule + the plan) and
link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] §deltas.

== Related documentation

* link:../bdd/bdd.adoc#clusterseed-scenario-map[the ClusterSeed scenario map] — where this scion is
  graft-called (the "instance is provisioned" host step).
* link:cluster-edge-spec.adoc[cluster-edge] — the edge this most closely mirrors (kubectl contact).
* link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge] — the other axis of the same readiness gate.
* link:auth-edge-spec.adoc[auth-edge] — the sibling extraction, same bare-`ProcessBuilder` shape.
* link:../patterns/port-edge-domain-ownership.adoc[port-edge-domain ownership] — domain CALLS / port DECLARES / edge PROVIDES.
