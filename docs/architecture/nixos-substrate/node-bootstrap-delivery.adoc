= Node-side bootstrap delivery — closing the substrate loop
:toc: left
:toclevels: 3

[NOTE]
====
This spec covers ONE seam of the NixOS node substrate: how the small *bootstrap set*
(the CNI + Flux) reaches a freshly-grown node's RKE2 auto-deploy directory now that
cloud-init and the `/srv/host` mount are gone. The broader substrate vision (homogeneous
image, `role is config not OS`, `chacun son rôle`) lives in link:substrate-model.adoc[the
substrate model]; where each concern lands is link:concern-ownership.adoc[concern
ownership]; the before/after delta is the link:../atlas/nixos-substrate.adoc[atlas view].
====

== The problem

Every RKE2 server auto-deploys any YAML placed under
`/var/lib/rancher/rke2/server/manifests/` at boot. Under the old model the manifests
scion rendered the tree and the incus GROW *mounted* it into the node via `/srv/host`.
That mount — and cloud-init — were dissolved by the NixOS-substrate refactor, leaving
NOTHING that writes to the node's `server/manifests`. The consequence: with
`services.rke2.cni = "cilium"` the node would still not converge the way the lab needs
(no HelmChartConfig, no Flux), and nothing seeds the GitOps bootstrap. The loop was open.

[[the-rule]]
== The rule — bootstrap set vs the rendered branch

The synthesis produces the whole cdk8s tree, but delivery splits by *when* a resource is
needed:

* the *rendered branch* (git, opt-in push) carries everything Flux reconciles POST-join —
  the workload manifests. Unchanged.
* the *node-side bootstrap set* carries the few resources that must exist BEFORE Flux can
  run: the cilium `HelmChartConfig` (customising RKE2's bundled cilium — the CNI, so the
  node reaches Ready without waiting on Flux), the Flux operator/instance/root, and the
  `sops-age` + `githubapp` Secrets Flux needs to pull and decrypt the branch.

A unit joins the bootstrap set by carrying the `io.seedmatic.rke2lab/node-bootstrap`
annotation (`ManifestAnnotation.NODE_BOOTSTRAP`), stamped once per unit by its
`PackageMetadataProfile(..., nodeBootstrap = true)` so no resource of a bootstrap unit
can escape the lane.

[IMPORTANT]
====
*Cilium native routing is DUAL-STACK and PER-CLUSTER.* The node's `cluster-cidr` is
dual-stack (`10.<44+id>.0.0/16,fd00:<44+id>::/56`) and differs per cluster, so it CANNOT be
a static `nixos/rke2.nix` literal — the `node-base` image is homogeneous across all clusters.
Instead the grow projects the CIDRs (from `ClusterNetworkBlueprint.podCidrDualStack(clusterId)`)
onto the instance's `user.rke2lab.cluster-pod-cidr` / `cluster-service-cidr` devlxd keys;
`rke2lab-identity` reads them into `/run/rke2lab/node.env`, and the `rke2lab-dualstack` oneshot
writes rke2's `10-dualstack.yaml` drop-in from there at boot (the same runtime-drop-in pattern as
`rke2lab-tls-san`). The cilium `HelmChartConfig` MUST enable IPv6 and set both native-routing
CIDRs — `routingMode: native` hard-fails the helm install (`ipv6NativeRoutingCIDR must be set
when routingMode is native`) the moment `ipv6.enabled` is true, and a v4-only config leaves the
node NotReady. `CiliumConfigManifestsUnit` derives those from the SAME blueprint
(`podCidr()` / `podCidrV6()`), so nix and cilium track one source (the identity-keyed blueprint)
rather than both hardcoding a literal. See link:../cluster-api/cluster-addressing-plan.adoc[the
cluster addressing plan] for the per-cluster CIDR formulae.
====

== The seam — synth carves, host poses, node writes

[mermaid]
----
flowchart LR
  subgraph osgi["OSGi realm (in-container)"]
    synth["synthesis scion"]
    explode["exploder"]
    file["NodeBootstrapArtifact.MANIFESTS<br/>.bootstrap/rke2lab-bootstrap.yaml"]
    cellar["ScenarioCellar<br/>SERVER_MANIFESTS (SEALED)"]
    synth --> explode
    explode -->|"NODE_BOOTSTRAP docs"| file
    file -->|"scion reads + files"| cellar
  end
  subgraph host["FLAT realm (host)"]
    grow["InstanceGrow"]
  end
  subgraph node["node (guest)"]
    unit["rke2lab-server-manifests.service<br/>(devlxd oneshot)"]
    dir["server/manifests/rke2lab-bootstrap.yaml"]
    rke2["rke2-server"]
  end
  cellar -->|"grow fetches (revealed)"| grow
  grow -->|"user.rke2lab.server-manifests"| unit
  unit -->|"writes, before"| dir
  dir --> rke2
----

. The *exploder* routes `NODE_BOOTSTRAP`-marked documents out of the per-resource branch
  tree into one multi-doc file, `NodeBootstrapArtifact.MANIFESTS` — a sibling one level
  above the exploded tree, so it is never committed.
. The *synthesis scion* reads that file and files it in the transactional cellar under
  `ServerManifestsCoordinate.SERVER_MANIFESTS` as a `ServerManifestsBundle`, SEALED (it
  carries the App private key and the cluster age identity).
. The pure-host *GROW* fetches it (revealed across the graft) and poses it on the
  instance's `user.rke2lab.server-manifests` devlxd key — alongside the cluster-PKI cases,
  the same dual-realm cellar crossing.
. The node's `rke2lab-server-manifests.service` (a devlxd oneshot, ordered BEFORE
  `rke2-server`) reads the key and writes `server/manifests/rke2lab-bootstrap.yaml`, so
  RKE2 auto-deploys the CNI + Flux on first boot.

[[second-poser]]
== The second poser — a node CAPI/CAPN provisions

Steps 1 and 2 above are realm-internal and hold for any cluster. Steps 3 and 4 do NOT: they name
`InstanceGrow` and a devlxd key. A control node CAPRKE2 provisions is created by CAPN from an
`LXCMachineTemplate` — no grow touches that instance, so nobody poses the key, and the node's
`rke2lab-server-manifests.service` (which is in the image, and works) has nothing to read.

Measured on `bioskop-wrkld` the day its first node was born: the bundle never arrived, so cilium fell
back to RKE2's bundled chart, which dials the apiserver at the in-cluster ClusterIP *before any CNI
exists*, timed out, and left the node `NotReady: cni plugin not initialized`. Nothing ran there at
all — so Flux could not have bootstrapped itself either. **The bootstrap set is not an optimisation;
it is the only reason a greenfielded cluster reaches Ready.**

So the seam does not get replaced, it *bifurcates*: same carve, same artifact, a second poser.

[mermaid]
----
flowchart TB
  ex["exploder carves — UNCHANGED<br/>one bundle per cluster"]

  subgraph A["poser 1 — host (shipped)"]
    ce1["cellar SERVER_MANIFESTS (SEALED)"]
    gr1["InstanceGrow → devlxd key"]
    n1["devlxd oneshot writes the file"]
    ce1 --> gr1 --> n1
  end

  subgraph B["poser 2 — CAPI/CAPN"]
    sec["Secret on THIS lane in rke2lab-&lt;cluster&gt;<br/>the lane that already carries the BYO-CA set<br/>and host-incus-identity — never the branch"]
    rcp["RKE2ControlPlane.files[]<br/>contentFrom.secret{name, key}<br/>path server/manifests/rke2lab-bootstrap.yaml"]
    n2["cloud-init write_files,<br/>before rke2 starts"]
    sec --> rcp --> n2
  end

  f["server/manifests/rke2lab-bootstrap.yaml"]
  rke2["rke2 auto-deploys the CNI, then Flux"]

  ex --> ce1
  ex --> sec
  n1 --> f
  n2 --> f
  f --> rke2

  classDef done fill:#d5f5d5,stroke:#2d7a2d,color:#112
  classDef todo fill:#fff2cc,stroke:#b8860b,color:#112
  classDef neutral fill:#e8e8f5,stroke:#555566,color:#112
  class ce1,gr1,n1 done
  class sec,rcp,n2 todo
  class ex,f,rke2 neutral
----

Four properties make this the shape rather than a variant of it:

* *The credential never enters a spec.* `RKE2ControlPlane.spec.files[].contentFrom.secret{name, key,
  namespace}` (CAPRKE2 v1beta2) is a REFERENCE, so the bundle stays in a Secret even though the
  workload's CR-set is itself rendered onto the MANAGER's branch (the rendered-branches model).
* *Single ownership is preserved with no new rule.* <<the-rule>> already routes a marked resource out
  of the branch, so Flux never sees these objects on either poser. No dual lane, no reconcile fight,
  nothing to compare.
* *Ordering is inherent.* `write_files` precedes `rke2`, which is exactly the guarantee the devlxd
  oneshot gives by being ordered `Before=rke2-server`.
* *No new type.* The artifact, the coordinate and the bundle record are the ones in <<the-types>>; only
  the transport differs.

=== When — no new trigger; the existing material gate

The render is triggered by a push to the rke2lab SOURCE branch, while what needs rendering here is a
fleet-state change (a workload target appearing). Those are different events, and the gap needs no
third one: `PoolAdoptionReconciler` already WAITS on material before it stamps the
`RKE2ControlPlane` — the BYO-CA Secrets, then the RKE2 config ConfigMaps — precisely because `files`
is injected once at creation (`ensure` is create-if-absent). The bundle Secret joins that same gate.

The event that introduces a target is the GROW: `workloadTargets` is grow-recorded into the root
`manifest.yaml` and replayed from HEAD by later UPDATEs. So the first render of a target's bundle
belongs to the grow; source pushes keep it in sync after.

[WARNING]
====
*The bundle is a PROVISIONING-time delivery.* `files` is written by cloud-init, so it reaches a node
CAPRKE2 *creates*. A node that was provisioned without it never receives it, and adoption does not
change that — which is why a node already running the default cilium chart stays `NotReady` no matter
how often the pool re-adopts it. It has to be re-provisioned.

The operator cold start — *delete the Incus instances, then let Pulumi reconcile the stack with
`refresh`* — composes with this cleanly, because the two nodes belong to different actors:

* The management node IS a Pulumi resource. `refresh` is what makes the stack notice it is gone (its
  state would otherwise still claim it exists), and the following `up` re-grows it — so mgmt gets its
  bundle through *poser 1*, unchanged. The cellar is host-side, so the sealed bundle survives the
  instance and can simply be re-posed.
* The workload node is NOT a Pulumi resource — CAPN owns it. Nothing in the stack re-creates it.
  What does is the pool: the reflection survives in git naming a pet whose instance is gone, every pet
  reads CONFIRMED absent, and that is exactly the
  link:../cluster-api/cluster-seeding-controller.adoc#void-reflection[void-reflection] path — the
  stale CR-set is dropped and CAPRKE2 greenfields a node against the freshly-stamped
  `RKE2ControlPlane`, which is where *poser 2* delivers.

Note the asymmetry this exposes: an etcd wipe alone would re-stamp the `RKE2ControlPlane` correctly
(`ensure` is create-if-absent, so a surviving one is NOT patched) but would re-adopt the surviving
workload instances, provisioning nothing — so the correct spec would reach no node. Deleting the
instance is what makes the delivery happen.
====

=== The one piece still to design

The carve runs per synthesis pass, so per rendered cluster — and a cluster being born has no pass. The
manager's pass must therefore run the `ClusterRole.WRKLD` unit set for each of its `workloadTargets`,
which produces the target's bundle and its `manifests/<cluster>` branch in one gesture. That is the
open item this seam now depends on; see
link:../cluster-api/manifests-rendered-branches.adoc[the rendered-branches model].

NOTE: The bootstrap set needs no per-role review: all eight units carrying `nodeBootstrap` sit in
domains `ClusterRole` enables for BOTH roles (`networking/cilium-config`; `gitops/flux-operator`,
`flux-instance`, `flux-root`, `githubapp`, `sops-age`; `cicd/pac-secret`, `render-signing`). `cicd` is
structural for both precisely because each cluster renders its own branch through its own Tekton.

=== Corollary — kube-vip is not a bootstrap concern

`seed-incluster` currently also injects a kube-vip RBAC `File` plus a `preRKE2Commands` recipe meant
to generate its DaemonSet. Both are to be DELETED, not fixed: the management cluster gets kube-vip
from Flux (`high-availability-kube-vip`), so a workload should too, from its own branch. The recipe
could never have worked on this image anyway — it needs `ctr` (only extracted once rke2 starts, and
this runs *pre*-rke2), `jq` (absent; the fleet convention is `yq`), and a registry pull under
`airGapped: true`. Its RBAC half is also what collides with the rke2lab unit's own
`system:kube-vip-role` / `-binding`, bound to a ServiceAccount in a different namespace.

[[the-types]]
== The types

[cols="1,2,2",options="header"]
|===
| Type | Module | Role

| `NodeBootstrapArtifact`
| `manifests-contract` (bundle-only)
| Enum whose one instance resolves the bootstrap file's path from an exploded tree — an
  instance, not a static path helper (instance-discipline). Used by the exploder (writes)
  and the synthesis scion (reads).

| `ServerManifestsCoordinate`
| `manifests-ingress-contract` (`type=dual-realm`)
| The `SeedCoordinate` the scion files under and the host GROW fetches by — dual-realm so
  the FLAT-realm GROW may reference it (the `ClusterPkiCoordinate` lesson).

| `ServerManifestsBundle`
| `manifests-ingress-contract` (`type=dual-realm`)
| The `@SeedContract("server-manifests")` record carrying the multi-doc YAML across the
  cellar; SEALED at rest.

| `NodeGithubTokenCoordinate`
| `manifests-ingress-contract` (`type=dual-realm`)
| The twin `SeedCoordinate` for the node's github *fetch* token — the coordinate the synthesis
  scion files under and the host GROW fetches by. Filed `Persistence.TRANSIENT` (not durable):
  the App creds are the durable source, the minted token is not, so it never persists between
  runs (the `github-token-mint-on-demand` discipline).

| `NodeGithubToken`
| `manifests-ingress-contract` (`type=dual-realm`)
| The `@SeedContract("node-github-token")` record carrying the fresh `contents:read` App
  installation token the standalone GROW poses into the node's cloud-init as
  `/run/rke2lab/nix-github.conf`, where the `rke2lab-rke2-config` oneshot reads it (nix
  `access-tokens`) to fetch the private `manifests/<cluster>` branch, then erases. SEALED at rest.

| `ReplicatorSourceSecretsMaterial`
| `manifests-contract` (bundle-only)
| The mittwald-replicator SOURCE secrets (git basic-auth, docker dockerconfigjson, tailscale
  oauth) the `replicator-secrets` seal rehydrates from `.secrets` and files SEALED; revealed
  in-container and rendered by `ReplicatorManifestsUnit` onto THIS lane, so the real credentials
  are seeded node-side and never committed to the branch. A blind manifests-side mirror (the
  `GithubAppMaterial` treatment).
|===

The *replicator source secrets* ride the bootstrap lane rather than the branch precisely because
they carry real credentials: the branch is committed via jgit with no sops filter, so a secret
rendered there would be plaintext (see the exploder's `.gitattributes` second belt). The seal
(`ReplicatorSecretsSealScenario`, sown by `the replicator secrets are sealed` before
incus-provision) reads `.secrets` `kubernetes.secrets` (the source→target mapping) + the
`tekton.*` / `tailscale.*` credential blocks through the read-only `SecretsGateway`, assembles the
`ReplicatorSourceSecretsMaterial`, and files it SEALED. The rendered source secrets carry the
mittwald `replication-allowed` + `replication-allowed-namespaces` annotations so the in-cluster
replicator fans each out to the target namespaces where the `replicate-from` placeholders live.

== Related

* link:../cluster-api/cluster-seeding-controller.adoc[The cluster seeding controller] — the second
  poser (<<second-poser>>): it owns the intentions, gates on the material, and stamps the
  `RKE2ControlPlane` that carries the bundle reference.
* link:../cluster-api/manifests-rendered-branches.adoc[The rendered branches] — which branch carries
  what, and the per-target pass this seam now depends on.
* link:substrate-model.adoc[The substrate model] — the vision this seam serves.
* link:erofs-store-lower.adoc[The EROFS store lower] — the other thing the image now carries whole: the store, as a read-only EROFS disk with a ZFS upper.
* link:erofs-store-layer-stack.adoc[The EROFS layer stack] — the same question as this seam, asked of the store rather than of the manifests: what reaches a node, and who materialises it. Answer there: `ndh` packs a layer per generation, the node mounts it.
* link:concern-ownership.adoc[Concern ownership] — the CNI + Flux row of the redistribution table.
* link:../atlas/nixos-substrate.adoc[Atlas — NixOS substrate] — the before/after delta.
* `nixos/bootstrap-manifests.nix` — the guest oneshot (the consuming end of the devlxd key).
* `nixos/rke2.nix` — `cni = "cilium"`, so the bundled CNI the `HelmChartConfig` customises
  brings the node Ready before Flux.
* link:../cluster-api/deterministic-cluster-access.adoc[deterministic-cluster-access] — the
  sibling dual-realm cellar crossing (cluster PKI) the GROW poses on devlxd the same way.
