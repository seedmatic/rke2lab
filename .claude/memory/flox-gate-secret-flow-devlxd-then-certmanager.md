---
name: flox-gate-secret-flow-devlxd-then-certmanager
description: "DIAGNOSTIC (2026-09-06, non fixé) — le scheduling-gate flox est INERTE en cluster (tailnet-purge & co démarrent avant leur env → CreateContainerError, self-heal par retry NRI). Racine : le webhook flox n'est pas déployé car son cert (cluster-pki, grow-sealed) est strippé par le render in-cluster secret-blind — cas d'un problème SYSTÉMIQUE de flow des secrets grow→cluster. Plan en 2 étapes tranché : (1) devlxd/NODE_BOOTSTRAP, (2) cert-manager."
metadata:
  node_type: memory
  type: project
---

**★ SHIPPÉ + VALIDÉ IN-CLUSTER (cold start bioskop-mgmt @823f6862, 2026-09-06).** Étape 1 = `ad5879c48` (+ fix render-mode `Optional`), étape 2 = `9949119f7`, poussés.

**SUIVI #3 — ARÊTE FLUX flox-consumer→runtime (SHIPPÉ `6ae7b8e4a`, poussé 2026-09-06, PAS encore render-validé).** Trou repéré par l'user : les workloads flox-consommateurs (kdns, tailnet-purge, funnel-mirror, headscale…) se déployaient AVANT que le webhook soit enregistré → pods non gatés (le webhook est `failurePolicy: Ignore`, il ne peut PAS retenir un pod lui-même) → `CreateContainerError` self-heal NRI. Fix : `FluxServiceKustomizationPlanner` auto-dérive une 3e source de `dependsOn` — toute cellule dont un pod template porte une annotation `flox.seedmatic.io/environment.*` OU `nix-build.*` (détection ground-truth par scan de l'arbre écrit, comme CRD→CR/WFC-PVC ; clé dérivée de l'enum `FloxAnnotation.ENVIRONMENT`/`NIX_BUILD`, PAS de `FloxDebugPolicy` qui est domaine-grossier + rate le nix-build cicd, PAS de dep déclarée par unité = manuelle/oubliable) attend LES DEUX cellules runtime `runtime/flox-webhook` (MutatingWebhookConfiguration + cert) + `runtime/flox-controller` (DaemonSet qui SERT le webhook + RÉALISE l'env), toutes deux `wait:true`. Self-skip des cellules runtime (pas d'auto-boucle). **#1 (render StartError) se dissout dans l'étape 2** : le webhook (maintenant live, RBAC pvc get+create OK ligne 186-188) crée le PVC `manifests-nix-store` + monte + injecte NIX_CONFIG au 1er render post-webhook → attendu self-résolu (valider par un re-render). NEXT restant = **#2 two-tier ClusterIssuer** (cf. FOLLOW-UP ci-dessous). Preuves live après grow : `MutatingWebhookConfiguration flox-controller` PRÉSENT (absent avant) ; Issuer+Certificate cert-manager READY → Secret `flox-controller-webhook-tls` ; `caBundle` injecté (ca-injector, 1536o) ; daemonset `--enable-webhook` ; Secret CAPI `bioskop-mgmt-kubeconfig` + ns `rke2lab-bioskop-mgmt` livrés par **devlxd/bootstrap** ; pods flox (headscale…) **`SchedulingGated`** au lieu de `CreateContainerError`. RÉSERVE : les pods créés AVANT l'enregistrement du webhook (tout début bootstrap : kdns, tailnet-purge) ne sont PAS gatés (un MutatingWebhook ne gate que les admissions postérieures) → self-heal NRI comme avant ; le webhook gate tout le post-boot. SÉPARÉ (pré-existant, non lié) : le **hook nix du pod render in-cluster** échoue (`StartError`, createContainer hook `name=nix` overlay `/nix` ← `/var/lib/flox-nri/nix-build-store`, flox-nri-plugin) → le render **in-cluster** reste cassé (le grow le contourne, render host-side) ; à traiter à part.

---

**Diagnostic conclusif d'origine (pour mémoire).**

## Symptôme
Pendant la reconciliation cold-start (avant que les flox envs soient réalisés), le pod `tailnet-purge` (Job, ns `mesh-system`, annotation `flox.seedmatic.io/environment.purge: mesh/tailnet`) échoue `CreateContainerError` : `failed to resolve flox environment: flox environment mesh/tailnet not GC-rooted at /nix/var/nix/gcroots/flox-runtime/env/mesh/tailnet`. Self-heal par retry NRI quand le DaemonSet réalise le gcroot (~12 min de reconcile).

## Racine (prouvée)
Le pod n'a **jamais été gaté** → il est schedulé avant que son env existe. La logique du gate est **saine** (`PodGateReconciler.readyNodesForEnv` : `nr.Ready && nr.ObservedGeneration==env.Generation` ; env absent → reste gaté, ligne 64-67 ; le tout derrière `hasGate` ligne 45). Le problème est **en amont** : **le webhook n'est pas déployé**.
- Live : `kubectl get mutatingwebhookconfiguration` → **aucun flox**. `daemonset/flox-controller` args = `--gcroot-base`, `--env-root` — **PAS `--enable-webhook`** (que le DaemonSet node-agent, pas de mode cluster-manager).
- Le webhook ET le `PodGateReconciler` ne sont enregistrés QUE sous `--enable-webhook` (flox-controller `cmd/.../main.go`, mode cluster-manager). Le DaemonSet fait double-emploi : node-agent + webhook **quand `--enable-webhook`**.
- `--enable-webhook` + le `MutatingWebhookConfiguration` sont émis **UNIQUEMENT si `ManifestSynthesisContext.current().webhookServing().isPresent()`** au render (`FloxControllerManifestsUnit.java:247`, `FloxWebhookManifestsUnit.java:85-86`).
- `webhookServing()` = `revealWebhookServing()` → cellier `ClusterPkiCase.WEBHOOK_SERVING`, **scellé UNIQUEMENT au GROW** (cluster-pki `ClusterSeal`/`ClusterPkiSealScenario`, cellier DURABLE Pulumi). L'**in-cluster render** (`PublishCliScenario`) tourne avec un **`EphemeralCellar`** semé seulement par `ghapp` → `WEBHOOK_SERVING` absent → `webhookEnabled=false` → **le webhook est strippé de la branche** (grow l'allume, 1er render in-cluster l'éteint ; prouvé : à `manifests/bioskop-mgmt@e0f789d5`, `--enable-webhook` absent du daemonset, `workloads/runtime/flox-webhook/` = group-marker vide).

## C'est SYSTÉMIQUE — flow des secrets grow→cluster
Tout `Secret` **gated-sur-reveal + rendu dans la branche** est strippé par le render in-cluster secret-blind (in-cluster : `.secrets` sops-chiffré illisible ET pas de seal cluster-pki). **CORRECTION (traçage 2026-09-06) : seuls DEUX étaient réellement exposés, pas trois** :
- **flox webhook TLS** (`revealWebhookServing`, cluster-pki) — `FloxWebhookManifestsUnit` → **Porte 2**,
- **kubeconfig CAPI** `<cluster>-kubeconfig` (`revealOperatorPki`, cluster-pki) — `ClusterKubeconfigManifestsUnit` → **Porte 1 (FIXÉ, cf. plan)**.
`GithubAppSecretManifestsUnit` roule **DÉJÀ** sur la voie durable (`new PackageMetadataProfile("gitops","githubapp", true)`) → jamais dans la branche, **PAS strippé** (le diagnostic précédent le comptait à tort). Le **replicator source secret** aussi. Le push in-cluster marche via le token **PaC env** (`RKE2LAB_PUSH_TOKEN`).

## L'INVARIANT — manifests ne porte JAMAIS de matériel de secret dans une ressource réconciliée par Flux (les DEUX PORTES)
Corollaire de la branche secret-blind ; gravé dans `pac-in-cluster-render-spec §secret-delivery`. Un secret n'entre dans le cluster que par deux portes, choisies par **immuable vs rotatable** :
- **Porte 1 — voie durable NODE_BOOTSTRAP (grow-only)** : l'exploder détourne le Secret HORS de la branche → `.bootstrap/rke2lab-bootstrap.yaml` → `SERVER_MANIFESTS` → clé devlxd `user.rke2lab.server-manifests` au grow → oneshot RKE2 `rke2lab-server-manifests` au bootstrap master. **Ne joue JAMAIS dans une réconciliation** (le prune Flux est inventory-scoped ; ces ressources n'ont aucun label d'inventaire Flux car appliquées par RKE2 → Flux aveugle en apply ET prune → **PAS d'écrasement**). IMMUABLE : kubeconfig CAPI, githubapp, sops-age, replicator source, signing key. Opt-in = annotation `ManifestAnnotation.NODE_BOOTSTRAP` : whole-unit `PackageMetadataProfile(...,true)` OU **per-resource pour une unité MIXTE** (patron `ReplicatorManifestsUnit`/`ClusterKubeconfigManifestsUnit` : seul le credential + son namespace ridnt, `dependsOn` pour l'ordre du fichier bootstrap ; le reste reste sur la branche).
- **Porte 2 — un contrôleur mint in-cluster depuis une graine durable** : manifests émet une **CR (pas un Secret)** ; le contrôleur réconcilie le Secret in-cluster → un re-render blind ne re-émet que la CR, rien n'est strippé. ROTATABLE/dérivé : cert-manager (`Certificate` → mint+renew, ca-injector remplit le `caBundle`), replicator mittwald (fan-out depuis la source durable), **operator Tailscale (cert funnel `*.ts.net` in-cluster — précédent user validé)**.
- **Complémentaires** : un secret rotatable utilise souvent LES DEUX (Porte 1 = graine durable, Porte 2 = dérivation). **INTERDIT** = la 3e voie : un Secret reveal-peuplé rendu direct sur la branche → strippé silencieusement.

## PLAN — 2 étapes (RÉORDONNÉ 2026-09-06 après traçage)
1. **Étape 1 — Porte 1 (devlxd) — CODÉ (à builder + commit)** : `ClusterKubeconfigManifestsUnit` — le Secret CAPI `<cluster>-kubeconfig` + son namespace basculés **per-resource `NODE_BOOTSTRAP`** (l'operator-kubeconfig reste un dotfile `local-config` consommé host-side au grow, PAS sur la voie durable). githubapp/replicator déjà durables. **Réarme le kubeconfig CAPI, PAS la gate flox.**
2. **Étape 2 — Porte 2 (cert-manager) — RÉARME LA GATE FLOX** : `--enable-webhook` (daemonset) + le `MutatingWebhookConfiguration` + son `caBundle` sont TOUS rendus-branche ET gated sur `webhookServing().isPresent()` → **la voie durable seule NE PEUT PAS réarmer le webhook** (le `caBundle` surtout ne peut pas être rendu in-cluster sans le cert ; mettre juste le TLS sur Porte 1 = jetable). cert-manager mint le cert self-signed in-cluster (Issuer CA self-signed), POSSÈDE le Secret (jamais dans la branche, jamais prune), ca-injector remplit le `caBundle` ; `--enable-webhook`+manifests émis **INCONDITIONNELLEMENT**. Supprime le chemin cluster-pki `WEBHOOK_SERVING` (SEUL consommateur → cleanup total : `WebhookServingCredentials`, `ClusterPkiCoordinate.WEBHOOK_SERVING`, `ClusterSeal.mintWebhookServing`/`WEBHOOK_SERVING_DNS`, `SealedClusterPki`, `WebhookServingMaterial`, `ManifestSynthesisContext.webhookServing`, `ManifestSynthesisRequest.webhookServing`, `revealWebhookServing`, le gate `webhookServing().isPresent()`).

## FOLLOW-UP two-tier NOTRE CA — SHIPPÉ + GROW-VALIDÉ (2026-09-06, option A, commit `a6cf5a29b`)
**VALIDÉ IN-CLUSTER** (clean-slate : `pulumi stack rm dev` + `incus delete master` + rebuild skipCache + `up`) : `ClusterIssuer rke2lab-ca` **Ready:True** ; certs `flox-controller-webhook-serving` ET `capi-operator-serving-cert` en `issuerRef ClusterIssuer/rke2lab-ca` (l'Issuer self-signed opérateur SUPPRIMÉ, `kubectl get issuer -n capi-operator-system` = vide) ; les webhooks PROVIDERS (`capi-selfsigned-issuer`, `rke2-{bootstrap,control-plane}-selfsigned-issuer`) restent self-signed = exception attendue (installés au runtime par l'opérateur, hors render). **PIÈGE VÉCU (adoption)** : le seal est idempotent (gate=`cluster-ca-bundle`) ⇒ un simple delete-instance+up NE re-scelle PAS (la case persiste dans le state Pulumi) → `cluster-issuer-ca` jamais frappée, Secret absent, `ClusterIssuer ErrGetKeyPair`. + un build `-Pnxmatic` avec build-cache a servi des bundles `cluster-pki-*` PÉRIMÉS (scellaient encore `webhook-serving`, ignoraient `cluster-issuer-ca`). FIX RÉEL = clean-slate total (`stack rm` + rebuild `skipCache`). RETENIR : pour adopter une nouvelle case scellée sur un cluster existant SANS clean-slate, il faudrait l'additif (option C, SopsDecryptor gardé) — pas fait. **BUG runtime QUE `verify` NE VOIT PAS** : une dep d'unité cross-domaine exige une dep de DOMAINE (`ManifestsDomain` 2e arg) — `cluster-api→platform` manquait → `PhaseFailure` in-container au grow (pas au build). Trou de couverture à combler (test registre plein-facet). **passe `@Nullable→Optional` = SHIPPÉE + BUILD-VERTE (2026-09-07, `-Pclaude,all-worlds … clean verify` OK).** Politique : SEUL `@MonotonicNonNull` autorisé (checkerframework, `provided`), tout autre `@Nullable`→`Optional` ou restructure. Tranché avec l'user : (1) champs de builders requis-set-once (BootstrapPaths/ManifestSynthesisResult/KdnsAssets/Net2PlanEndpoint/ClusterNetworkBlueprint/Generalist.access + DefaultManifestSynthesisService.State.scaffold/registry)→`@MonotonicNonNull` ; champs builder vraiment-optionnels-avec-défaut (Generalist.driftSpecialist/ledgerRegistry)→`Optional`. (2) fronts internes→`Optional` : SystemdNotReadyError.snapshot, LaunchSecretsWriter env (`Function<String,Optional<String>>`), SystemdAdapterScenario.endpointFrom param. (3) fronts externes RESTRUCTURÉS : doctor `@JsonCreator fromWire`×3 (Symptom/ProblemRef/RemediationProgramRef)→**throw-on-unknown** (⚠️ change la tolérance : wire malformé crashe le decode au lieu de dégrader — assumé) ; `ThrownModel` composants message/cause/fileName→`Optional` (`of` non-null ; SeedCodec a Jdk8Module donc sérialise ; consommateurs ReconstructedThrowable/GraftThrowable/ScenarioPlayer/ThrownModelTest ajustés) ; `@Reference` OSGi `enclosureGate`→champ `Optional<EnclosureGate>` (DS 1.5.1 R8 supporte le field-ref Optional). **PIÈGE VÉCU** : l'organiseur d'imports du hook Edit STRIPPE un import ajouté au 1er edit d'un batch si son usage n'arrive qu'à un edit ULTÉRIEUR du même batch (vu sur MonotonicNonNull dans Generalist+DefaultManifestSynthesisService, Optional dans ThrownModel+SystemdNotReadyError) → toujours revérifier import-vs-usage après un batch import+usage, sinon `cannot find symbol` au build. RESTE `MedicalRecordReader:128`=commentaire (non-problème). PAS encore commité (l'user pousse). NEXT : CA cluster workload au grow **bioskop-wrkld** (idée : cert-manager mgmt pré-crée `<cluster>-ca/-etcd/-proxy/-sa` sous rke2lab-ca AVANT que CAPI provisionne — viable car mgmt cert-manager préexiste ; inconnue : caprke2 consomme-t-il ces Secrets ou distribue la CA par join RKE2 ?).

**PERF cold-start Flux — retryInterval 30s (commit `cc65473de`, SHIPPÉ+grow-validé)** : les Kustomizations par-service n'avaient pas de `retryInterval` → =`interval` 5m → une Ks NotReady re-queue à 5m ET ne re-checke ses `dependsOn` que sur son tick ⇒ chaque NIVEAU de dep coûte jusqu'à 1 tick de 5m. Run 1 (avant fix) : objets sains ~2min mais Ks Ready ~11min plus tard (POLLING pur ; prouvé : kube-vip pod Ready 21:28:55, sa Ks Ready 21:39:43). Fix = `retryInterval:30s` (dormant quand Ready car alors `interval` gouverne ; actif seulement NotReady=cold-start). Run 2 (après) : front 11→~6min, span total ~9m40s. Attribution run 2 : ~6min chaîne infra SÉRIE (bootstrap→cert-manager→cluster-issuer→cert flox-webhook→flox-controller-operators) + ~2min réaliseur (6 envs ~20s chacun, PAS le goulot) + ~1.5min queue mesh (env tailnet→purge→tailscale helm→connector→funnel). Le reliquat = travail série sur chaîne PROFONDE que mes arêtes (two-tier cluster-issuer + #3 flox-runtime) ont approfondie — correct mais allonge le chemin critique. Leviers non-pris : réduire profondeur, paralléliser purge→tailscale (si best-effort), demand-prioritize réaliseur (faible gain ici).
**Tranché option A** (fresh-seal, pas d'additif ; SopsDecryptor GARDÉ = bijection d'encrypt, réutile future). Chaîne : mammoth-skate root → intermédiaire → **6ᵉ CA `cluster-issuer-ca`** (frappée sous le MÊME intermédiaire dans `ClusterCaGenerator.generate`→record `ClusterCaSet`, HORS bundle-nœud 11-clés) → leaves d'app. **Scellement** : `ClusterPkiCoordinate.CLUSTER_ISSUER_CA` + record `ClusterIssuerCa(caCertChainPem,caKeyPem)` SEALED ; `SealedClusterPki` 4-arg ; `ClusterPkiSealScenario.Then` store. **Manifests** : miroir `ClusterIssuerCaMaterial` via case neutre `ClusterPkiCase.CLUSTER_ISSUER_CA`→request→context ; **`ClusterIssuerManifestsUnit`** (platform/cluster-issuer, FOUNDATION) = `ClusterIssuer rke2lab-ca` kind CA (branche, inconditionnel) + Secret `rke2lab-ca-key-pair` type tls en **kube-system** (= cluster-resource-namespace de cert-manager, `--cluster-resource-namespace` NON fixé) stampé **NODE_BOOTSTRAP** (Door 1, jamais branche). **Migrations** : flox `Certificate` issuerRef→`{ClusterIssuer,rke2lab-ca}` (Issuer self-signed SUPPRIMÉ) + dependsOn platform/cluster-issuer ; **CAPI opérateur** via seam `UpstreamYamlInclusion.UpstreamRewrite` (accept/transform fournis par l'appelant, design A) — `CaIssuerRewrite` drop l'`Issuer capi-operator-selfsigned-issuer` + repointe le `Certificate capi-operator-serving-cert`→rke2lab-ca, + dependsOn platform/cluster-issuer. **CAPI cluster-CA (workload) = HORS SCOPE** : CAPI aujourd'hui = opérateur seul, ZÉRO nœud provisionné, `kubectl get clusters` vide, mgmt mono-nœud, master hors-CAPI (seed-master). La cohérence CA workload devient vivante au **grow bioskop-wrkld** (Phase 2.B/2.C) — et LÀ l'idée user (cert-manager mgmt pré-crée les `<cluster>-ca/-etcd/-proxy/-sa` du workload sous rke2lab-ca AVANT que CAPI provisionne) est VIABLE (mgmt cert-manager préexiste au workload — pas la collision d'ordre du master mgmt). Inconnue à lever alors : caprke2 consomme-t-il ces Secrets ou distribue la CA par join RKE2 ? **ADOPTION = cold-start** : le seal idempotent (gate=`cluster-ca-bundle`) ⇒ purger du state Pulumi les 3 cases cluster-pki (`cluster-ca-bundle`+`cluster-age-key`+`admin-credentials`) + l'orphelin **`webhook-serving`** (mort depuis étape 2) → `pulumi state delete <urn>` → rebuild instance → `up` re-scelle les 4 cases fraîches. **LE repassé en PROD** (`TailscaleManifestsUnit useLetsEncryptStagingEnvironment: false`) à faire avant le grow. Doc `deterministic-cluster-access.adoc` §ClusterIssuerCa.

## ★ WORKLOAD secret/CA delivery — investigation 2026-09-09 (2 sub-agents) : PRÉREQUIS cloud-init TRANCHÉ, fork ouvert

Reprise pour décider comment le cluster **workload** (bioskop-wrkld, créé par CAPI+CAPN+CAPRKE2) reçoit sa CA/secrets. Deux inconnues upstream LEVÉES :
- **CAPN `LXCMachineTemplate.spec.template.spec.config`** = `map[string]string` SANS whitelist ni webhook (v1alpha2, `api/v1alpha2/lxcmachine_types.go`, mergé verbatim dans `InstancePut.Config`) → on PEUT poser `user.rke2lab.*` verbatim (voie A devlxd viable côté mécanisme).
- **CAPRKE2 v0.25.2 honore lookup-or-generate (BYO-CA = OUI)** : si les secrets existent, il ne régénère pas. **MAIS noms spécifiques RKE2, PAS kubeadm** : `<cluster>-ca` (server), `<cluster>-cca` (client), `<cluster>-etcd` (etcd server), `<cluster>-peer-etcd` (etcd peer), tous `kubernetes.io/tls` (tls.crt + tls.key=clé privée CA), namespace du cluster. **PAS de `-sa` ni `-proxy`** (RKE2 les auto-génère) → l'idée gravée avec ces suffixes était partiellement fausse.
- Verdicts : **(B) ESO depuis mgmt = PAS VIABLE** pour la CA (circulaire : ESO a besoin de l'API kube qui a besoin de la CA). **(C) pré-créer les 4 secrets** VIABLE ; reco initiale = **C2** (les 4 secrets mintés par le SEAL cluster-pki existant, racinés `mammoth-skate-tls`, rendus NODE_BOOTSTRAP dans le ns workload sur mgmt, révélés comme `AdminCredentials`→`OperatorPkiMaterial`) — meilleur que cert-manager-sous-`rke2lab-ca` qui ajoute un intermédiaire → re-risque `PATH_LENGTH_EXCEEDED` (`deterministic-cluster-access.adoc:251-266`).

**★ PRÉREQUIS DÉCISIF (résolu 2026-09-09) : le node-base N'A PAS DE cloud-init.** `services.cloud-init` activé NULLE PART ; `nixos/identity.nix:6` + `nixos/bootstrap-manifests.nix:2` disent explicitement « No cloud-init … cloud-init and the /srv/host mount are gone ». `rke2-server` est BAKÉ (`nixos/rke2.nix` `services.rke2.enable=true` role server cni cilium), configuré par des oneshots devlxd (`rke2lab-{identity,dualstack,tls-san,node-labels}` `before rke2-server`) ; AUCUN `token:`/`server:` baké (master mgmt = cluster-init solo). ⇒ **CAPN injecte le bootstrap CAPRKE2 dans `cloud-init.user-data` que le guest IGNORE** → le bootstrap CAPRKE2 est INERTE → **C moot COMME CANAL** (CAPRKE2 ne pilote pas le RKE2 du nœud ; sa conso de CA ne vaut que si son bootstrap tourne). Le vrai problème = **collision de modèles** (« rke2 config-ownership CAPRKE2-vs-node-base » du plan, révélé profond) : comment le HA workload se FORME (join token+server URL+init-vs-join, non baké/non livré aujourd'hui).

**FORK à trancher (pas codé, design) :** **D1** = rendre node-base cloud-init-aware en forme MIDDLE (datasource cloud-init qui n'ÉCRIT QUE des drop-ins `config.yaml.d` — token/server/CA ; RKE2 reste baké, cloud-init NixOS ne peut pas installer mais peut write_files) → CAPRKE2 pilote la config, C redevient naturelle ; reste à répartir l'ownership `config.yaml.d` (CAPRKE2=token/server/CA vs oneshots devlxd=identity/cidrs/san/labels). **D2** = garder purement déclaratif + étendre devlxd au join HA (CAPN pose `user.rke2lab.*` token/server/init, nouveau oneshot les écrit ; CAPRKE2 = couche intent, cloud-init ignoré ; il faut sourcer le token DE CAPRKE2 VERS devlxd) — se bat contre le contrat CAPI.

**★ 3e sub-agent (2026-09-09) — objectif USER reformulé = UN SEUL canal des DEUX côtés (standalone seed-master ET in-cluster CAPI). Verdict tranché sur source upstream :**
- **devlxd-uniforme ÉCARTÉ** : CAPN (`lxc/cluster-api-provider-incus` v0.9.0) hardcode le payload bootstrap vers `cloud-init.user-data` (`internal/instances/kubeadm.go:37-41`) — aucun flag/champ pour rerouter. `LXCMachineSpec.Config` (map, no-whitelist) peut écrire `user.rke2lab.*` MAIS seulement en LITTÉRAL statique → **sur la branche** (pas sourcé d'un Secret au reconcile). SEUL `bootstrap.dataSecretName → cloud-init.user-data` lit un Secret **hors-branche**. Donc D2/devlxd-uniforme exigerait un **poseur impératif in-cluster** (lit le Secret bootstrap, pose `user.*` via l'API incus) qui **se bat contre le contrat CAPI** — non viable proprement.
- ⇒ **le seul canal uniforme secret-safe = cloud-init/userdata PARTOUT** (natif CAPN ; seed-master bascule de `user.rke2lab.*` vers `cloud-init.user-data`). **Rework CONTENU** : garder les oneshots node-base (ils lisent `/run/rke2lab/node.env` + matériel sops → écrivent `config.yaml.d`/`server/tls`), changer seulement la SOURCE (cloud-init écrit `node.env`+matériel au lieu de devlxd) ; périmètre = `identity.nix`+`sops.nix` (source) + `InstanceGrow` (pose cloud-init au lieu des clés plates). cloud-init comme TRANSPORT ≠ provisioner OS → l'image reste déclarative, la doc substrate-model n'est pas violée (elle rejetait cloud-init comme forkeur d'OS).
- **Fait RKE2/CAPRKE2 v0.25.2 (source `bootstrap/internal/controllers/rke2config_controller.go`)** : en HA, **SEUL le nœud INIT a besoin de la CA** (`handleClusterNotInitialized`→`LookupOrGenerate` les 4 secrets `<cluster>-{ca,cca,etcd,peer-etcd}`) ; les **joiners** (`joinControlplane`, l.816-818) ne reçoivent QUE le **token + ServerURL** (`https://<vip>:9345`) et **récupèrent la CA du serveur init via le join**. Le **token** est le seul secret partagé par TOUS (l.432, généré une fois). ⇒ payload per-nœud petit ; C2 (CA seal-mintée) ne cible que l'init.

**FORK NET (factuel) :** **(U) userdata-uniforme** = 1 canal des 2 côtés (ce que l'user veut), atteignable, rework contenu, MAIS touche le chemin mgmt PROUVÉ + réintroduit cloud-init (comme transport). **(D1) asymétrie** mgmt=devlxd / workload=cloud-init = zéro rework mgmt, 2 canaux, défendable (bootstrappeurs différents) mais contredit l'uniformité. Reco Claude = **U** (uniformité atteignable + rework contenu) sauf si zéro-risque-sur-mgmt prioritaire → D1. **★ U TRANCHÉ par l'user (2026-09-09) : on bascule sur cloud-init/userdata partout.** Plan de migration ordonné (6 étapes) + verdicts faisabilité (A datasource incus = SHIM A1 NoCloud obligatoire ; B CAPRKE2 = inert `/opt/install.sh`+airGapped ; C = oneshots gardés sur node.env) + risques (R1 rke2-server auto-start à gater After=cloud-init) gravés dans **`.claude/cloud-init-uniform-channel-plan.md`**. **★ ÉTAPES 1-4 SHIPPÉES + GROW-VALIDÉES LIVE (2026-09-09, bioskop-mgmt master renew).** Le bootstrap mgmt passe désormais ENTIÈREMENT par `cloud-init.user-data` (plus aucune clé `user.rke2lab.*` devlxd). Commits `871ac540e` (shim NoCloud, étape 1) + `5f21d32dd` (source-swap node-base identity/sops/rke2 + suppression bootstrap-manifests + `InstanceGrow` génère le cloud-config base64 + record `NodeBootstrapMaterial` host-only), NON-POUSSÉS. Preuves live sur `master` : shim active ; seed user-data=71988B ; node.env vraies valeurs ; 3 drop-ins config.yaml.d ; `rke2lab-identity` + `sops-install-secrets` BOTH active (CA déterministe livrée+déchiffrée+installée via cloud-init) ; server-manifests bootstrap présent (Flux semé) ; rke2 up, kube API up, reconcilie. Gotchas résolus : shim `wantedBy`/`before` `cloud-init-local.service` (PAS cloud-init.target) ; `write-files` ∈ cloud_init_modules = cloud-init.service ; conteneur privilégié (raw.lxc proc:rw) requis pour le write hostname. Plan complet + risques : `.claude/cloud-init-uniform-channel-plan.md`. **RESTE = Étape 5 (workload) : `RKE2ConfigTemplate.airGapped=true` + `/opt/install.sh` inerte baké (CAPRKE2 n'a pas de skip-install), pour que le canal cloud-init CAPRKE2 pilote un rke2 BAKÉ sans tenter `curl get.rke2.io`.** Ça + foundation 2b (render -wrkld) + C2 (CA workload = 4 secrets seal NODE_BOOTSTRAP dans mgmt) = le chemin workload. See [[workload-grow-foundations-resume]].

## Contraintes gravées (pour l'étape 2)
- Le **webhook flox DOIT rester self-signed** (cert-manager self-signed CA), PAS Tailscale : il gate les pods tailscale eux-mêmes (**circulaire**), tourne **avant le tailnet** (bootstrap), SAN interne `flox-webhook.rke2lab-system.svc` (Tailscale ne signe que `*.ts.net`).
- **Pas besoin de persister** ce cert (ni PVC ni ZFS) : l'API-server lit le `caBundle` depuis le `MutatingWebhookConfiguration` (ca-injector) → un CA self-signed régénéré par cluster-lifetime est cohérent (rien d'externe ne l'épingle). YAGNI.
- L'idée user **self-signed→Tailscale + persist `tank/rke2lab/persist`** est bonne mais pour les **certs EXTERNES** (funnel/headscale `*.ts.net`, confiance publique + stabilité d'identité au rebuild) → **3e chantier distinct**, pas le gate.

See [[cold-start-cleanup-and-funnel-cert-persistence]] [[manifests-publish-in-cluster-render]] [[flox-env-migration-design]].
