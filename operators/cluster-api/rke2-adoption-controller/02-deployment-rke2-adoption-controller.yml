---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    io.seedmatic.rke2lab/domain: cluster-api
    io.seedmatic.rke2lab/layer: operators
    io.seedmatic.rke2lab/package: rke2-adoption-controller
  name: rke2-adoption-controller
  namespace: rke2lab-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: controller
      app.kubernetes.io/name: rke2-adoption-controller
  template:
    metadata:
      annotations:
        flox.seedmatic.io/environment.controller: cluster-api/rke2-adoption-controller
        flox.seedmatic.io/gid.controller: "0"
        flox.seedmatic.io/home.controller: /root
        flox.seedmatic.io/uid.controller: "0"
      labels:
        app.kubernetes.io/component: controller
        app.kubernetes.io/name: rke2-adoption-controller
    spec:
      containers:
      - args:
        - --health-probe-bind-address=:8081
        - --metrics-bind-address=:8080
        command:
        - rke2-adoption-controller
        env:
        - name: HOME
          value: /root
        image: rke2lab/flox-carrier:0.1.0
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: controller
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        volumeMounts:
        - mountPath: /.config/flox
          name: flox-config
        - mountPath: /.cache/flox
          name: flox-cache
      serviceAccountName: rke2-adoption-controller
      volumes:
      - emptyDir: {}
        name: flox-config
      - emptyDir: {}
        name: flox-cache
