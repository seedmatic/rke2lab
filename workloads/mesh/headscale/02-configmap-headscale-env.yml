---
apiVersion: v1
data:
  CLUSTER_LAN_HEADSCALE_INETADDR: 192.168.1.193
  DARWIN_HOST: bioskop-wrkld
  HEADPLANE_NAMESPACE: "${headplane-namespace}"
  HEADSCALE_NAMESPACE: mesh-system
  HEADSCALE_URL: http://headscale.mesh-system.svc.cluster.local:8080
  RKE2_CLUSTER_NAME: bioskop-wrkld
  VIP_NETWORK_CIDR: 10.80.7.0/24
kind: ConfigMap
metadata:
  annotations:
    io.seedmatic.rke2lab/domain: mesh
    io.seedmatic.rke2lab/package: headscale
  name: headscale-env
  namespace: mesh-system
