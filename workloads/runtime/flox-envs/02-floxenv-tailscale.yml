---
apiVersion: flox.seedmatic.io/v1alpha1
kind: FloxEnv
metadata:
  annotations:
    io.seedmatic.rke2lab/domain: runtime
    io.seedmatic.rke2lab/package: flox-envs
  name: tailscale
  namespace: rke2lab-system
spec:
  consumption: overlay
  folder: mesh
  manifest:
    install:
      bash:
        outputs: all
        pkg-path: bash
      coreutils:
        outputs: all
        pkg-path: coreutils
      tailscale:
        flake: floxcatalog:catalogue#tailscale
    options:
      systems:
      - aarch64-linux
    schema-version: 1.10.0
