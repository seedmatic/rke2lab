= Atlas L1 — In-cluster seeding view (seed-incluster)
:toc: left
:toclevels: 3

_Level-1 (per-subsystem) view of the link:../integration-atlas.adoc[integration atlas] (L0)._
_L0 shows how this subsystem integrates with the others and the value that emerges; this file is the detail._

The in-cluster twin of the link:seed.adoc[host-side seed orchestration]. Where that view runs
`pulumi preview`/`up` from the host (out-of-band, the STANDALONE root boot), this subsystem —
`seed-incluster` — is the *in-cluster* reconciler that brings CAPI's view of the fleet into being and
keeps it true, **by adopting the clusters already running, not re-provisioning them**. The full spec
(resource model, funnel, config ownership, federation) is
link:../cluster-api/cluster-seeding-controller.adoc[Cluster Seeding Controller]; this view frames the
**avant→après** of the current evolution: the roster-persistence turn that makes the model
*unconditionally* deterministic.

[NOTE]
.Terminology — "reflector" here ≠ the pipeline reflectors
====
This view's *cluster→git reflector* (a controller that mirrors observed state into git, in the sense of
Flux's `image-reflector-controller`) is UNRELATED to the host pipeline's `ShapeReflector` /
`SplitReflector` / `ManifestsAmendReflector` (Java-reflection / amendment-merge components in
link:seed.adoc[the seed L1] + link:config.adoc[config L1]). Different subsystem, different sense of
"reflect". It is always qualified "cluster→git reflector" here, and its resource is `PoolReflection`.
====

[[thesis]]
== The thesis — determinism is a property of git↔reality, and we own the missing direction

Adopt-first re-derives the CAPI CR-set from the *intention* every reconcile, so a cluster is
deterministic — but only **conditionally**: it assumes the running reality never drifts from the
declared roster. The moment a node set diverges from `ClusterIntention.spec` (dynamic scaling →
CAPI-minted random names, a replaced node), the branch reflects the *intent*, not *reality*, and a
cold-start reproduces what the render *believes*, not what *was*.

The system already runs one direction — **git→cluster** (Flux applies the render; the controller
adopts). The missing key element is the OTHER direction, **cluster→git**: a controller that observes
the live cluster and commits its observed roster back onto the managing branch, so git is reconciled
FROM reality. `git == reality` → a cold-start reproduces the *actual* cluster, even after scaling.
That is **unconditional** determinism, and it is the role newly given to the cluster→git reflector.

[[diagram-w]]
== Diagram W — the seeding model: BEFORE (codebase) vs AFTER (vision)

Blue = shipped, untouched. Green = added (the vision). Red-dashed = a gap deliberately closed.

[mermaid]
....
flowchart LR
  subgraph B[before — codebase: the 2×2, CONDITIONAL determinism]
    b_int["ClusterIntention / PoolIntention<br/>desired · Flux/git"]
    b_ad["ClusterAdoption / PoolAdoption<br/>observed EPHEMERAL · etcd (wiped at cold-start)"]
    b_ros["roster = Intention.spec.nodes (canonical)<br/>match by providerID · adopt-first"]
    b_det["determinism CONDITIONAL<br/>git reflects INTENT, not reality"]
    b_gap["Provisioning phase reached, NO action<br/>(the greenfield gap)"]
    b_int --> b_ad --> b_ros --> b_det
    b_ad -.-> b_gap
  end
  subgraph A[after — vision: OBSERVE + NOTE, the reflection IS the switch]
    a_int["ClusterIntention / PoolIntention<br/>desired · Flux/git"]
    a_ref["cluster→git reflector (uni-directional)<br/>OBSERVE Machines → NOTE the roster · Flux OUT<br/>image-automation · App-token"]
    a_refl["PoolReflection<br/>git-only YAML doc · observed DURABLE roster<br/>on manifests/&lt;cluster&gt; (escape-preserved)"]
    a_sw{"PoolReflection present on the branch?<br/>(read from git — not etcd; immune to Flux timing)"}
    a_adopt["ADOPT · pre-create the named Machines<br/>CAPRKE2 adopts by name"]
    a_green["GREENFIELD · declare RCP(replicas)<br/>CAPRKE2 provisions its OWN · we only OBSERVE"]
    a_det["determinism UNCONDITIONAL · git == reality<br/>CAPI/CAPN/CAPRKE2 own the Machine lifecycle"]
    a_int --> a_ref --> a_refl --> a_sw
    a_void["VOID · all reflected pets CONFIRMED gone<br/>drop their CR-set, remember status.voidedPets<br/>the only controller-decided greenfield"]
    a_sw -->|"yes · lived before"| a_adopt
    a_sw -->|"yes, but every pet gone"| a_void
    a_sw -->|"no · first boot / reset"| a_green
    a_void -.->|"CAPRKE2 re-provisions → reflector REWRITES"| a_refl
    a_void --> a_det
    a_green -.->|"CAPRKE2 creates → reflector NOTES"| a_refl
    a_adopt --> a_det
    a_green --> a_det
  end
  classDef built fill:#eef,stroke:#88a,stroke-width:1px;
  classDef new fill:#d4f4dd,stroke:#2e7d32,stroke-width:2px;
  classDef gone fill:#fdd,stroke:#c33,stroke-width:2px,stroke-dasharray:4 3;
  class b_int,b_ad,b_ros,b_det,a_int built;
  class b_gap gone;
  class a_ref,a_refl,a_sw,a_adopt,a_green,a_void,a_det new;
....

== Verdict — does it hold?

. *The reflection IS the switch, and we only OBSERVE.* The 2×2 stays; the vision ADDS one role —
  `PoolReflection`, a **git-only YAML document** (observed, DURABLE) on the managing `manifests/<cluster>`
  branch — NOT a CRD, never applied to etcd. Its PRESENCE decides adopt vs greenfield — NO runtime
  liveness probe, read directly from GIT (immune to any apply-ordering; the operator view is the
  intention's *status*, not an etcd copy): present → adopt the observed roster (pre-create the named
  Machines); absent → greenfield. The controller never drives the Machine lifecycle: in greenfield it
  declares the RCP `replicas` and CAPRKE2 provisions; it only observes and NOTES the result. What it AVOIDS is the
  reverted "flip" (the controller deleting/re-creating Machines to force a shape), which raced
  CAPRKE2's replica controller. ONE bounded exception was added on 2026-09-21: when CAPN confirms
  EVERY reflected pet gone, the reflection is VOID — it describes a pool that no longer exists, and
  holding is a self-sustaining deadlock (the reflection names the pets, the adopt branch re-creates
  their Machines, the reflector observes those Machines and re-writes the same reflection; no term is
  anchored in the instances). It deletes WITHOUT re-creating, at a name CAPRKE2 will not re-use, and
  remembers the decision in `status.voidedPets` — so it is not the churning shape.

. *The reflector is not a new orchestrator.* It mirrors reality into git via the blessed
  `image-automation-controller` loop (`fetch → commit → push`, retry-on-conflict, never force-push,
  App-token). The precedence decision lives in the controller (`PoolReflection` presence), not in git's
  merge semantics; the two committers (render, reflector) own disjoint files, so git never 3-way-merges.

. *The document is git-only — no CRD, no etcd apply.* The reflector writes a name-based `owner-ref`
  annotation (UID-free, so it survives the cold-start UID death) purely for TRACEABILITY + orphan
  detection: a reflection whose owner `PoolIntention` is gone is deleted by the reflector on the branch.
  There is no etcd copy and no ownerRef re-stamp — the operator view is the intention's status.

. *The boundary is structural, not a preference.* The root (`STANDALONE`, self) stays canonical because
  it CANNOT reflect its own state during its own cold-start (the controller does not exist yet at its
  bootstrap); only `IN_CLUSTER` managed clusters — observed by their managing plane — are
  reflector-eligible. So determinism is unconditional exactly where it can be.

Conclusion: *the reflector comes FIRST — it produces the `PoolReflection` whose PRESENCE is the
adopt-vs-greenfield switch, turning a conditionally-deterministic model into an unconditionally-
deterministic one (`git == reality`). The controller OBSERVES and NOTES; CAPI/CAPN/CAPRKE2 own the
Machine lifecycle.* Status: DESIGNED — the 2×2 + adopt are shipped; the reverted greenfield "flip" is
superseded; the reflector + the presence switch + greenfield-as-observe are the coded residue. See
link:../cluster-api/cluster-seeding-controller.adoc#reflector[the reflector spec].

== Related documentation

* link:../cluster-api/cluster-seeding-controller.adoc[Cluster Seeding Controller] — the full spec:
  resource model (2×2 + `PoolReflection`), adopt-first funnel, greenfield-arm, the reflector, federation.
* link:seed.adoc[Seed orchestration L1 (host-side)] — the STANDALONE twin that seeds the root out-of-band.
* link:../management-workload-topology.adoc[Management / Workload Topology] — the `STANDALONE` vs
  `IN_CLUSTER` axis the reflector boundary maps onto.
