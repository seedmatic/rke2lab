# Memory index — rke2lab

Project memory for rke2lab. Cross-cutting facts (profile, conventions, principles) + cross-repo chantiers live in the **hub** ([[hub:MEMORY]] at `/private/var/lib/git/nxmatic/claude-hub.d/main`), auto-loaded as session root. One line per entry (~200 chars max); detail lives in the file. Links: `[[name]]` = rke2lab-local, `[[hub:name]]` = hub.

## Current chantier — flox envs → runtime delivery (NixOS substrate)

- [★ flox envs → RUNTIME via une FloxEnv CRD + contrôleur (design CONVERGÉ 2026-08-27, specs `ec76cd6ae`, code PAS commencé)](flox-envs-runtime-crd-delivery.md) — **sortir les flox envs de l'image bakée → chaque env = un `FloxEnv` CR (repo `github:seedmatic/flox-controller`) réconcilié par un contrôleur node-agent sur le `/nix` HÔTE + gcroots ; le plugin (nôtre) résout l'annotation `[<ns>/]<name>` comme réf de CR ; PAS de PVC (spike : closures grosses + déjà sur le store hôte, le CR = inspectable).** Pur-k8s domain-agnostic (domain→namespace) ; `spec.folder` (défaut=namespace) pilote le layout host + les `[include]`. Foundation (plugin+hooks+config) reste bakée. See [[flox-carrier-nix]] [[node-image-cold-start-digest-gap]] [[nixos-node-substrate-state]].
- [★★ flox-controller BÂTI + intégré + DÉPLOYÉ (RESUME 2026-08-28)](flox-controller-build-deploy-state.md) — **le composant existe (develop 96d3833, VERSION 0.0.0-develop), rke2lab le consomme+bake+synthétise (CRD+DaemonSet+RBAC), déployé sur bioskop-mgmt.** 4 bugs pelés au déploiement : deadlock layer (namespace→foundation), node-label jamais posé (kubelet --node-labels seulement au 1er join → oneshot nixos boot `30-node-labels` + gate `node-label-concord`), Entrypoint /bin faux, **Entrypoint store-path MASQUÉ par le mount host /nix → binaire = fichier RÉEL hors /nix** (leçon clé, comme le carrier /etc). Base carrier = **embarqué+auto-provisionné par le contrôleur** (`EnsureBase`). DERNIER inconnu = l'**exec on-node** (`flox activate`/`containerize`) — valider en `nix run` sur le node (pas de re-grow). Checklist migration + progress-log seam différés. See [[flox-envs-runtime-crd-delivery]] [[flox-carrier-containerize-spike]].
- [★ Spike carrier — flox containerize = fakeNss symlinks, PAS de post-step vrais fichiers (2026-08-27, flox 1.14)](flox-carrier-containerize-spike.md) — **le contrat `/etc/{passwd,group,nsswitch}` du carrier est fourni par `flox containerize` en symlinks `fakeNss` vers `/nix/store` (pas de fichiers réels ; passwd a déjà `root:x:0:0`).** Pas un problème : le plugin overlaie `/nix→/nix` avec le `/nix` HÔTE en lower → le `/nix` de l'image est JETABLE, l'hôte fait autorité pour TOUT (dont l'entrypoint = store path). Donc les symlinks ne sont pas plus fragiles que l'entrypoint → **Route B** (pas de post-step) : le contrôleur RÉALISE+GC-roote le closure-image COMPLET sur chaque node (sinon `nix.gc` moissonne fakeNss). Cause racine de [[flox-carrier-nix]] révisée (baked ne réalisait pas sur l'hôte). containerize marche SANS FloxHub (warning seul). Doc `flox-controller/docs/design.adoc` = AsciiDoc + 4 figures Mermaid (context, reconcile sequence, pattern `/nix` jetable, seam) — FAIT 2026-08-28. See [[flox-envs-runtime-crd-delivery]] [[flox-carrier-nix]].
- [★ cicd in-cluster render loop SHIPPED+PUSHED + ★ funnel gotcha (2026-09-01)](tailscale-operator-funnel-nodeattr.md) — **la boucle de render in-cluster est bâtie & poussée** : units cicd `RepositoryManifestsUnit`(PaC Repository CR)+`RenderPipelineManifestsUnit`(PVC maven-cache + Task git-fetch + Task render-publish flox-injectable + Pipeline) + `PacSecretManifestsUnit`(pipelines-as-code-secret, 3 clés, NODE_BOOTSTRAP) + `.tekton/render.yaml` stub (annotations flox `step-render`) + `FloxEnvFolder.CICD` + FloxEnv `cicd/maven` ; push token in-cluster (scion container-aware : cellar OPERATOR / env `RKE2LAB_PUSH_TOKEN` IN_CLUSTER, extrait du git_auth PaC) ; `flox-catalogue` expose `jdk25`+`maven` (mêmes attrs nixpkgs que le build, `inherit (pkgs)`). Commits `564f143f8`→`72fb3255f`+`14601a712`(.secrets) / catalog `1351dcb6d`. **Cérémonie webhook FAITE** : App `seedmatic-rke2lab` (id 4581114) webhook→`pac-webhook.mammoth-skate.ts.net` (l'App avait DÉJÀ checks:write+push+statuses) ; Flux basculé sur un webhook de REPO→`flux-webhook.…/hook/7d6ed571…` (prouvé `200`). `.secrets` `github.webhook.token`→`.secret` (clé k8s reste `token`, contrat Flux). **★ GOTCHA = [[tailscale-operator-funnel-nodeattr]]** (funnel `tag:k8s` pas `autogroup:members`). Design PaC : Tekton=manifests rendus. NEXT = **re-grow + vérif PaC bout-en-bout** (dig pac-webhook→IP, PipelineRun render-manifests sur push). Détail chantier `[[manifests-publish-in-cluster-render]]` (vit dans main — mémoire worktree divergente). See [[flux-per-service-kustomizations]] [[flox-controller-build-deploy-state]].
- [tailnet stale-device GC — deterministic trigger FOUND, design converged NOT coded (2026-09-02)](tailnet-prune-on-incus-renewal.md) — **le renouvellement de l'instance incus EST le trigger** du prune des devices tailscale stale (le `pac-webhook-2` qui cassait le webhook). Design : `local.Command` (dép `com.pulumi:command`) dans `InstanceGrow.createInstance` (Instance a `deleteBeforeReplace`+`replaceOnChanges config`), `triggers=[imageFingerprint]`, `sleep <délai> ; manage-tailnet --prune-stale-devices --stale-after <t> --yes` (délai REQUIS = lag offline Tailscale). Binaire = `inputs.ndh.apps.${system}.manage-tailnet` (rke2lab a déjà l'input ndh). Auth = OAuth `operator-oauth` de rke2lab (paramétrer manage-tailnet pour env-creds ; `.secrets` CLAIR en standalone via `filter=sops-yaml`, chiffré in-cluster → prune=op dev). OPEN = localiser la valeur brute du client_secret (seal `ReplicatorSourceSecretsMaterial`, pas le `.secrets` config). DIFFÉRÉ (regarder le cold-start d'abord). **2026-09-03 : tailnet-prune SHIPPÉ opérateur-side (`dac7eb5f9`, over-seed manage-tailnet) MAIS le design a pivoté → voir [[cold-start-cleanup-and-funnel-cert-persistence]] (prune tailnet déplacé IN-CLUSTER avant enregistrement, supersède le Command).** See [[manifests-publish-in-cluster-render]] [[tailscale-operator-funnel-nodeattr]].
- [★ cold-start cleanup + funnel-cert persistence (design CONVERGÉ 2026-09-03, PAS codé)](cold-start-cleanup-and-funnel-cert-persistence.md) — **★★ le vrai fix du "no TaskRuns" : le cert LE du funnel `pac-webhook` explose la limite (5 certs/FQDN/168h) sur re-grows répétés → TLS échoue → livraisons webhook GitHub timeout.** Décisions : (1) **persister** le cert (nom stable) pas rotation (plafond 50/domaine/sem) ; (2) storage 2-tiers `tank/rke2/control-nodes/<node>/*` éphémère vs `tank/rke2/persist/*` persistant (cert funnel + maven-cache ; nom `persist` = conv. impermanence) ; (3) cert-persist = état proxy tailscale sur PVC `persist/` via ProxyClass + `TS_STATE_DIR` (le Secret est nommé `$(POD_NAME)`, non-stable) ; (4) **DEUX prunes SÉPARÉS** (pas d'orchestration) : **Job oneshot tailnet** avant l'opérateur tailscale (prévient le drift ; manage-tailnet linux + Secret `operator-oauth` répliqué ; **supersède `dac7eb5f9`**) + **DaemonSet ZFS-orphelins** par-node (nsenter, pattern flox-controller ; exempte `persist/*`). Facts : ~62 datasets orphelins (3.28G), opérateur ts v1.102.3, App 4581114 partagée PaC+push. Ce soir = attendre reset LE ~2026-09-04 02:30 UTC ou rename one-off. See [[tailnet-prune-on-incus-renewal]] [[manifests-publish-in-cluster-render]].
- [★ ghapp-webhook reconcile + funnel rename pipelines-webhook (SHIPPED+PUSHED 2026-09-04, `8835b9fae`)](ghapp-webhook-reconcile-and-funnel-rename.md) — **★★ scion `ghapp-webhook` semé au grow re-pointe le webhook de l'App (PATCH /app/hook/config, App JWT, edge cultivating→PEND sous survey) + pose le secret ; l'opérateur ne touche plus l'UI webhook.** Contract 1.1.0 (`GithubAppWebhookConfigurer`/`WebhookConfig`/`WebhookReconcileInput` @Amendment FUNNEL), edge, bdd (Scenario+Handler+AmendReflector, patron dataplan), `Amendment.FUNNEL`. Funnel SSOT = `PacWebhookFunnel(tailnet)` dans **`manifests.ingress` DUAL-REALM** (bundle-only manifests.contract casserait realm-boundary pour le host-flat) ; **renommé pac-webhook→pipelines-webhook** (budget LE frais, limite par-FQDN) ; proxy-class/state-secret/PV dérivent tous de `LEAF` → rename = 1 édition. `force:true` sur chaque service-cell Kustomization (Jobs immuables re-templatés → delete-and-recreate au lieu de Ready=false). `REGISTRATION_URL` enrichi (webhook_active+url+events[]). **2026-09-04 SOIR : boucle render PROUVÉE end-to-end** (push→App webhook→PaC→PipelineRun `render-manifests-8vlnt` Succeeded : nix build claude-hub PRIVÉ fetché + maven + render+publish). Fixes additionnels (tips→`4871b3ff5`, ndh `baa59d48`) : operator-oauth→couche operators (deadlock), .tekton resolver ns→rke2lab-system, **claude-hub privé déplacé nxmatic→seedmatic** (flake repoint + nix access-tokens + PaC scope via TektonConfig `platforms.KUBERNETES` PAS openshift), `concurrency_limit=1`. Interventions manuelles one-time (patch tektonconfig retrait `platforms.openshift` stale, prune tailnet avant enregistrement funnel, delete Jobs funnel). **cache maven/nix VIDE** (build froid chaque render) : cause = `nix build .#manifests-cli` évalue PUR (« ignoring untrusted flake configuration setting 'pure-eval' ») → `getEnv M2_REPO/MAVEN_BUILD_CACHE`="" → cache non câblé. **FIXÉ = `accept-flake-config = true` dans le NIX_CONFIG du NRI flox** (`flox-controller internal/floxenv/floxenv.go NixConfig()`, `dc4fe19` develop ; rke2lab flake.lock bumpé). `sandbox=false` était DÉJÀ dans NixConfig → mon `033da2a46` redondant→DROPPÉ (reset `fed3e5d78`). scaffold tuée, dev sur develop. **NEXT = déployer flox-controller dc4fe19 (grow/build) + vérifier le dataset maven-cache se peuple + que le publish ff-push (branche figée à 99d4b481).** Détail dans le fichier. See [[cold-start-cleanup-and-funnel-cert-persistence]] [[flux-per-service-kustomizations]] [[spec-coverage-gate-state]].

- [flox env migration → FloxEnv CRs (2026-08-29 : INTÉGRÉ + COMMITTÉ + POUSSÉ)](flox-env-migration-design.md) — **★★ kdns = 100% contrôleur.** `FloxControllerManifestsUnit`(CRDs+RBAC) + `FloxFlakeCatalogueManifestsUnit`(GitRepository) + `FloxEnvManifestsUnit`(nommé PAR FLAVOR) ; fix régression netplan `192.168.1.320` ; closures kdns bakées RETIRÉES ; branche `flox-catalogue` publiée (flake à la racine). Webhook LIVE (re-grow #1), HEADSCALE MIGRÉ, enum durable `FloxEnvFolder`. NEXT = valider headscale → HEADPLANE → dissoudre CM. See [[flox-controller-build-deploy-state]] [[constants-review-backlog]].
- [FloxEnv readiness gating — locus A SHIPPED 2026-09-06 (flox-controller develop, PAS commité)](floxenv-readiness-node-scoped-condition.md) — **★ scheduling gate implémenté DIRECTEMENT (B abandonné, shell flox-wait SUPPRIMÉ).** Webhook pose `spec.schedulingGates:[flox.seedmatic.io/env-ready]` (guard `NodeName==""`, webhook DOIT être CREATE-only) ; `PodGateReconciler` (cluster-manager, `--enable-webhook`) résout les `RefsFromAnnotations`→FloxEnvs, calcule le set de nodes prêts par env (`ready && obsGen==generation`), **intersecte**, puis — pod encore gated — **AND `kubernetes.io/hostname In {intersection}` dans le nodeAffinity requis (KEP-3521 permet d'AJOUTER l'affinity à un pod gated)** et retire la gate en un `Update`. **u1 (résolution node) RÉSOLU par ce narrowing** : pas besoin de nodeName, on épingle AVANT d'ungate → le scheduler binde sur un node qui peut monter l'env ; single-node dégénère, multi-node correct (ce que B devait couvrir → plus besoin). `RefsFromAnnotations` splitte sur le DERNIER `/` (folder structuré `mesh/base` récupéré entier). SSOT spec màj `docs/architecture/patterns/flox-env-readiness-gating.adoc`. Tests verts (compile-run direct, `go test` OOM ici). Latence (realiser priorisé) + cascade (Flux `timeout`) INCHANGÉS. NEXT rke2lab = `FloxControllerManifestsUnit`: grant SA `pods get;list;watch;update` + scope webhook CREATE ; puis u2 agrégat Ready node-aware (hors chemin gate), u3 timeout Flux. See [[flox-env-migration-design]] [[flox-controller-build-deploy-state]] [[tailnet-prune-on-incus-renewal]].
- [★★ gate flox INERTE in-cluster → flow des secrets grow→cluster (DIAGNOSTIC 2026-09-06, plan 2 étapes, PAS fixé)](flox-gate-secret-flow-devlxd-then-certmanager.md) — **le scheduling-gate ne gate RIEN en cluster : `daemonset/flox-controller` n'a pas `--enable-webhook`, aucun `MutatingWebhookConfiguration` flox → tailnet-purge & co démarrent avant leur env (`CreateContainerError`, self-heal retry NRI).** Racine : `--enable-webhook` + le webhook sont gated sur `webhookServing().isPresent()` au render (`FloxControllerManifestsUnit:247`), cert scellé UNIQUEMENT au grow (cluster-pki `WEBHOOK_SERVING`) → le render in-cluster **secret-blind** (EphemeralCellar, `.secrets` sops-chiffré) le **strippe** (prouvé `@e0f789d5` : daemonset sans flag, `workloads/runtime/flox-webhook/`=group-marker vide). **SYSTÉMIQUE** : idem kubeconfig (`revealOperatorPki`) + githubapp (`revealGithubApp`) — tout Secret gated-sur-reveal rendu dans la branche est strippé. Voie durable = **NODE_BOOTSTRAP/devlxd** (le replicator l'utilise déjà). **PLAN TRANCHÉ 2 étapes : (1) router flox-webhook-tls + kubeconfig + githubapp par devlxd ; (2) webhook cert → cert-manager self-signed (le webhook DOIT rester self-signed : circulaire avec tailscale + pré-tailnet + SAN interne ; pas de persist — caBundle via ca-injector).** Cleanup étape 2 = tout le chemin cluster-pki `WEBHOOK_SERVING` (seul consommateur). See [[floxenv-readiness-node-scoped-condition]] [[cold-start-cleanup-and-funnel-cert-persistence]].
- [★ render des secrets = la CLI manifests, PAS un pipeline neuf (DESIGN gravé 2026-09-09, PAS codé)](secrets-render-facet-of-manifests-cli.md) — **la CLI manifests rend déjà les secrets ; à l'explode le matériel `NODE_BOOTSTRAP` est effacé de la branche + extrait dans le matériel de bootstrap (Porte 1) → branche = ZÉRO secret.** Jumeau in-cluster = même CLI (`PublishCliScenario`), aujourd'hui secret-blind ; à dé-aveugler par l'identité de reveal (cellier recipients seeding→seeded, clé age livrée Porte 1). Application PAR incarnation : grow=explode→bootstrap@boot ; **in-cluster=server-side apply direct dans `rke2lab-secrets`**→replicator (fork tranché user). **LE vrai reste = déchiffrer `.secrets` in-cluster SANS filtres git smudge/clean → `SopsDecryptor` (existe) + clé age en-process, sur le blob brut ; + fetcher la source portant `.secrets`, pas que la branche rendue.** Chemin : ESO(REST/webhook)→écarté (mgmt=racine, pas de refresh)→Flux oublié→ça. See [[secret-tiers-eso-model]] [[flox-gate-secret-flow-devlxd-then-certmanager]].
- [secrets tiers ESO — bootstrap→ingress→fan-out (design 2026-09-09, PAS codé)](secret-tiers-eso-model.md) — **modèle 3 couches : Porte 1 bootstrap ; ingress (mgmt=racine ex-`.secrets`/seal, workload=ESO kubernetes provider tire du mgmt) ; fan-out (replicator mittwald, GARDÉ).** ESO=INGRESS (de l'extérieur→`rke2lab-secrets`), replicator=FAN-OUT (charnière→namespaces) = distincts. **ESC ABANDONNÉ** (cloud, on est local). Rotation mgmt = re-seal ciblé, PAS d'endpoint permanent. RENAME `rke2lab-replicator-source`→`rke2lab-secrets`. Downstream du provisioning workload. See [[secrets-render-facet-of-manifests-cli]] [[flox-gate-secret-flow-devlxd-then-certmanager]].
- [manifests render — cache & facet (2026-09-06 : fix planner PUSHÉ+PROUVÉ, 2 follow-ups)](manifests-render-cache-and-facet.md) — **★ race Connector tailscale au cold-start FIXÉ (`412724651`) : le planner Flux sautait l'arête CRD→CR quand fournisseur+conso partagent le COORD ; tailscale est le seul installeur dont l'opérateur (operators layer) et son CR (workloads) sont même coord `mesh/tailscale` → arête `workloads→operators` jamais émise → dry-run avant CRD. Fix = ne sauter que la cellule propre.** PROUVÉ localement (recette : build `-Dmaven.build.cache.enabled=false` + `java -Drke2lab.manifests.publish.mesh=true ... synthesize`). **MAIS le render in-cluster n'a PAS reflété le fix : son maven build cache a servi un `manifests-core` périmé** → follow-up = buster le cache du render (`skipCache=true`). **+ asymétrie facet : `synthesize` ignore `recordedFacet()`/`facet.file` (posture opérateur, mesh OFF par défaut) ; seul `publish` le lit** → enhancement proposé (synthesize lit le manifest du worktree par défaut, PAS implémenté). Contraintes build worktree : cache maven off obligatoire (sinon `-am` ne résout pas les siblings), `all-worlds` lance les tests LIVE (éviter). See [[manifests-publish-in-cluster-render]] [[render-facet-follows-grow]] [[floxenv-readiness-node-scoped-condition]].

## Active chantiers — OSGi / unitrepo / pipeline

- [★★ Cellier TRANSACTIONNEL — DONE (2026-07-16, commit e5b058386 ; vérifié 2026-09-06)](cellar-transactional-design-state.md) — **ScenarioCellar UNIVERSEL (seul écrivain de tag) + SeedRunLedger (fabrique + open/commit/rollback) + SeedRunLedgerExtension (pont JUnit, injection champ patron SessionSeed, PAS ParameterResolver — jGiven narre les params).** Framework OSGi PARTAGÉ → pas de compteur distribué (sow synchrone, graft=bracket), l'identité voyage par un txId frappé-racine propagé dans le trigger du sow, clé LDAP côté scion. (A) atomique tout-ou-rien, tag pas attachment. Timing jGiven LEVÉ par construction (begin/end ne touchent pas le modèle; verdict via getExecutionException). Spec §cellar-transactional réécrite (ancienne forme superséée), figures dans `.claude/claude-preview.adoc`. **SHIPPÉ comme overlay : `TransactionalCellar`+`TxIdSeed`(scenario-engine/container)+`ScenarioCellar` EXISTENT (le cellier est le seul écrivain de tag) — cette forme SUPERSÈDE le design initial `SeedRunLedger`/`SeedRunLedgerExtension` 4-étapes (jamais codé sous ce nom).** Reste = build -Pall-worlds complet. See [[cluster-seed-execution-state]] [[collaborative-design-method]].
- [★★ Preview = TESTBED — 4 défauts packaging/gate fixés + correction I6 host-tree (2026-07-14)](preview-testbed-defects-2026-07-14.md) — **jouer la 1re `pulumi preview` a marché comme banc d'essai: chaîne de défauts pelée un à un.** FIXÉS: D1 gson stagé-only (realm-lib sur dép directe, `eeebd0efe`), D2 junit-platform transitif (realm-lib PROPAGE sur imports mandatory, `1fa8e26b5`), D3 SessionSeed jamais enregistré→SeedRun null (`@RegisterExtension`), D4 **le gate REALM_BOUNDARY aveugle à type=contract** (`forbidden`=isDomain seul → `isBundleSide`=isDomain||isContract, `147f21f34` — LE gate qui a menti, 0 WARN sur un vrai leak). RESTE: D5+D6 même anti-pattern (host-flat lit contrat domaine typé). **Correction I6 (tranchée user): le host tree APPARTIENT À INCUS, pas manifests** — c'est l'instance incus qui le MONTE (rke2-manifests.d = 1 folder parmi N); le CRUD/fold/rotation/drift = scénarios INCUS dédiés (`IncusBddScenarios`), `Host*Entry`→`incus-contract`; HostTreeHead/SlotSelector/Differ host-flat SUPPRIMÉS de seed-master, host garde le bind Pulumi seul. Gravé spec §CORRECTION + atlas Diagram V. NEXT = coder la refonte (graver d'abord, tranché). See [[gson-flat-vs-staged-regression]] [[master-execution-stage-missing-state]] [[controlplane-to-osgi-migration-frame]].
- [★★ Cellier étendu = producteur de resources (RENVERSEMENT gravé 2026-07-14)](cellar-extended-resource-producer.md) — **le cellier n'est pas qu'un store post-récolte : sur Pulumi, `Cellar.store` PRODUIT la resource → l'ancien `ResourceManager` s'y DISSOUT.** Tout scion contribue (pas que le docteur). Tranché: dependsOn CAUSAL (graphe d'amendements, option C, pas la position runbook); deux natures Pulumi (resources vs outputs top-level = façade de lecture host SANS métier, twin de fetch); le SCION marque la valeur d'output par `@Scion(Role.FRUIT)`, la façade hisse mécaniquement; le SOL ne va jamais au cellier (push au sow). Granularité (a) fruit-entier maintenant, (b) marquage-au-champ = évolution PLANIFIÉE (on tombera dessus dans le top-down). Gravé: host-cellar-realisation-spec §every-scion-contributes, atlas/seed Diagram R, lexique (fruit). NEXT = aligner OSGi (brique incus). See [[master-execution-stage-missing-state]] [[controlplane-to-osgi-migration-frame]].
- [★★ Master execution stage MISSING (CONSTAT 2026-07-13)](master-execution-stage-missing-state.md) — **l'étage host d'EXÉCUTION du provisioning master a été SUPPRIMÉ de seed-master, pas débranché.** ClusterSeedScenario a la même ossature que BootstrapPipeline de main mais n'a repris que la NARRATION (sow-and-graft), pas le corps. 6/10 packages controlplane absents (resources/incus/pipeline/bbox/readiness/systemd), dont ResourceManager + IncusResourceBootstrap ~3490l + **BootstrapPaths** (roots + vue host/staging vs node) — d'où le scénario n'a AUCUN état de provisioning, l'outdir du scion manifests n'a pas d'origine. Ligne tranchée: seul le graphe gRPC reste host (3/17 classes incus touchent com.pulumi), TOUT le reste = scion. Chantier = migration TOP-DOWN du pipeline. Pas borné en cours: links→contributor central adossé au catalog (RKE2LAB_POLICY_LINK_*=symlink par domaine, lu par rke2lab-manifests-install.sh), l'overlay est une exception à dissoudre. See [[controlplane-to-osgi-migration-frame]] [[master-provisioning-state]] [[cluster-seed-execution-state]].
- [ClusterSeed two-worlds recomposition (★ CURRENT brainstorm, 2026-07-08)](cluster-seed-execution-state.md) — **★★ RESUME: fork B tranché.** Reprise pour réactiver `NestedRunbookTest` (seul `@Disabled`, chemin d'échec cluster-readiness) → a divergé en une recomposition deux-mondes du seed scenario. TRANCHÉ B (sous-scénarios joués IN-CONTAINER, greffés `addNestedStep` dans le runbook host) — les 2 nœuds OSGi = les 2 îlots jGiven d'origine (systemd-adapter, cluster-readiness). Graft PROUVÉ comme rendu (spike `CrossWorldGraftSpikeTest`) mais transport STUBBÉ → reste à câbler live. **Fragments fakes ABANDONNÉS pour jouer un scénario** (comportement figé) → remplacés par MOCKS semés par le test dans Felix (`context().registerService` en `@BeforeAll`, configurable). Live≠test: scénario principal IDENTIQUE, consomme le registre, ne sème rien. Incrément 1 = cluster-readiness in-container (patron `DoctorScenario`, DSL sur cluster-port, `cluster-core-test`). Q1/Q3 tranchées: tout-dans-OSGi RESSERRE la frontière (host cesse d'importer cluster-port), ZÉRO mot ajouté à world-gateway (ObservationWire+ReadinessVerdict suffisent). Figures + les 3 hand-offs dans `.claude/claude-preview.adoc` (§RECOMPOSITION, §B TRANCHÉ, §DISCIPLINE). See [[world-gateway-frontier-discipline]] [[per-domain-osgi-fakes-chantier]] [[bdd-pipeline-migration-plan]].
- [world-gateway frontier discipline (★ FEEDBACK, 2026-07-08)](world-gateway-frontier-discipline.md) — **★ règle systémique: jamais ajouter un mot à world-gateway sans prouver d'abord qu'aucun mot existant ne porte le besoin.** La frontière host↔OSGi est le contrat le plus coûteux (engagement des 2 mondes, touche le gate SCHEMA_CONCORD). DEUX niveaux de port à ne jamais confondre: world-gateway = mots de FRONTIÈRE (traversent) vs cluster-port/systemd-port = INTRA-domaine (ne traversent JAMAIS). Corollaire contre-intuitif: pousser la logique DANS OSGi resserre la frontière. See [[cluster-seed-execution-state]].
- [gateway crossing — the three natures (★ FEEDBACK, 2026-07-08)](gateway-crossing-three-natures.md) — **★ POURQUOI la règle ports/gateway (transfert structuré mais opaque) est riche mais dure à APPLIQUER — l'user a buté dessus plusieurs fois.** Le même objet a TROIS natures le long d'une traversée (typé vivant → String JSON opaque → typé vivant, parfois autre classloader) et RIEN au point d'usage ne signale dans quelle phase on est, ni si la frontière franchie est un seam (partagé) ou un edge domaine (bundle). La compréhension est là (2 réflexes justes à chaud: "traverser en JSON", "on a déjà un ObjectMapper"); ce qui manque = un signal LOCAL de la phase de traversée. Dual de [[world-gateway-frontier-discipline]] (quels mots) ↔ celui-ci (à quel point c'est dur à VOIR). See [[system-exports-seam-gate-backlog]] [[document-seam-cannot-expose-jackson-jsonnode]].
- [specs se découpent en s'INSPIRANT des modules (★ FEEDBACK, 2026-07-08)](specs-decompose-like-modules.md) — **★ transposer la MÉTHODE de découpage des modules aux specs, PAS s'aligner 1:1.** Les modules ont enseigné: axes orthogonaux (domaine × rôle), découpe par NATURE, no-mixing strict, un foyer par chose. Découper les specs PAR NATURE avec la même rigueur — un spec ne fusionne pas "thèse BDD" (pattern) + "orchestration seed" (archi sous-système), comme un module ne fusionne pas port+edge. Symptôme: `pipeline-spec.adoc` mélange 3 natures. Décision de re-cut PAS prise ("map first"); cette note EST la carte (blast radius ~30 liens). See [[gateway-is-rest-in-jvm-insight]] [[cluster-seed-execution-state]].
- [port vocabulary — pas de dup inter-domaine (analyse 2026-07-08)](port-vocabulary-not-cross-domain-dup.md) — **Réponse à "le vocab des ports est vaste, un vocab général multi-domaine éliminerait-il du spécifique?" → NON.** Mesuré: world-gateway (18) = 6 transport GÉNÉRIQUE (Document/Coordinate/Domain/WireEnum — déjà minimal, c'est LE vocab multi-domaine cherché, il existe) + 12 chargés par le SEUL domaine doctor/readiness (aucun consommateur ailleurs). Les petits ports (cluster/incus/systemd) ne se dupliquent PAS entre eux (contrats distincts). La vraie dup = jumeaux Wire↔records (ObservationWire↔Observation…), prix ASSUMÉ de l'aplatissement seam. Verdict user: noter, refactorer RIEN. See [[gateway-crossing-three-natures]] [[world-gateway-frontier-discipline]].
- [★ la gateway EST du REST in-JVM (INSIGHT 2026-07-08, point de départ du prochain brainstorm)](gateway-is-rest-in-jvm-insight.md) — **★★ l'user a nommé le modèle de la gateway: transporter toute la REQUÊTE par UNE porte (pas des appels de service partagés), chaque monde garde SA copie du vocabulaire. "J'ai réinventé REST."** Le code est déjà ~90% là: `ReadinessAuthority.assess(Document)`/`ConsultingService.consult(Document)` sont déjà envelope→envelope (REST), juste emballés dans des interfaces de service partagées (le RPC résiduel). Une porte unique `handle(Document)→Document` les remplacerait → seam réduit à l'enveloppe requête/réponse, les 12 mots doctor redeviennent INTERNES OSGi, jumeaux Wire dissous, signal LOCAL retrouvé. Répond à la vast-vocab en rendant le vocab NON-PARTAGÉ. Questions ouvertes: qui possède la porte + dispatch par coordinate (multiplexor). See [[gateway-crossing-three-natures]] [[port-vocabulary-not-cross-domain-dup]] [[cluster-seed-execution-state]].

- [ClusterSeed execution state (RESUME HERE, 2026-07-06)](cluster-seed-execution-state.md) — **★★ mid-flight resume point.** Plan at `docs/superpowers/plans/2026-07-05-cluster-seed-scenario-plan.md` (gitignored), figures at `.claude/claude-preview.adoc` (gitignored, on disk — read first). Committed: Task 1 `c901949`, Task 2 `4b40a9d` (RunMode to rework), ClassRealm socle `00c2441`. IN FLIGHT: socle ClassRealm chantier (steps 3-6 left) interrupted ClusterSeed on purpose. THEN rework RunMode, THEN resume ClusterSeed Tasks 3-9. Known Task-2 drifts to fold + build command inside. Subagent-driven; drift→joint review. See [[classrealm-adaptable-pattern]] [[runmode-livegate-pulumi-abstraction]] [[collaborative-design-method]].
- [boot.discovery mandatory import in-container (fix 2026-07-07)](boot-discovery-mandatory-import-incontainer.md) — **★ socle regression fixed.** Every in-container test via `withJUnitRunner()` NoClassDef'd on `boot.discovery/ClassRealm`: the ClassRealm chantier (`8ccc8ed`) made `JUnitLauncherCore.wiringOf` load ClassRealm UNCONDITIONALLY, but scenario-engine imported boot.discovery `resolution:=optional` → late crash. Fix: drop optional (mandatory import) + system-export boot.discovery in `withJUnitRunner`. boot-discovery is Private-Package host-flat SOCLE (not a bundle-to-consume) → system-export, not install. Rule: what the code loads unconditionally at runtime must NOT be optional. See [[classrealm-adaptable-pattern]] [[osgi-system-export-resolution-only]].
- [ClassRealm / Adaptable pattern (socle, 2026-07-06)](classrealm-adaptable-pattern.md) — **★ materialize the doc's classloader-realm as a type: `ClassRealm` with `Optional<T> adapt(Class<T>)`, unifying `HostClassRealm` (ex-HostClassLoaderView, flat JCL) + coming `BundleClassRealm` (delegates to Bundle.adapt).** Name = Plexus ClassWorlds sense (classloader-isolated), NOT security; `Class` prefix anchors it. Contract promoted from `ConsultingService.adapt`. Semantics B (adapt what you have, empty otherwise). Migrate ONLY existing `.adapt(Class)` call-sites — invent no faces. Commit `00c2441` (steps 1-2 green). See [[cluster-seed-execution-state]] [[single-source-of-truth-before-logic]].
- [RunMode / LiveGate / Pulumi abstraction (2026-07-06)](runmode-livegate-pulumi-abstraction.md) — **★ Pulumi is IMPLEMENTATION — the domain must not see it.** RunMode = tri-state FACT (STANDALONE|PULUMI_PREVIEW|PULUMI_RUN) at the Pulumi edge; only PULUMI_RUN mutates master; standalone+preview = inert probes + pending, complete runbook, zero mutation. Domain consumes ONE abstract face `LiveGate` (isOpen/through, no com.pulumi in its type), a PROJECTION of RunMode. PendingMarkingExecutor stays behind the gate. Supersedes the committed 2-boolean RunMode (must rework). See [[cluster-seed-transport-consensus]] [[cluster-seed-execution-state]].
- [Domain ports de-seamed → contract (★ CURRENT, OSGi green 2026-07-11, uncommitted→committing)](domain-ports-deseamed-state.md) — **★★ the de-seam sweep.** All 6 domain `-port` → `-contract` (type=seam→contract, system-export dropped, installed bundle-to-bundle), doctor was the template. netplan FUSED its 2 packages into one `netplan.contract`; manifests KEPT 3 (`contract`/`.node`/`.profiles`). cluster+systemd `-bdd` RETROFITTED onto the bbox in-container scion. **Only TWO seams remain in osgi/: `pipeline` (shared Topic-grammar ENGINE — boot + manifests-synthesis use it, de-seams with the BDD migration) and `seed-broker-port` (the membrane, stays).** host/exec poms trimmed to seed-broker ONLY → host Java intentionally broken, deferred to the scenario-rewrite-from-specs chantier (NEXT). Gotchas: closure pulls contract-bundles but needs the FRAGMENT in its seed when only the fragment imports the de-seamed pkg; clean `target~nxmatic` before `-Pnxmatic`. See [[mock-service-substitution-pattern]] [[seed-broker-host-adaptation]].
- [Mock-service substitution pattern (★ RETAINED, shipped 2026-07-11)](mock-service-substitution-pattern.md) — **the test-collaborator pattern that WON.** No standing `@Component` fake; the CALLER `registerService`'s its collaborators into the real registry. Criterion: does it cross the seam? yes → inject it, the standing fake dies. THREE shapes: out-of-container (host-JVM test, mock on the host loader, needs the port system-exported — the PRIOR form, `ClusterReadinessScenarioInContainerTest`); in-container passenger (`FrameworkUtil.getBundle` context, bounded `getServiceReference` await — `HealthSystemContributionTest`); and the **in-container SCION** (the DEFAULT now for a `-bdd`: `{domain}-bdd` bundle with the scenario + `run()` front-door, a `{domain}-bdd-test` FRAGMENT whose passenger registers the mocks on the shared loader then plays the front-door, an `@OsgiWorld` proxy — `bbox`). Scion trap: the `@Test` body must OWN the data sink (jGiven fail-fast leaves stage state unstable). SUPERSEDES [[per-domain-osgi-fakes-chantier]]. `doctor-core-fake` + `dbus-systemd-edge-fake` DELETED from osgi/. See [[seed-broker-host-adaptation]] [[port-edge-domain-ownership]].
- [Per-domain OSGi fakes chantier (CANCELLED for OSGi 2026-07-11)](per-domain-osgi-fakes-chantier.md) — **⚠️ REVERSED — do NOT implement.** Prescribed the opposite of what shipped: a standing `variant=fake` fragment per domain. We killed the standing fakes; the retained pattern is [[mock-service-substitution-pattern]] (caller registerService's). Kept only as the record of the abandoned approach. `dbus-systemd-edge-fake` survived only host-side, now also deleted from osgi/.
- [BDD context-injection carrier (2026-07-07, SHIPPED)](bdd-context-injection-carrier.md) — **★ the target shape for host→stage context, reuse for the other 6 pipelines.** ONE `StageContext` carrier pushed into the jGiven value-DAG via `executor.readScenarioState` (no `*Aware`/`accept*`; scenario = pure composer). Runbook + outputs harvested by inject-the-holder (`AtomicReference` seeded by driver, filled by run). jGiven 2.0.3 facts verified at source: injector map persists, `@AfterScenario` runs on stages ONLY (not the test instance — dead-code trap), `Resolution.NAME` mandatory for erased `Optional`/`AtomicReference` state. See [[bdd-null-hygiene-frontier-rule]] [[jgiven-custom-executor-seam]].
- [BDD null-hygiene frontier rule (FEEDBACK, 2026-07-07)](bdd-null-hygiene-frontier-rule.md) — **★ ZERO null ambiguity in OUR model.** Decorate at the FRONTIER (external nullable API: store.get, awaitService, jGiven model) — `requireNonNull` if required, `Optional` if truly optional — never `!= null`/@Nullable/sentinel inside our code. Shipped fixes: probes/sink→Optional, dropped readinessLogger!=null guards, doctor awaitService→Optional.ifPresent, HOST_FACTS/CONNECTION→requireNonNull. Grep `!= null` after any bdd change; every hit must be a frontier. See [[bdd-context-injection-carrier]] [[collaborative-design-method]].
- [Target over transitional scaffolding (FEEDBACK, 2026-07-07)](target-over-transitional-scaffolding.md) — **★ migration steering:** intermediate steps SECURE the migration; when they COMPLICATE dev instead of securing it, SKIP toward the target. Signal to skip = adding null/Optional/@Nullable/duplication/fakes SOLELY to keep old+new worlds running at once. Concrete: PureStagesTest DELETED (offline full-scenario never runs in target), ResourcesStageTest DEFERRED (don't fabricate a fake BootstrapResult — the real one comes with the pure driver, Task 8). Still don't break the live boot mid-flight. See [[cluster-seed-execution-state]].
- [Collaborative design method (KEEP THIS, 2026-07-06)](collaborative-design-method.md) — **★★ feedback: the reasoning method the user loves and asked to preserve across compaction.** Verify at the source never from memory; figure-first with a recommendation; challenge both premises no default winner; name by anchoring on existing reality (invent nothing); source-vs-projection thinking; honest reserves; don't overbuild (YAGNI, door tooled). Keep collaborating at this depth for design work, even suspending an increment to get a cross-cutting design right. See [[options-always-as-c4-diagrams]] [[single-source-of-truth-before-logic]].
- [Mermaid label constraints (FEEDBACK, 2026-07-09)](mermaid-label-constraints.md) — **★ never nest escaped `\"` inside a quoted Mermaid label** (broke a whiteboard render); use single quotes `'...'` for inner quoting, always wrap special-char labels in `"..."`, `<br/>` not `\n`. Grep the diff for `\"` before saving any Mermaid block. See [[options-always-as-c4-diagrams]].
- [BDD-pipeline migration plan (GO 2026-07-04)](bdd-pipeline-migration-plan.md) — **★ the seeding pipeline → jGiven scenarios on the JUnit launcher; GO reached, 7 concerns PROVEN.** FORWARD SEQUENCE (do NOT reorder): (1) spec-network legibility cleanup FIRST (new model primary, old Topic/State avant-only), then (2) ClusterSeed-vertical migration one pipeline at a time (other 6 coexist on Topic/State). Settled decisions + honesty caveats (E/cross-world proven in isolation, real-Felix wiring = the migration) inside. Authored in design/pre-integration so it survives the branch deletion. See [[spike-bdd-pipeline-reference-tag]].
- [spike BDD-pipeline reference tag](spike-bdd-pipeline-reference-tag.md) — **★ reference.** The POC code (reference impl for the 7 mechanisms) is pinned at git tag `spike/bdd-pipeline-poc` (`7a5c3d68`), retrievable via `git show spike/bdd-pipeline-poc:<path>` after `feature/bdd-pipeline-poc` is deleted. Namespace `spike/`, NOT `wip/`. See [[bdd-pipeline-migration-plan]].
- [ClusterSeed transport consensus (Q1 settled 2026-07-05)](cluster-seed-transport-consensus.md) — **★ increment 2 brainstorm, Q1 CLOSED.** How the run crosses the launcher membrane: the custom `LaunchedPipelineExchange` ThreadLocal is a TEST-ONLY crutch — jGiven's native `ScenarioHolder` (`ThreadLocal<ScenarioBase>`) + `JGivenExtension` already hold the `ReportModel`, so **exit the custom global in prod** (user's call, verified in jGiven 2.0.3 jars). 3 decisions: OUTBOUND = synchronous harvest return `R=SeedRun(runbook,outputs)` (the Future insight); entry scenario owns the model, sub-stages share via native scenario-state; crash-safety = `try{play}finally{render}` in the host driver. Q2 inbound NOW CLOSED → [[cluster-seed-inbound-session-store]] (native JUnit session store, no exchange). Whiteboard `.claude/claude-preview.adoc`. See [[bdd-pipeline-migration-plan]] [[engine-lifecycle-socle-state]].
- [ClusterSeed inbound = native JUnit session store](cluster-seed-inbound-session-store.md) — **★ Q2 inbound CLOSED (2026-07-05, junit-platform 6.0.3 sources read).** Host-facts cross via the NATIVE session store, NOT a custom ThreadLocal/exchange: driver opens `LauncherFactory.openSession()`, does `session.getStore().put(NS, HOST_FACTS, bag)` before `execute()`; `LauncherStoreFacade` makes the run's store a CHILD of the session store, so the scenario reads it up the parent chain. Verified NOT a ThreadLocal (`DefaultLauncherSession.store` = instance field, `NamespacedHierarchicalStore` = `ConcurrentMap`, cross-thread safe). Kills the drafted `HostFactsExchange`. COST = first task: `JUnitLauncherCore.run` must open a `LauncherSession` (today `LauncherFactory.create()` nude) — additive socle change. See [[cluster-seed-transport-consensus]] [[engine-lifecycle-socle-state]].
- [jGiven custom-executor seam (preview injection)](jgiven-custom-executor-seam.md) — **★ how to inject a custom `ScenarioExecutor` ONLY in preview, verified 2026-07-05 (two throwaway tests, green).** RETAINED seam (user's idea): extend `ScenarioTestBase` (NOT `ScenarioTest`), declare `@ExtendWith[our seeder, JGivenExtension]` in order → our `postProcessTestInstance` runs first, reads runMode from the store, and conditionally `getScenario().setExecutor(new PreviewExecutor())` — before jGiven's `injectStages` wires the `@ScenarioStage`s onto it. NOT via `ScenarioHolder` (null at our postProcess — jGiven populates it in ITS postProcess, after ours); the channel is `getScenario()` on the instance. This is P4: `DeferringScenarioExecutor` kills `@NestedSteps` sub-trees (E9), so preview uses the listener-decorate `PreviewExecutor` instead. Abandoned `createScenario()` override had an init-order NPE trap (subclass field null when super-field `scenario=createScenario()` runs). See [[cluster-seed-inbound-session-store]].
- [OsgiConnection service-selector — test picks the fake, stage stays agnostic](osgi-connection-service-selector.md) — **★ SEAM shipped (2026-07-06, compiles).** How a host-agnostic BDD stage resolves a FAKE OSGi service in tests WITHOUT knowing it's a test: version-selection is a property of the CONNECTION (produced by the driver, the sole thing that knows world+variant), NOT the stage nor the staging. `OsgiConnection.serviceSelector()` = optional LDAP fragment ANDed into `awaitService` (`over(ctx,owns,onClose,Optional<String>)`); prod `embedded()` empty→live; test driver `Optional.of("(variant=fake)")`→fake. The service never rides a scenario-state — the CONNECTION does, stage pulls THROUGH it. Fake = standalone SCR fixture BUNDLE (not fragment: 3 ifaces from 3 bundles + ports lack `Service-Component`) with `property="variant=fake"`. `service.ranking` = tooled door for live+fake coexistence. See [[cluster-seed-execution-state]] [[object-graph-navigability-principle]].
- [SCENARIO_STATE_DAG gate (PARKED vision)](scenario-state-dag-gate-closes-migration.md) — **★ NOT built, not current (2026-08-14).** A would-be 6th staging gate: build-time topological check on `@Provided/@ExpectedScenarioState` order (the safety the old `State<I,B>` chain gave for free). Verified ABSENT from code (staging ext ships SPEC_COVERAGE + realm/instance gates only); the ClusterSeed migration is settled WITHOUT it → dormant idea, NOT a pending closing task (the filename is legacy). See [[bdd-pipeline-migration-plan]] [[pattern-gate-coverage-map]] [[build-gates-over-review-reminders]].
- [realm-isolation jsr310 regression — RESOLVED + committed](realm-isolation-jsr310-regression-debug.md) — **✅ FIXED 2026-06-30 (commits ae46278b + 0185b32a), pulumi preview green end-to-end.** The isolation increment broke the deployed exec-jar (`JavaTimeModule not a subtype`). Fix: jsr310 nested in the cdk8s carrier + `Cdk8sApps` TCCL-pin factory; slf4j-api un-staged via a boot-stack bound in `isRealmLibrary`. A 2nd latent host bug surfaced once synthesis ran (`ClasspathTreeCopier` resolved via TCCL not the class loader — fixed). Reproducing test `EmbeddedBundlesBootTest.embeddedSynthesisRunsTheCdk8sSetupPathOnTheStagedJackson` GREEN. NOTE: a SEPARATE pre-existing seed-master BDD test-compile breakage (unmasked by cache-off) is another chantier, not ours. See [[realm-library-isolation-state]] [[reconsider-choices-when-revisiting]].
- [Nesting our own flat module per realm (PATTERN)](nesting-our-own-flat-module-per-realm.md) — **proven 2026-06-30 (world-gateway 2D, `gateway-document-codec`).** Share ONE source of jackson-using logic across host/OSGi WITHOUT crossing the String-only seam: a plain flat-jar module (no Bundle-SymbolicName) shaded flat into the host AND nested into a domain bundle's Bundle-ClassPath (`-includeresource;lib:=true`). First time we nest one of OUR OWN modules, not third-party. No staging-rule change; gates 0/0. See [[realm-library-isolation-state]] [[cdk8s-carrier-flat-jar-pattern]].
- [World-gateway 2D — IN EXECUTION + PIVOT](world-gateway-2d-execution-state.md) — **★ CURRENT (2026-06-30, mid-2D).** Zone-0 DONE (networknt build-tooling, `gateway-document-codec` flat∧nested, SCHEMA_CONCORD gate dormant 0/0; commits to `308da3ad`). A 6-doc structure carto INVALIDATED the ASM concord approach (4 translators read-A-write-B, `intervention` has no FIELD_*, docs nest). USER PIVOT (do NOT revert): records-as-contract per Document, JSON schema GENERATED from the record, automated by scan — fuses the FIELD_* elimination INTO 2D. NEXT = write the records-as-contract DESIGN (plan-mode, user said OK), then re-plan Tasks 4-9. See [[world-gateway-2c-complete-2d-designed-state]] [[nesting-our-own-flat-module-per-realm]] [[eliminate-field-constants-via-schema-binding-backlog]].
- [World-gateway 2E — annotations plan](world-gateway-2e-annotations-plan.md) — **★ NEXT, designed+approved, NOT started.** Type the doctor internal record↔map readers by putting jackson-ANNOTATIONS on the pure domain (zero new realm surface) + decoding rich records directly via codec; deletes 3 *Reader + 6 toOutputMap + 2 fromOutputMap. Big change — compact first. Exact bottom-up plan + gotchas inside.
- [Codec InstantModule → JavaTimeModule](codec-instant-module-should-be-javatimemodule.md) — the hand-rolled `InstantModule` should become jackson's official `JavaTimeModule` (jsr310); the T6 "avoid ServiceLoader" reasoning was flawed since we register EXPLICITLY. Deferred until after T9 commit; gauge with user.
- [World-gateway lost open extensibility — CRITICAL debt](world-gateway-lost-open-extensibility-debt.md) — **★ surfaced 2026-07-05 by the doc-review; user judged it a CRITICAL architecture element.** records-as-contract shipped WITHOUT the per-domain roster (the "multiplexor"): `Coordinate` is a closed central enum + egress still host-side `OutputBuilder`, so a new domain edits the CENTER not the edge. Multiplexor NOT dead — only its FRONTIER moves: roster + `DomainDagMapper`s assemble the DAG **on records OSGi-side**, the record→Document transposition sits at the ONE seam edge. PRIORITY chantier right AFTER the BDD pipelines land (cluster = the N=2 trigger, arrives with the migration). New design done WHEN we get to it; design preserved in world-gateway-spec §"Planned evolution". See [[atlas-before-after-shift-at-merge]].
- [Eliminate FIELD_* via schema binding (HORIZON backlog)](eliminate-field-constants-via-schema-binding-backlog.md) — **user's real typing goal, post-2D.** 2D contracts the `WorldGatewayCatalog.FIELD_*` (SCHEMA_CONCORD checks code-fields == schema-properties, both coexist). The HORIZON: once the 6 schemas exist, codegen a typed record per coordinate OR bind via the codec, so the FIELD_* strings + dispersed `put(String,…)` sites disappear. Needs schemas first → after 2D (likely after the capstone). See [[world-gateway-2c-complete-2d-designed-state]].
- [Realm library isolation](realm-library-isolation-state.md) — increment COMPLETE+gate-green but the deployed jar regressed (see the debug entry ABOVE). 6 tasks: each world loads its own jackson; DUPLICATE_REALM_CLASS exemption derived from the type=seam surface (Task 4 — keep regardless). Spec 3aaa66fb, plan fe1ee277, commits 8bc21cce..caea2d7b. cdk8s confirmed a LEGIT dual-realm case (host builds org.cdk8s.App). See [[reconsider-choices-when-revisiting]] [[external-edges-chantier-handoff]] [[incus-edge-placement-backlog]].

- [designer runbook + compass](designer-runbook-state.md) — **★ live render of the DESIGN state, [[hub:reentrance-northstar]].** A capability sentence IS a JGiven scenario; gates (box colour) derive playable/narrated; the COMPASS reads gaps as "N flips green→blue away". SPEC graved (atlas scenario maps on 5 views + `designer-runbook.adoc`). v0 (DesignProbe) not yet built. See [[unitrepo-design-unification-state]] [[orchestration-purity-benefit]] [[bdd-jgiven-test-strategy]].
- [unitrepo design unification](unitrepo-design-unification-state.md) — **★ DESIGN SPEC GRAVED 2026-06-20/21 (docs/architecture/unit-repository/ + atlas 4th view).** The docrepo-dag unitrepo vision folded INTO rke2lab via the atlas-additivity ritual: it HOLDS (additive), and half ALREADY SHIPPED (resolver in prod, R4 framework, handler-spi). 2 decisions settled (mechanism C dropped → bundles in embedded Felix; handler edge = 2 axes `osgi.extender=unitrepo.handler` + `unitrepo.type` namespace). Born a reusable atlas device: capability-anchoring. NEXT = revise+repatriate the spec. See [[hub:docrepo-dag-state]] [[osgi-runtime-r4-boot-seam-state]].
- [Federated unitrepo p2p design (out-of-band DESIGN 2026-06-27, NOT built)](federated-unitrepo-p2p-design.md) — **★** peer-to-peer federated provisioning design (units CLONE, repository PULLs) over the Document seam at three topologies (same-JVM → separate-JVM-colocated → remote-pod); the durable record of the 2026-06-27 out-of-band brainstorm. Deepest motive = CLASSPATH HYGIENE (REALM_BOUNDARY becomes physical, not lint); RSA federates the CONTROL plane (Repository is a service), units travel by clone/URL never RSA; de-risked via a 3-rung ladder (SIMPLE relaunch-colocated NOW vs RICH persistent-endpoint POC before cluster). ECF = only live/Felix-safe RSA. See [[unitrepo-design-unification-state]] [[osgi-frontier-underpopulated-chantier]] [[fragment-contribution-mediation-model]] [[pipeline-orchestration-osgi-vision]].
- [Pipeline orchestration → OSGi (VISION)](pipeline-orchestration-osgi-vision.md) — **★ user's REAL system model, surfaced by R4.** The decision/orchestration logic is mis-placed host-side; the fluent pipeline DSL should become an OSGi capability (orchestrator = OSGi service; actualisation steps Incus/Pulumi/gRPC stay HOST as ports it calls, DIP). NOT all-logic→OSGi. Sequence AFTER [[cli-osgi-migration-carto]]. See [[osgi-runtime-r4-boot-seam-state]].
- [Orchestration-purity benefit (HORIZON increment)](orchestration-purity-benefit.md) — analysis: making the jgiven scenario engine PURE buys (1) a pure designer-runbook bundle (closes re-entrance), (2) scenarios-as-`UnitResource` → gates become resolution EDGES → the compass is Felix-resolved not hand-counted, (3) decouplability. Cost = separate scenario MODEL (pure) from report ENGINE (host), a refactor. Plan as a SEPARATE increment, AFTER the doctor-model extraction. See [[pipeline-orchestration-osgi-vision]] [[designer-runbook-state]] [[unitrepo-design-unification-state]].
- [Frontier = playability + edges (internal/external)](../../../../docs/architecture/patterns/frontier-playability-model.adoc) — **★ DESIGN GRAVED 2026-06-21 (frontier-playability-model.adoc + glossary).** The host/OSGi frontier sharpened: 3 orthogonal axes (playability / edge / direction); the PORT is the membrane of BOTH worlds (one door). EDGE has 2 species — *external* (crosses the SYSTEM boundary, mutualised `<target>-edge`, 7 inventoried) vs *internal* (a domain's face toward our system). Internal edge is PRESCRIPTIVE: tell = impl-type leakage. manifests/netplan CLEAN, **doctor MISSING** → [[doctor-internal-edge-debt]]. Doctor extraction MERGED (ac2fae1b). See [[system-space-world-universe-glossary]] [[orchestration-purity-benefit]].
- [External-edges chantier — RESUME HERE](external-edges-chantier-handoff.md) — **★ active chantier (2026-06-22).** Replicate the `<target>-edge` template over 6 boundaries. INTEGRATED into design/pre-integration: pulumi-edge (template), osgi-boot-single-source (capability scan), **ssh-to-age-edge** @ 4d99f599. A GREEN increment of **osgi-boot-alignment** squash-merged (60e81998) to give the staging extension a common base — that chantier is still PARKED/open. Port/edge/domain roles graved (`port-edge-domain-ownership.adoc` — domain CALLS / port DECLARES / edge PROVIDES). NEXT = the **osgi staging extension** (own chantier below); osgi-boot-alignment resumes AFTER it (jul→slf4j + exec-jar DS-API gate). THEN dbus-systemd → incus/cluster/host-fs. Integrate to design/pre-integration, NEVER main.
- [dbus-systemd-edge — SHIPPED](dbus-systemd-edge-spec-state.md) — **★ external edge #3 SHIPPED to design/pre-integration (squash merge 2026-06-23).** The first edge BOTH extracted from host AND migrated into OSGi: `DbusSystemdProbe` → `osgi/systemd/dbus-systemd-edge`, a `@Component` over dbus-on-TCP with the 3 dbus-java jars embedded whole (`lib:=true`) so ServiceLoader transport discovery resolves in the bundle classloader (proven by `DbusSystemdEdgeBootTest`). dbus reclassified host→playable (only Pulumi stays host). `SystemdRuntimeProbe`/`SystemdStatusSnapshot` in `systemd-port` (systemd IS a domain — legit asymmetry with pulumi); `DbusTcpSpecialist`→doctor-core; typed `SystemdUnitId`. Durable: spec `docs/architecture/osgi/dbus-systemd-edge-spec.adoc` + the KEYSTONE section of `port-edge-domain-ownership.adoc` (control-node mediates by ports, never internals; domains upward-blind). Cross-edge taxonomy (sonde vs porte) for the remaining edges lives in the file. See [[external-edges-chantier-handoff]] [[dbus-systemd-probe-poll-backlog]] [[felixframeworkextension-renamed-outofcontainer]].
- [cluster-edge — BUILT](cluster-edge-built-state.md) — **★ external edge #4 SHIPPED (2026-06-26), the kubectl contact.** Built the CLASSIC edge way (cluster-core + cluster-port + cluster-edge), NOT the parked fragment-mediation design. `cluster-port` (type=seam): `ClusterReadinessPhase` rapatriated out of doctor-records, `ClusterSchemaRef`, neutral `ControllerRef`, the Option-B `ClusterReadinessContact` (stateless `isApiReady`/`areControllersEffective`). `cluster-edge` = `KubectlClusterContact` @Component, pure ProcessBuilder, **zero embedded jars**. seed-master injects the contact from the registry (mirrors resolveSystemdRuntimeStatus) + thins `ClusterBootstrapReadinessVerifier` (~150 lines of kubectl gone; host keeps gate/poll/policy→ref projection/wait loops/VerificationResult). `EmbeddedBundlesBootTest` 3rd test proves the embedded edge publishes typed. Commits cafa65db/1d8e966b/b07afe8e/42bf7b8b; full reactor green, gates 0 error. See [[external-edges-chantier-handoff]] [[dbus-systemd-edge-spec-state]] [[fragment-contribution-mediation-model]].
- [Controlplane→OSGi migration frame (★ CONVERGED 2026-07-07, brainstorm)](controlplane-to-osgi-migration-frame.md) — **★ the FRAME governing the full controlplane sweep + the 6-pipeline BDD migration.** User goal: migrate ALL host-side `controlplane/` logic into OSGi services the host CALLS; only `pulumi-edge` stays host. Verified: 104 files under controlplane/, exactly 17 import com.pulumi (the edge, forced host by the two-worlds invariant); the 87 Pulumi-free migrate; incus+bbox still have no osgi module. LOAD-BEARING insight (user's): the host↔OSGi frontier is a **birth canal** — the host (Pulumi) always crosses FIRST, calls OSGi via `awaitService` (the "remote façade"), gets flat data back, materialises master; crossed ONCE then SEALED (living master = peer-to-peer OSGi, [[world-gateway-2c-peer-model-design]], not built). One-shot ⇒ the current gateway suffices, zero new machinery; no OSGi→host call exists or is needed. Fat classes (IncusResourceBootstrap: `Output<T>` interlaced) cut EDGE-FIN: flat plan → OSGi service, `Output<T>` wiring stays host (the cluster-edge gesture). This SUPERSEDES the narrower frontier/fakes ordering by absorbing it. Figures in .claude/claude-preview.adoc §HANDOFF. See [[osgi-frontier-underpopulated-chantier]] [[cluster-edge-built-state]] [[per-domain-osgi-fakes-chantier]] [[cluster-seed-execution-state]].
- [OSGi frontier under-populated (★ PREREQUISITE chantier — now the BDD BLOCKER)](osgi-frontier-underpopulated-chantier.md) — **the trou: only a few domains+edges are actually IN embedded Felix; incus, jgiven (prod runbook!), cdk8s-systemd, unitrepo-core + all third-party run FLAT on the host JCL.** ★ 2026-07-07 UPDATE: this became the explicit BLOCKER of the BDD-pipeline migration. ClusterSeed reached a stable plateau (Task 8 `f99e1b7b2`, fluent world deleted, green) and the user SUSPENDED the offline exercise + the other 6 pipelines: you can't play a pipeline fully OFFLINE until each domain it touches has an OSGi bundle to attach a fake to — and **incus has NO module under osgi/** (provisioning still host-side). Forward order: frontier + incus external edge FIRST → [[per-domain-osgi-fakes-chantier]] → resume BDD offline. incus edge is now top hole (above unitrepo/jgiven). See [[external-edges-chantier-handoff]] [[cluster-seed-execution-state]] [[fragment-contribution-mediation-model]] [[bundle-on-jcl-is-wrong-classpath]].
- [DomainDagMultiplexor — two models, one Document contract (DESIGN 2026-06-24)](multiplexor-two-models-design.md) — **★ settled brainstorm, NOT built.** The host↔OSGi seam reframed as TWO world-models (OSGi diagnostic vs host stack) meeting ONLY at a serialized `Document(domain, coordinate, JsonNode)` — never sharing a type. A `DomainDagMultiplexor` (OSGi) mux/demuxes an envelope stream; a host `DomainDagAdapter` mirrors it to Pulumi. **Mechanism = DS (chosen over fragment, user voted B): records stay PRIVATE, the DomainDagMultiplexor imports ONE interface forever.** Kills ~2000 lines of `toOutputMap`/`OUTPUT_KEY`/`*Reader`. Invariant: `grep -ri pulumi osgi/` == 0. Fewer live components than the prior version. DESIGN CLOSED 2026-06-25: 3 specs (multiplexor-spec + **world-boundary-spec** = THE single inter-world door, type-seam+awaitService+DS+sealed-impl + the **ACL concentration check**: 2 falsifiable greps as the increment's quality gate, `toOutputMap` in osgi = 14→0) + atlas; `type=record` first-class category chosen (user) with a build-time purity guard; `DoctorConsultingService` = the open consultation-door knot (5 live call sites). Anchors `9c6ad5e8` (doctor restore) + `929b3aad` (specs). NEXT = incremental build roadmap in the file. See [[fragment-contribution-mediation-model]] [[doctor-internal-edge-debt]].
- [World-gateway Document design (CONVERGED 2026-06-27, NOT built)](world-gateway-document-design.md) — **★ revises [[multiplexor-two-models-design]] after a 5-angle code review found the multiplexor spec covers only 1 of 3 real leaks** (seam SPEAKS records; host REASONS on records — the `Severity.parse()` boot crash; egress). Principle: **ports describe SERVICES not data** — `consult(Symptom,Observation)` becomes `consult(Document)`; everything crosses as a **Document (YAML)** validated by a **JSON Schema per document-type (coordinate), at build+runtime**; **OSGi holds authority + owns all typing**, the host TRANSPORTS (map-B, no doctor type host-side → kills `== Severity.CRITICAL`); hierarchy = domain owns the mapper `@Component`, document-type owns the schema. Boot pipeline re-decomposed **GROUND/GATEWAY/APPLY** but GATEWAY is FRAMED BY the boot-run-close (verified: only 2 of 5 stages cross the seam). **PIVOT 2026-06-27: gate-first, not crash-first** — the missed drift is because the 3 staging gates check EXPORTED surface, none checks the CONSUMER direction; so add a 4th gate `StagingGate.REALM_BOUNDARY` (rename `Gate`→`StagingGate`), an ASM two-realm reachability law (reads method BODIES + the exec's own target/classes, auto-attributes flat=host/seam vs bundle=OSGi-internal), governed **WARN** during migration → **ERROR** at the end; doctor-records stays bundle-only throughout. `production`→`live` rename rides along. spec (world-gateway) + governance spec updated; **Plan 1 WRITTEN** at `wip/plans/2026-06-27-realm-boundary-gate.md` (the gate + 2 renames); Plan 2 (the Document migration) writes FROM the worklist the gate prints. DONE: Plan 1 shipped (REALM_BOUNDARY gate, 0 error/44 warn); Plan 2 DECOMPOSED into 2A/2B/2C/2D — 2A IN EXECUTION, see [[world-gateway-2a-execution-state]]. See [[cdk8s-carrier-flat-jar-pattern]] [[options-always-as-c4-diagrams]].
- [Pipeline↔jgiven separation design (2026-07-01, CONVERGED, mostly NOT built)](pipeline-jgiven-separation-design.md) — **★ RESUME HERE for the reliability arc.** The thread the user named: **non-null → pulumi-outputs → jgiven → pipeline** (each link revealed by fixing the previous at its source); FIX it in REVERSE (pipeline first). Root cause = TWO pipelines tangled (our `FluentTopicRunner` grammar — zero jgiven import — vs jgiven `Scenario`/`Stage`) in 6 seed-master classes. DECIDED: vocabulary reset (`*Stage`→**`*Topic`**, `PipelineState`→**`TopicContext`**(immutable, holds `LiveGate`)+**`TopicOutcomes`**(mutable); `Stage`/`State` reserved to jgiven); two gates (`LiveGate` living + `PulumiOutputRegistry` output); `BootstrapTopic` = the jgiven provider. KEY finding: the jgiven dry-run is DEAD on our path (`startScenario(String)` never triggers it — no public API) → delete the `System.setProperty(JGIVEN_DRY_RUN)`. See [[flatten-at-edge-observation-layer]] [[atlas-reconciliation-2026-07-01]] [[nullaway-jdk25-recipe]].
- [Atlas-first reflex + levelling (2026-07-02)](atlas-first-reflex-and-levelling.md) — **★ WORKING PRINCIPLE: when a problem arises, FIRST reopen the atlas, locate the problem, start THERE** (not in a spec/code). Atlas now split levelled: **L0** = `integration-atlas.adoc` (integration + transverse *value threads* + L1 index, keeps filename so 16 inbound links hold); **L1** = `architecture/atlas/{doctor,config,runtime,unit-repo,manifests,host-pipeline}.adoc`. First value thread graved = *one unit-model, many manifestations (bootstrap↔detached)* — dissolves "Patient = Pulumi stack" vs "detached knows nothing of Pulumi" (both are UNITS; Pulumi = the bootstrap ContentStore). **RESUME DEBT: re-read THIS session's pipeline arc against the atlas — the lapse since June caused re-derivation of the late-June federated unit-repo design.** See [[pipeline-jgiven-separation-design]] [[multiplexor-two-models-design]].
- [Atlas reconciled to codebase (2026-07-01)](atlas-reconciliation-2026-07-01.md) — **★ 33 commits had shipped off-atlas since 06-30.** world-gateway 2D flipped DESIGN→SHIPPED in the atlas (the `DomainDagMultiplexor` is a NAMED ERASURE; records-as-contract shipped instead — spec status fixed too); a **6th Host/control-plane pipeline view** added (the host's core role, was absent — surfaced by the two-gates design + null-safety); governance note+spec corrected 4→6 `StagingGate`s. BACKLOG: `world-gateway-spec.adoc` chapeau still ~multiplexor, not rewritten. See [[flatten-at-edge-observation-layer]] [[world-gateway-2c-complete-2d-designed-state]].
- [World-gateway 2C COMPLETE + 2D DESIGNED (2026-06-30)](world-gateway-2c-complete-2d-designed-state.md) — **★ CURRENT STATE / RESUME HERE.** 2C MIGRATION COMPLETE on feature/cluster-edge: REALM_BOUNDARY worklist 38→0 then **flipped WARN→ERROR** (commit 9cc51d92, build green at ERROR = the static separation lock); 8 commits (35940184 z1 identities→seam · a75f81c8 z2 ObservationView · afcd0c28 z3a append(Document)+CLI Option A · 3fc6f767+9fc49185 z3b peer node · e5b6a788 z4 ClusterSchemaRef · a314ccbc pom-names · f7bc086f test-contract); whole-branch review verdict **READY TO MERGE** (all 5 cross-zone dimensions hold). 2D **DESIGNED + SPEC'D** (commit 50a4419f, `world-gateway-2d-schema-contract-spec.adoc`): JSON Schema per coordinate (6), a 4th `SCHEMA_CONCORD` staging gate (meta-schema validity + FIELD_*↔properties concord via ASM), networknt validator, per-realm DocumentCodec runtime-capable-but-OFF (capstone turns on), schemas in doctor-core/resources; 5 reconciliation decisions settled, flip IN the merge. **NEXT = writing-plans on the 2D spec → execute (subagent-driven) → remote-validation capstone (own brainstorm→spec) → finishing-a-development-branch.** Document.payload STAYS String (carrier/streaming = transport backlog for the capstone). NO 2E. Ledger: .superpowers/sdd/progress.md. See [[document-codec-instance-in-2d-backlog]] [[osgi-layout-shipped-state]].
- [World-gateway 2A SHIPPED + 2B specced/ready (2026-06-27)](world-gateway-2a-execution-state.md) — **★ 2A DONE on feature/cluster-edge (kept, not merged); 2B SPECCED+PLANNED+COMMITTED (4c91a852), ready to EXECUTE.** 2A: the readiness verdict crosses as a structured `Document` (`ReadinessAuthority.assess`, doctor-core @Component owns the typing); `ControlplanePolicy` doctor-free; fixed a Plan-1 gate cold-tree deadlock (`enforceGates` moved after `delegate().execute()`). **Durable pattern:** the in-container proxy tests DERIVE their install closure from the host (`installImportClosureOf` + `withJUnitRunner`, generalizing `BundleIndex.closeOverImports`) — seeded with the running system bundle's exports; seams stay system-exported (a seam exports MULTIPLE packages, list all); a fragment is pulled only if it exports a package the host imports. **2B (next, the RESUME section at the file's bottom):** the consult path crosses as a Document, by zone (shared `ConsultingService` seam FIRST): `consult(Document)→Document` twin of assess (DISTINCT verb), `Generalist` renders narration + diagnosis AsciiDoc OSGi-side (no asciidoctor — markup not HTML), probes emit a checkpoint Document (symptom as slug), `DoctorGraph`→`ConsultationDag`; checkpoint EXTENDED with symptomKind/summary/details. 5 design decisions settled with the user, don't re-litigate. See [[realm-boundary-gate]] [[maven-build-cache-and-staging-verify]] [[felixframeworkextension-renamed-outofcontainer]] [[doctor-graph-vs-dag-vocabulary-backlog]].
- [World-gateway 2C PEER MODEL design (CONVERGED 2026-06-30, brainstorm, not built)](world-gateway-2c-peer-model-design.md) — **★ 2C drives REALM_BOUNDARY worklist 38→0.** The user's load-bearing insight: host and OSGi are PEERS at the same level — host knows the STACK (opaque to OSGi), OSGi knows the DOCTOR (opaque to host), joined ONLY by opaque Documents + shared checkpoint identity; BIDIRECTIONAL, each keeping its responsibilities → makes remote-OSGi (RSA) trivial (the literal sense of world-**gateway**). KEY MECHANISM: the flat `*Reader` mix TWO layers — stack-structural (outputsNamed/version, STAYS host) + doctor-form (rebuild ConsultationReport from its sub-tree, the shape OSGi wrote at 2B egress, STAYS OSGi); split them, only a checkpoint-keyed `List<Document>` crosses. The 3 host-provided ports flip record→Document: `recordFor→MedicalRecord` ⇒ `DocumentJournal.historyOf(checkpoint)→List<Document>`; `append(Intervention)` ⇒ `append(Document)`; `recordForCurrentPatient`+`reviewOpenProblems` ⇒ `reviewDrift(checkpoint):void`. *Reader→doctor-core; SnapshotView/Entry/Source→host-internal (pulumi-edge). VERIFIED on 2 real cases (DriftReview: collapse total, return discarded, writer also leaks; MedicalRecordDump: HOST-PURE transcode, zero OSGi). Patient = stack identity (not medical) → seam like Checkpoint. ROADMAP (4 increments before merge): layout-first → 2C (5 zones) → flip WARN→ERROR → remote-validation capstone. Whiteboard: .claude/claude-preview.adoc. See [[world-gateway-2a-execution-state]] [[checkpoint-identity-to-seam-backlog]] [[osgi-aggregator-layout-spec-state]].
- [Document seam cannot expose jackson JsonNode (ROOT CAUSE — FIXED)](document-seam-cannot-expose-jackson-jsonnode.md) — **★ FIXED 2026-06-28 by Option B.** `Document.payload()` once returned a jackson `JsonNode` (a BUNDLE type) through a FLAT `type=seam` (gateway-port) → two `JsonNode` realms → `LinkageError` in-container. Felix `@FrameworkLog(DEBUG)` proved the wiring. FIX shipped: `Document` is now `record Document(String,String,String)` — payload is a serialized JSON String, gateway-port dropped jackson, each world parses with its OWN jackson (doctor-core gained a DIRECT jackson bundle dep). DoctorCoreInContainerTest 32/32 green, no LinkageError. Still TODO: SEAM_PURITY staging gate (a seam's Import-Package may name only system-exported/JDK/OSGi packages) to freeze the invariant. See [[world-gateway-2a-execution-state]] [[direct-dependency-for-every-import]].
- [World-gateway 2B zone-1 egress knot](world-gateway-2b-zone1-egress-knot.md) — **★ active 2B blocker resolved (2026-06-28).** zone-1's probe `Observation` + consult `ConsultationReport` are BOTH dual-purpose (consult/runbook AND Pulumi egress→`ConsultationReportReader` reconstruction). User constraint: the Pulumi output must KEEP the same info (form may differ) + the medical record must stay reconstructible (the stack IS the record store). Resolved A-struct: the consultation Document carries the rendered strings (narration/diagnosisAdoc) AND the structured plan/observations/expectations in the existing `toOutputMap()` shapes, so readers stay UNCHANGED. Task 2's Document was under-built (strings only) → must be amended OSGi-side first; the probe keeps `Observation` (egress+scenario), only consult-reasoning leaves the host. See [[world-gateway-2a-execution-state]].
- [DocumentCodec instance in 2D (BACKLOG)](document-codec-instance-in-2d-backlog.md) — Option B (2026-06-28) made `Document.payload` a String and REMOVED `Document.newPayload()`; each world now parses/serializes with its own `ObjectMapper` (the dispersed-mapper smell is back, deliberately scoped). In 2D, when payloads carry schema + deterministic ordering, centralize behind an instance `@Component DocumentCodec` (the JSON twin of manifests' `YamlMapper`), threaded into `Generalist` via the DAG builder and to the host via the pipeline context. See [[world-gateway-2a-execution-state]] [[world-gateway-document-design]].
- [Direct dependency for every import (RULE)](direct-dependency-for-every-import.md) — user rule (2026-06-28): every imported class MUST come from a DIRECT Maven dependency, never transitive — even when it compiles. POM-level twin of seam purity. Grep a module's imports against its direct deps when adding/moving an import. See [[document-seam-cannot-expose-jackson-jsonnode]].
- [Transitive import leaks in doctor-core-test (BACKLOG)](transitive-import-leaks-doctor-core-test-backlog.md) — doctor-core-test imports `doctor.records.*` + `doctor.spi.*` but declares neither directly (both transitive via doctor-core/provided); pre-dates Option B. Add direct `provided` deps in a hygiene pass; sweep other `-test` fragments for the same. See [[direct-dependency-for-every-import]]. (Note: 2B Task 4+5+6 added the DIRECT doctor-spi dep to doctor-core-test for ClinicalReasoning — the records leak remains.)
- [Checkpoint identity → seam (BACKLOG)](checkpoint-identity-to-seam-backlog.md) — `Checkpoint` (doctor.records) is a PURE shared-identity enum (slug/scenarioTitle/resourceName, zero record refs), consumed by host BDD/Pulumi AND the doctor. By world-gateway seam logic its home is the seam (gateway-port/world-gateway), not bundle-only doctor.records. Out of 2B consult-path scope (also serves egress + verdict); move in a dedicated identity-to-seam increment. See [[world-gateway-2a-execution-state]].
- [Fragment-contribution mediation model (DESIGN, supersedes keystone)](fragment-contribution-mediation-model.md) — **★ settled 2026-06-23 brainstorm, NOT built.** The KEYSTONE is DEAD → peer-to-peer mediation through the registry; tell apart control of MEANING (distributed) vs TIME (stays in the executable). A domain contributes a FRAGMENT to a host (cluster → doctor-core): the fragment carries a live SCR `@Component` (Mediator, produces the fact) AND a Specialist (reasons), bringing its OWN Import-Package so the host stays blind (fragment-test model promoted to prod). First use case = cluster (cluster-port + cluster-edge + fragment; NO cluster-core). B3 (generic contribution-host) DEFERRED to the 2nd host. ★ OPEN PROOF: Felix SCR activates a fragment-contributed `@Component` (boot test first). Rewrites `port-edge-domain-ownership.adoc` + dbus spec keystone. See [[external-edges-chantier-handoff]] [[dbus-systemd-edge-spec-state]] [[doctor-internal-edge-debt]].
- [FelixFrameworkExtension → OutOfContainerFrameworkExtension](felixframeworkextension-renamed-outofcontainer.md) — the JVM-side OSGi test harness was RENAMED (commit a20d2c7c); name both test-fragment halves by PLACE (out-of-container ↔ in-container). Older memories + wip specs still say the old name; the class is gone. See [[dbus-systemd-edge-spec-state]].
- [dependency:analyze gate (BACKLOG)](dependency-analyze-gate-backlog.md) — automate `dependency:analyze-only` failOnWarning in build-parent as the anti-drift gate. NOT done: reactor has 215 analyze warnings, ~95% structural OSGi false positives (annotation.versioning @ source-retention, runtime-scope deps, substring-installed bundles, test aggregators) needing a calibrated ignore set; holds the measured inventory + real compile-drift candidates. Sibling of the `bundle-test-parent` dedup (327c8c4c). See [[dbus-systemd-edge-spec-state]].
- [osgi-boot-alignment — incr 1 integrated, incr 2 (boot decomposition) integrated](osgi-boot-alignment-state.md) — **TWO increments, both squash-merged into design/pre-integration. (1) squash 60e81998: single-sourced the embedded-OSGi boot (ONE model module `osgi/boot/boot-discovery`; bundles located by what they DECLARE — BSN third-party / embed-capability LDAP ours; filename heuristic GONE), `type=seam` guard, BSN→capability migration. (2) the BOOT DECOMPOSITION — pure `BootPlanner`→`BootPlan` vs effectful `FrameworkLauncher`→`BootedFramework`, `BootPipeline` grammar, `SeedRuntime`+`OsgiRuntime` DELETED, shared `boot-logging`, pax/JCL `optional` scope, unified `closeOverImports`.** jul→slf4j DONE (folded into boot-logging). OPEN: ★ deferred proof (seam Class identity across JCL/BCL). See [[boot-decomposition-state]] [[bundle-on-jcl-is-wrong-classpath]] [[osgi-staging-extension-chantier]].
- [boot decomposition — SHIPPED + integrated](boot-decomposition-state.md) — **★ 2026-06-23.** Boot decomposition: DECISION (`BootPlanner`→`BootPlan`, pure) vs ACT (`FrameworkLauncher`→`BootedFramework`), fluent grammar `BootPipeline` (one `embedded()` entry), `SeedRuntime`+`OsgiRuntime` DELETED, shared `boot-logging`, `HostClassLoaderView`. Then 4c (pax/JCL): the spike participant REPLACED by declarative `optional` scope on the boot-stack in `osgi/runtime`; `BootStackJar` gains groupId + the PASSIVE DS-API trio; `StagingExecutionStrategy` re-resolves the stack BY COORDINATE. 4d: felix.scr import-closure unified into one `BundleIndex.closeOverImports` frame (per-jar action a contributed handler). Spec `docs/architecture/osgi/osgi-boot-decomposition-spec.adoc`. See [[bundle-on-jcl-is-wrong-classpath]] [[osgi-boot-alignment-state]].
- [bundle on JCL = wrong classpath (INVARIANT)](bundle-on-jcl-is-wrong-classpath.md) — **★ user 2026-06-23:** a BCL-destined bundle on the flat JCL is a WRONG classpath, full stop (pax was just the visible case — a 2nd slf4j provider). Corollary: **staged ⟺ off the JCL**. MECHANISM (settled 4c): declarative `optional` scope in the owning module (Maven does not propagate optional → off every consumer's JCL, IDE-safe by native rule); the spike `afterProjectsRead` participant was REJECTED for this — simpler, no code. The staging extension re-resolves the stack by coordinate to stage it anyway. Mirror of [[osgi-system-export-resolution-only]].
- [osgi staging extension — INTEGRATED](osgi-staging-extension-chantier.md) — **★ INTEGRATED into design/pre-integration (squash 50150e56); two-phase build re-verified green on the tip; real `pulumi preview` boots green.** The build-time Maven extension (`maven-embed-staging-ext/staging-extension`, a @Priority(20) MojosExecutionStrategy) single-sources the shade-exclude ↔ META-INF/bundles staging from a CLOSURE over bundle Import-Package; both exec faces now empty seams it fills (9/7/8 bundles derived). DS-API gate CLOSED: SCR_API_PACKAGES deleted from both executors, the trio installed+wired bundle-to-bundle in BOTH topologies via `DiscoveryPolicy` (all/allExcept/only, shared prod+test) + `closeOverImports`. osgi.cmpn split cut at source (BOM exclusion + build-parent bannedDependencies). HANDOFF still OPEN: [[internal-package-pattern-idea]], two-executor unification ([[boot-pipeline-unification-backlog]]), exec/→embed-exec rename. Follows [[osgi-boot-alignment-state]].
- [Test-logging chantier (folded into osgi-boot-alignment; jul→slf4j remains)](test-logging-chantier.md) — test-time OSGi/Felix/SCR + JUL logs visibility. The `FelixFrameworkExtension.locateBundle` substring bug + `@FrameworkLog` felix-level half are DONE in osgi-boot-alignment; what REMAINS is the jul→slf4j bridge (`SLF4JBridgeHandler`+`LevelChangePropagator`) as a Jupiter extension in junit-testkit. KEEP `GrpcChannelNoiseCapture` (opposite intent). See [[seedlog-logback-migration-backlog]] [[osgi-logs-flow-to-host]].
- [Prefer OSGi for a playable edge — 3 reasons](prefer-osgi-edge-three-reasons.md) — explicit frontier + inherited SCR IoC + free fail-fast (mandatory `@Reference`); IoC and fail-fast are two faces of one mechanism. Playability only ENABLES; these are why to MAKE the choice. Surfaced building ssh-to-age-edge. See [[external-edges-chantier-handoff]] [[hub:error-handling-layered-contract]].
- [boot-pipeline unification (BACKLOG)](boot-pipeline-unification-backlog.md) — deferred idea raised in osgi-boot-alignment: after the boot MODEL (`osgi/boot/boot-discovery`), extract the boot EXECUTION too (OsgiRuntime + FelixFrameworkExtension → a shared boot-pipeline beside it). Origin = a trap: testkit→runtime compile dep dragged pax-logging's `org.slf4j` provider, breaking jGiven resolve. Model not executor was the need. NOT in the current chantier. See [[external-edges-chantier-handoff]].
- [Doctor internal-edge (INTEGRATED)](doctor-internal-edge-debt.md) — **★ Placement 2 + tests-full-OSGi INTEGRATED into design/pre-integration 2026-06-21 (squash `4e3e1427`), not yet on main.** doctor-port = TRUE membrane (value vocab + contracts + `DoctorConsultingService`); doctor-core = 8 package-private actors, depends on port; symmetric with manifests/netplan, compiler-enforced. Tests run in-container via `-test` fragments → ReferralReplies cycle dissolved, 5 HOST tests rehomed, doctor-testkit deleted. 2 durable facts: readers = read-edges (→port); **package-private (not bnd) IS the membrane mechanism**. DX: in-container tests are VSCode-clickable DynamicTests (after a warm-up run). See [[jgiven-osgi-testkit-shipped]].
- [OSGi runtime R4 — boot seam](osgi-runtime-r4-boot-seam-state.md) — **★ SHIPPED 2026-06-20, proven by `pulumi preview`.** Felix boots inside seed-master's `Pulumi.run`; host seam reads the registry via `awaitService`. 5 bundles embedded intact under `META-INF/bundles/`; `system.packages.extra` derived from Import-Package. See [[osgi-system-export-resolution-only]] [[osgi-logs-flow-to-host]] [[r4-resolver-service-ification]] [[capn-cert-ownership-incoherence]].
- [capn-cert ownership incoherence (R4 NEXT domino)](capn-cert-ownership-incoherence.md) — `capn-client.crt` read from classpath by an OSGi-world unit but lives in the host-world jar → invisible to the bundle. SETTLE who authenticates (host Stage A vs in-cluster CAPN) then fix on facts (likely host owns it → pass via context). See [[null-arg-is-a-rule-violation]].
- [Bootstrap pipeline contributable (VISION) + pipeline extraction SHIPPED](bootstrap-pipeline-contributable-vision.md) — `pipeline-core` (L1, unifies the 2 duplicated runners) + `SeedRuntime` (L3, collapses the 3 boot copies) SHIPPED. L2 ABANDONED (stages host-coupled). The contributable-pipeline rewrite = NEXT, belongs to [[pipeline-orchestration-osgi-vision]]. See [[cli-osgi-migration-carto]].
- [Java cleanup backlog (LIVE)](java-cleanup-backlog.md) — running TODO for Java/build hygiene. OPEN: `@NonNullByDefault` enforcement, `-Werror` at 0 warns. Add a line when pushed, tick when done. See [[non-null-input-rule]] [[build-verification-gotchas]].
- [Memory synthesis — prune the how (BACKLOG)](memory-synthesis-prune-the-how.md) — **transverse memory-gardening pass**: keep what/why, prune the how (resume-states, WI-done logs, ★ markers). Run on its own. (This index rewrite is a first pass.) See [[migration-branch-no-fallback]].

## OSGi runtime migration — SHIPPED slices (durable record; how lives in git)

- [OSGi runtime migration state](osgi-runtime-migration-state.md) — design phase shipped; spec `wip/specs/2026-06-18-osgi-runtime-migration-design.adoc` (R1–R7 slices, monotone atlas). Impl slices schedulable.
- [R1–R3 SHIPPED — SCR stood up + SPIs declared + intra-bundle @Reference](osgi-runtime-r1-scr-state.md) — felix.scr 2.2.18 + DS API in BOM (R1, anti-cheat on embedded Felix); 10 impls `@Component` (R2); `NodeEnvContributorRegistry` binds 6 contributors via SCR (R3); testkit→`osgi/testkit`. Detail: [[osgi-runtime-r2-declare-spis-state]] [[osgi-runtime-r3-consume-references-state]] [[bnd-annotations-spike-state]].
- [R4 WI-C0 — service-ify the Felix Resolver](r4-resolver-service-ification.md) — **★ DECISION + DONE.** prod consumes the INJECTED `org.osgi.service.resolver.Resolver`; `new ResolverImpl` left to tests (test owns its dep). unitrepo-core imports only the service interface.
- [CLI OSGi migration](cli-osgi-migration-carto.md) — SHIPPED. manifests-cli + netplan-cli boot embedded Felix, read their `-port` from the registry; `netplan.api`→netplan-port (single-exporter); 8 orphaned META-INF/services deleted. See [[osgi-system-export-resolution-only]] [[dual-path-inline-until-r5]].
- [R4 resume-state (SHIPPED pointer)](osgi-runtime-r4-resume-state.md) — R4 shipped 2026-06-20; pointer + live backlog dominoes (CLIs, null-args, ThreadLocal readers, manifests-core scope, worktree provisioning).

## OSGi invariants / decisions / rules (durable what-why)

- [OSGi system-export = resolution only (INVARIANT)](osgi-system-export-resolution-only.md) — **★** a `system.packages.extra` package carries CLASSES only, none of the bundle's OSGi behaviour. Criterion: system-export ⟺ "NOT designed for OSGi". See [[osgi-runtime-r1-scr-state]].
- [cdk8s carrier + flat-jar pattern (SHIPPED 2026-06-26)](cdk8s-carrier-flat-jar-pattern.md) — **★ flat (non-OSGi) jars belong on the BUNDLE side, not host-flat.** Carrier `osgi/manifests/manifests-cdk8s` embeds cdk8s/constructs/jsii/jetbrains-annotations in its `Bundle-ClassPath` (`-includeresource …;lib:=true`) + exports `org.cdk8s`/`software.constructs` → wired bundle-to-bundle (deriveSystemExports drops them from system-packages). Owner-vs-host role inversion: `cdk8s-systemd`→`osgi/systemd/systemd-cdk8s-manifests`, owned by systemd (its unit vocab) but a `Fragment-Host` of the manifests carrier; package moved under the systemd root (anti-split). Commit d5a221fa. See [[refactor-statics-on-touch]] [[manifests-tests-pre-osgi-debt]] [[jgiven-domain-into-pipeline-debt]].
- [fabric8 K8s client wrapped as ONE bundle — PROVISIONAL, owe official-OSGi switch (2026-08-07)](fabric8-osgi-wrap-provisional-debt.md) — **fabric8 7.8.0 is NOT cleanly stageable: core jars (client/api/httpclient) are header-less plain JARs, and the ~21 model jars SPLIT-PACKAGE `io.fabric8.kubernetes.api.builder` (17x) + `model.annotation` (11x) — Felix can't wire it.** The `:bundle` classifier is client-only (still imports the split model) + `Require-Capability osgi.serviceloader.processor` (aries-spifly). So `osgi/runtime/fabric8-wrap` embeds the whole closure `lib:=true` on ONE Bundle-ClassPath (split dissolved, plain first-match) + exports `io.fabric8.*` once; ServiceLoader bypassed via explicit `JdkHttpClientFactory` (no spifly needed — it's ORTHOGONAL). Resolves green (realm-wiring 0 error, zero raw model jars staged). **DEBT: switch back to official OSGi (`kubernetes-client:bundle` + a resolvable model bundle set + aries-spifly, already roadmapped) once fabric8 dedups the model split or ships an aggregate** (watch fabric8io/kubernetes-client #822/#487/#3890/#3774), then delete the wrap. See [[cdk8s-carrier-flat-jar-pattern]] [[serviceloader-specialist-spi]] [[prefer-osgi-edge-three-reasons]].
- [.internal package pattern (IDEA / future chantier)](internal-package-pattern-idea.md) — user's call 2026-06-22: recognise a framework-INTERNAL package by its NAME (`*.internal`) so "internal, never system-exported" is structural, not inferred — distinguishes the DS-API runtime (felix.scr-internal) from a host-shared seam without a special case. Surfaced closing the DS-API gate. Own chantier (audit/rename across osgi/). See [[osgi-system-export-resolution-only]] [[osgi-staging-extension-chantier]].
- [OSGi logs flow to the host (DECISION)](osgi-logs-flow-to-host.md) — **★** host bootstraps OSGi → OSGi logs converge on host Logback via Pax Logging + `StaticLogbackContext=true`. Install pax FIRST. See [[osgi-logging-and-cli-debt]].
- [Migration branches carry NO fallback](migration-branch-no-fallback.md) — **★ PRINCIPLE:** throw away useless code the moment it's in the way; maintain only migration code. See [[dual-path-inline-until-r5]].
- [Dual-path forks inline until R5](dual-path-inline-until-r5.md) — **★ STEER:** host/osgi forks stay inline if/else (flat half retires R5); where no framework-less caller exists, cut to OSGi-only now. See [[osgi-runtime-r4-boot-seam-state]].
- [system/space/world/universe glossary](system-space-world-universe-glossary.md) — **★ settled:** system=the whole; space=layout dir; world=classloader region; universe=RESERVED for the resolver. osgi describes / host actualises / exec materialises. See [[osgi-runtime-r4-boot-seam-state]].
- [OSGi baseline / install discipline](osgi-baseline-install-discipline.md) — per-package @Version + bnd-baseline reconcile with "never install to ~/.m2" (SNAPSHOT work-artifact never installed; baseline jar = a release coord, build-dep of nothing). See [[osgi-package-versioning-carto]].
- [OsgiRuntime decomposition debt — RESOLVED](osgi-runtime-decomposition-debt.md) — RESOLVED 2026-06-23 by the boot-decomposition increment: `OsgiRuntime` DELETED, split into pure `BootPlanner`→`BootPlan` + effectful `FrameworkLauncher`; file-vs-embedded branch became the `BundleLocation` sealed type. The exact export-deriver / bundle-source / start-level-installer split the user hinted at. See [[boot-decomposition-state]].
- [Testkit framework-injection + test-fragment model](osgi-testkit-framework-injection-idea.md) — **★ SHIPPED → [[jgiven-osgi-testkit-shipped]], first client integrated ([[doctor-internal-edge-debt]]).** A fragment = the in-framework twin of package-private; `InContainerJUnitRunner` solved the 3 classloader crossings (TCCL thread, engine-as-Class, BundleWiring discovery). DX: in-container tests are VSCode-clickable DynamicTests (after a warm-up run). Still FUTURE: the Jupiter-injection half (resolve test deps from the booted registry). See [[osgi-system-export-resolution-only]].
- [jGiven-OSGi wrap spike VERDICT (adopted)](jgiven-osgi-wrap-spike-verdict.md) — **★ MEASURED + ADOPTED 2026-06-21.** jGiven wraps as a first-class OSGi bundle, cost LOCAL not viral (deps already all bundles; 1 `bootdelegation=sun.misc` + 1 forced fragment import; no `DynamicImport-Package:*`). Palier 3 ran a Given/When/Then in-container reading a host's package-private field white-box. Refines (not refutes) [[osgi-system-export-resolution-only]]: a 3rd lever = local wrap when a header-less jar must PARTICIPATE in OSGi. Report `docs/architecture/osgi/jgiven-osgi-wrap-spike-report.adoc`. See [[osgi-testkit-framework-injection-idea]].
- [jGiven-OSGi testkit SHIPPED (on main)](jgiven-osgi-testkit-shipped.md) — **★ INTEGRATED to main 2026-06-21 (4ab15104).** The verdict productionised: `osgi/jgiven/` aggregator with `jgiven-wrap` (jGiven as a bundle) + `jgiven-testkit` (shared `jgiven-fragment.bnd` include + `JGivenTestkit.felix()` boot helper, depends on `osgi/testkit`) + `jgiven-probe`/`-probe-test` (host+(-test)-fragment, doubling as the `@Osgi` palier-2/3 guard, green on Felix 7.0.5). `osgi-spike/` + `-Pspike` deleted. Gotcha: plain bnd-maven-plugin has no cross-jar `-include` → reactor-relative `${.}` path. First client = doctor. Handoff `docs/architecture/osgi/jgiven-osgi-testkit-handoff.adoc`. See [[jgiven-osgi-wrap-spike-verdict]] [[doctor-internal-edge-debt]].

## OSGi cleanup / module layout — SHIPPED slices (cartos collapsed; detail in files + git)

- [osgi/ aggregator layout — 3 groups by nature (out-of-band SPEC 2026-06-28, NOT built, post-merge)](osgi-aggregator-layout-spec-state.md) — **★ stop osgi/ mixing 6 natures in one flat `<modules>`.** 3 intermediate groups: `foundation/` (domain-annotations, world-gateway, pipeline), `runtime/` (boot, runtime-host, junit-testkit, bench), `domains/` (métier) + 2 flat parents. RULES: testkit lives WITH its bundle (no `testing/` nature); aggregator iff ≥2 modules. RENAMES: gateway→world-gateway (pkg …world.gateway.port; `GatewayCatalog`→`WorldGatewayCatalog`), runtime→runtime-host, jgiven dissolves INTO pipeline. ssh-to-age-edge stays a flat leaf. **PLAN WRITTEN 2026-06-30** (`wip/plans/2026-06-30-osgi-aggregator-layout.md`): layout-first increment on THIS branch (NOT post-merge, NOT a dedicated worktree — user decided), BEFORE 2C (2C is written on world-gateway names). 6 tasks: relocate domains/foundation/runtime, rename runtime-host + world-gateway. **jgiven dissolution (§5.4) DEFERRED** — it is a realm change not a layout move → [[jgiven-dissolution-is-a-realm-change-backlog]]. See [[layout-skeleton-state]] [[jgiven-domain-into-pipeline-debt]] [[world-gateway-document-design]].
- [osgi/ layout SHIPPED (2026-06-30)](osgi-layout-shipped-state.md) — **★ the layout-first increment landed (commits a05dd52c..a0f64f2a, full reactor + all tests GREEN).** osgi/ is now 3 nature-groups: `foundation/`{domain-annotations, world-gateway, pipeline/{pipeline-port, pipeline-jgiven, pipeline-testkit, pipeline-probe, pipeline-probe-test}}, `runtime/`{boot, launcher, junit-testkit, bench}, `domains/`{6 domains + ssh-to-age-edge}; root module list = 5 entries. Renames: gateway→world-gateway (pkg …world.gateway.port, GatewayCatalog→WorldGatewayCatalog), runtime leaf→launcher (not the spec's runtime-host), jgiven regrouped under pipeline/ (SAFE — no export fusion). CLI selectors: -pl :launcher / :world-gateway / :pipeline-port. PROCESS LESSON: a reviewer finding that contradicts a green build → check COMMITTED state (git show HEAD:path) not the working tree, and git status after every implementer commit (46c7cdf0 committed renames but not content edits; fixed in a0f64f2a). See [[osgi-aggregator-layout-spec-state]] [[jgiven-dissolution-is-a-realm-change-backlog]] [[world-gateway-2c-peer-model-design]].
- [jgiven dissolution is a REALM change, deferred (2026-06-30)](jgiven-dissolution-is-a-realm-change-backlog.md) — **★ found while planning the layout (user: "did we forget jgiven?").** §5.4 "dissolve jgiven into pipeline" was never executed AND is dangerous as written: `pipeline` is `type=seam` (system-exported FLAT); jGiven enters as an INSTALLED bundle (`JGivenTestkit.installFromClasspath` + `installBundles(WRAP_BSN)`). Seam-exporting `com.tngtech.jgiven.*` = two realms = LinkageError (the DUPLICATE_REALM_CLASS condition, the jackson bug again). The layout spec self-contradicts (§5.4/§3/§6 dissolve vs §8 "aucune fusion"). The layout increment relocates jgiven AS-IS to foundation/jgiven/ and defers the dissolution to its own boot/testkit-redesign increment. See [[jgiven-domain-into-pipeline-debt]] [[osgi-aggregator-layout-spec-state]] [[document-seam-cannot-expose-jackson-jsonnode]].
- [API-extraction tri (CARTO)](api-extraction-tri-carto-state.md) — API extraction = 2 moves by WHO consumes: bridge api (host consumes) vs intra-OSGi api. The per-type sort + naming settled; prerequisite to R4. See [[osgi-runtime-r4-boot-seam-state]].
- [Rename contract→port + taxonomy](rename-contract-to-port-state.md) — SHIPPED. The boundary modules went bridge-api→contract→`-port` (saga detail in git + [[extract-bridge-api-state]] / [[rename-bridge-to-contract-state]]). **★ Taxonomy:** name follows NATURE — `-port` (boundary, both worlds depend, package by concern) / `-spi` (impl side) / `-api` (call side). `unitrepo-handler-api`→`-spi` too. See [[osgi-cleanup-slice-state]].
- [OSGi cleanup slice](osgi-cleanup-slice-state.md) — SHIPPED. Re-placed contracts host/→osgi/ beside their impl; bundle-ified them; per-package @Version across osgi/; bnd-baseline on. See [[contract-placement-and-versioning-carto]] [[osgi-package-versioning-carto]].
- [Contract placement + versioning (CARTO)](contract-placement-and-versioning-carto.md) — the contract is a PURE MODEL → belongs in osgi/ as a versioned bundle, consumed as a plain jar by the host (port belongs to the domain, DIP). See the atlas §"two spaces".
- [OSGi package versioning (CARTO)](osgi-package-versioning-carto.md) — each EXPORTED PACKAGE carries its own semver, decoupled from the 0.1.0 GAV; bnd-baseline machine-enforces it. See [[osgi-baseline-install-discipline]].
- [osgi-bench/testkit naming](osgi-bench-testkit-naming-state.md) — SHIPPED. Killed the redundant `osgi-` prefix under `osgi/` (`bench-*`, `testkit`). artifactId==leaf-dir-name.
- [Layout Steps 1–5 SHIPPED — osgi/host/exec spaces built](layout-skeleton-state.md) — `osgi/`+`host/` skeleton + aggregator/parent split (S1); manifests+unitrepo under `osgi/`, deps by GAV (S2); 3 host-only modules → `host/`, seed-master straddles (S3); `osgi/` by domain-dir-iff-≥2 (S4); top-level `exec/` by nature, 2 CLIs + seed-master→`exec/seed-master` (S5). Detail: [[osgi-space-bundles-state]] [[host-space-state]] [[osgi-leaves-state]] [[exec-aggregator-state]].
- [bnd-annotations spike](bnd-annotations-spike-state.md) — SHIPPED. Java annotations = source of truth, bnd generates manifest + Metatype XML; BOM pins the 3 `*.annotations` artifacts. See [[check-osgi-standard-before-modeling]].
- [Step 2 decomposition state](step2-decomposition-state.md) — slice 1 (BootstrapConfig relocate) SHIPPED. 4 OSGi planes + 2 axes + roadmap; Model B rejected. NEXT = config-extender or doctor split. See [[coherence-rules-coordinator]].

## Doctor / health system (the big subsystem)

- [HealthSystem keystone + runbook/record (SHIPPED core)](healthsystem-keystone-state.md) — SHIPPED. Per-run HealthSystem holds registry + GrantPolicy, employs Generalist via ClinicalAccess. Absorbs the access-control brainstorm [[healthsystem-access-control-model]] + [[cohort-correlation-spike]]. Companions SHIPPED: [[runbook-doctor-state]] (Layer-3 write+read enrichment, doctor=app-logic), [[referral-roundtrip-state]] (diagnose→ReferralReply+Assessment), [[medical-record-impl-complete]] (on-demand query API from Pulumi; supersedes [[medical-record-query-api-state]]).
- [Intervention-provenance state](intervention-provenance-state.md) — SHIPPED to main (260 tests). DriftSpecialist + InterventionLedger + StackCoordinate. ONLY OPEN = e2e LIVE proof. See [[hub:specialist-as-ledger-northstar]] [[hub:model-substrate-alignment]].
- [Clinician genus + entity-vs-value detector + exposure gradient](clinician-genus-entity-value-detector.md) — **★ vocabulary made into model (brainstorm 2026-06-25).** Clinician = GENUS (identity + operation model); species = diagnostician (Specialist: assess+prescribe) / remediator (administers). Detector: VALUE projects its own data (smart record, e.g. MedicalRecord), ENTITY enacts a repertoire on collaborators (clinicians, by construction — they carry an id). Port-exposure GRADIENT: HealthSystem crosses now, diagnosticians stay internal forever, Remediator crosses when the pipeline goes OSGi. Found missing: DriftSpecialist (has effect, not yet a Clinician) → reframed as the médecin-conseil, see [[medecin-conseil-efficacy-analyst-design]]. See [[doctor-remediation-model]] [[multiplexor-two-models-design]].
- [Médecin-conseil — efficacy analyst (DESIGN 2026-06-26)](medecin-conseil-efficacy-analyst-design.md) — **★ settled brainstorm, NOT built.** The DriftSpecialist reframed as a PURE-READER efficacy analyst (the Sécu's médecin-conseil): produces a bounded, dated `EfficacyReport` referencing (not copying) the problem consultations + resolved symptoms, KEEPING the "why" the discarded Drift letters threw away. 3 verdicts (effective / ineffective / confounded-declared); the external-change INFERENCE is DROPPED (writes nothing → purity fork dissolved); the unexplained case is a RENVOI to recruitment, not an efficacy verdict. External interventions enter the history by declaration (OPERATOR_MANUAL, built) or observation — never by guess. The GATE ([[efficacy-first-prescription-provisional]]) consumes the report. Maille DEFERRED to real recruitment use-cases. See [[clinician-genus-entity-value-detector]] [[intervention-provenance-state]] [[doctor-remediation-model]] [[practitioners-as-components-design]].
- [Every actor a component — practitioner vs consultation (DESIGN 2026-06-26)](practitioners-as-components-design.md) — **★ settled direction, NOT built.** Correct the system so EVERY doctor actor is a component, not just stateless ones. The one knot: the Generalist holds per-run `ClinicalAccess` (closed over the admitted patient) → can't be a DS singleton. Fix = split the EMPLOYED practitioner (stable `@Component`, identity + operational mode) from the per-run CONSULTATION (a value holding the access). Generalises the `Clinician` genus to carry an operational mode beside `clinicianId()`. Absorbs the Generalist-visibility/practitioner-directory backlog. Fork A/B/C (call-param / DS ComponentFactory / employment-registry, leaning C) DEFERRED to build. See [[clinician-genus-entity-value-detector]] [[medecin-conseil-efficacy-analyst-design]] [[healthsystem-access-control-model]].
- [Specs current at brainstorm/handoff end (FEEDBACK)](specs-current-at-brainstorm-end.md) — **★ discipline:** leave the `docs/` architecture specs up to date at the END of every brainstorm/handoff. A record/component absent from the specs = DRIFT (code-out-of-spec), not a doc omission. Memory notes ≠ the `.adoc` specs (shared, versioned source of truth). Now HARDENED into a build-time gate, see [[spec-coverage-gate-state]]. See [[object-graph-navigability-principle]].
- [Options always as C4/UML diagrams (FEEDBACK)](options-always-as-c4-diagrams.md) — **★ discipline:** whenever I present the user options/forks to choose between, ALWAYS figure them as C4/Mermaid diagrams (in `.claude/claude-preview.adoc`), not prose tables — the user reflects best on diagrams. Applies to every option-set, not only problem framing. Diagrams then graduate into the specs. See [[specs-current-at-brainstorm-end]].
- [Staging-gates governance — DONE, UNCOMMITTED (2026-06-26)](spec-coverage-gate-state.md) — **★ THREE build-time laws in the staging-extension** (RECORD_PURITY, SPEC_COVERAGE, INSTANCE_DISCIPLINE), read from bytecode via ASM, each governable per-bundle by `@GovernedBy(Gate, EnforcementLevel)` on a package-info (default ERROR=locked, WARN=visible backlog, IGNORE=infra). Single-element exemptions via `@Exempt(Gate, reason)`. fail-AT-end + per-gate summary. Annotations in `osgi/domain-annotations`. Spec at docs/architecture/osgi/staging-gates-governance-spec.adoc. Build GREEN, all debt at WARN; remaining = the commit. See [[specs-current-at-brainstorm-end]] [[options-always-as-c4-diagrams]] [[build-gates-over-review-reminders]] [[object-graph-navigability-principle]].
- [Build gates over review reminders (FEEDBACK)](build-gates-over-review-reminders.md) — **★ discipline:** a rule the user keeps re-asking for (instance-passing, specs-current, record-purity) becomes a BUILD-TIME gate, not a convention — seen at every build, and once the debt is cleared the default-ERROR level locks it. Level is graduated `@GovernedBy(Gate, EnforcementLevel)` default ERROR; new laws are instances on `ResolvedBundle`, built in Java with a test. See [[object-graph-navigability-principle]] [[spec-coverage-gate-state]] [[refactor-statics-on-touch]].
- [Refactor statics on touch (FEEDBACK)](refactor-statics-on-touch.md) — **★ boy-scout rule:** when a change *references* a class that still has a non-factory `public static` method, refactor that static into an instance in the SAME change — the moment you touch it the context is loaded (you see its role/callers/data), so the move is cheap; deferred, the WARN backlog only stops growing, never shrinks. A static inside a DAG = a design defect (missing edge). The owner may be a thread-local-carried context or one of several nodes in scope. See [[build-gates-over-review-reminders]] [[object-graph-navigability-principle]] [[manifests-tests-pre-osgi-debt]].
- [Tidy-on-drift widens scope (FEEDBACK)](tidy-on-drift-widens-scope.md) — **★ the user ALWAYS widens a work session to tidy-to-convened-patterns when they notice a drift**, so a diff wider than the plan (e.g. socle aac0518de: 129 files — FQN→imports ×42, property rename+hoist, System.out→logger, module renames) is EXPECTED, not scope creep to flag. Report it as "cleanup convened as patterns"; still verify it's green + surface what was swept; a widening toward a CONVENED pattern is welcome, an unconvened new abstraction is not (ask if unsure). See [[refactor-statics-on-touch]] [[sequential-no-compat-workflow]].
- [Atlas before/after shift at merge (FEEDBACK)](atlas-before-after-shift-at-merge.md) — **★ docs describe the current codebase + genuine future; NOT work-history.** Two rules: (1) an atlas before/after SHIFTS forward once the "before" is realized — collapse the consumed transition to current-state, reserve before/after for the still-LIVE next evolution; (2) this cleanup belongs at EACH merge (a merge's DoD includes the doc shift + stripping increment/SHIPPED/2A-2D scaffolding), not as a retroactive sweep. VIGILANCE: before removing work-history, ask "does this abandon a property/design someone judged critical?" — the 2026-07-05 review surfaced the [[world-gateway-lost-open-extensibility-debt]] exactly this way. See [[tidy-on-drift-widens-scope]] [[docs-diagrams-not-java]].
- [Manifests tests pre-OSGI debt — DONE 2026-06-26](manifests-tests-pre-osgi-debt.md) — **✓ migrated:** built `manifests-core-test` (in-container fragment mirroring doctor-core-test); `DefaultManifestExplodeServiceTest` now acquires `ManifestExplodeService`+`YamlMapper` from the registry (no `new Default…()`), 6/6 green in Felix; old POJO test deleted. The 5 pure-API tests legitimately stay flat-classpath POJO. Doubled as the runtime proof of the cdk8s carrier wiring. See [[cdk8s-carrier-flat-jar-pattern]] [[derive-incontainer-graph-from-imports-backlog]] [[object-graph-navigability-principle]].
- [Doctor live-record roadmap + open design](doctor-live-record-roadmap.md) — step1 + access-control SHIPPED; remaining = step2 remediation/Referral ([[doctor-remediation-model]], Remediator = the "hands"). PARKED design: [[preview-whatif-topic]] (preview replays BDD on observed⊕hypothesis), [[serviceloader-specialist-spi]] (roster = extension point → federated OSGi), [[efficacy-first-prescription-provisional]] (per-symptom gate settled, pick provisional), [[seeded-history-automation-api]] (seed/readonly-preview test techniques).

## Infra / manifests / config

- [★ NixOS node substrate (CURRENT chantier)](nixos-node-substrate-state.md) — **the post-June trunk: every RKE2 node = same NixOS Incus image (nixos-generators), role is config not OS; cloud-init + distrobuilder + /srv/host systemd all collapse.** Base = `feature/cluster-seed-scenario` (not main). Substrate MATURE (nix build, devlxd identity, deterministic PKI, rendered-branch Flux). GAP = no management cluster to grow workloads; node NotReady (`cni="none"`, cilium only an rke2 addon override). Baremetal nikopol→bioskop. NEXT = boucler le substrat (cilium/CNI Ready + validate the Flux loop). See [[cluster-seed-execution-state]] [[master-execution-stage-missing-state]] [[gitops-cluster-api-transition-plan]].
- [OSGi-idiom debt: logging + shadowed services](osgi-logging-and-cli-debt.md) — TRIAGED: no standalone debt remains. Log→LogService via Pax (R4/R6); ServiceLoader→DS = the runtime trunk; the rk2lab typo is dead. See [[check-osgi-standard-before-modeling]].
- [Coherence-rules coordinator](coherence-rules-coordinator.md) — walker-retirement: cross-domain rule = explicit pure check that REPORTS (option B), not silent Felix prune; `resolve()` = single coherence gate. CoherenceRule interface deferred to rule-of-three.
- [Config restructuring state](config-restructuring-state.md) — Inc1 (Rke2labConfig DTO + InfraDomain enum) MERGED; Inc2 (doctor remediation) waits on doctor work.
- [Manifests doc consolidation](manifests-doc-consolidation.md) — DONE: hub manifests-architecture.adoc + 4 companions.
- [Terminology refactor state](terminology-refactor-state.md) — manifests renames done; BootstrapPhase + DomainRegistrars pending.
- [Manifest registrars enum refactor](manifest-registrars-enum-refactor.md) — NEXT-TOPIC: 11 registrars → enum; defer (touches dormant buildDomainRegistry).
- [Refactor pipeline candidates](refactor-pipeline-candidates.md) — 9 methods to refactor (fluent grammar/builder/record); start with synthesizeInContext.
- [Package-private sweep](package-private-sweep.md) — remove non-essential private; exemplar = DefaultManifestSynthesisService.
- [Domain registry abstraction](domain-registry-abstraction.md) — DEFERRED; unify Manifest+Infra registry pairs at rule-of-three.
- [seed-vcluster](seed-vcluster.md) — next chantier: bootstrap vCluster gitops-mgmt + Flux; needs vcluster operator unit + pulumi-command.
- [GitOps + Cluster API transition plan (re-homed design record)](gitops-cluster-api-transition-plan.md) — **STALE (2026-06):** peer1+ provisioning via seed-peers + GitOps + CAPI + Tekton, over distrobuilder + cloud-init + `/srv/host` + `nxmatic/rke2lab` — nearly all superseded/deleted by [[nixos-node-substrate-state]]. Read as history; the live delivery model is now rendered-branch Flux.

## Rules / patterns / gotchas

- [instance methods, not static helpers (★ FEEDBACK, 2026-08-29)](instance-methods-not-static-helpers.md) — **I keep defaulting to `private static` helpers; user wants behavior as instance/member methods on the owning DAG pojo, converted uniformly.** Pure leaf utils + factories stay static. "difficile de te policer à ce propos". See [[flox-env-migration-design]].

- [Non-null input rule](non-null-input-rule.md) — **rule:** never accept a nullable input; represent optionality at the boundary. Machine-enforce via `@NonNullByDefault` ([[java-cleanup-backlog]]).
- [A null arg to our API is a rule violation](null-arg-is-a-rule-violation.md) — **feedback:** a null literal to OUR API = a violation (exposed the OSGi framework-bypass). Fail-fast on locally-verifiable conditions. See [[non-null-input-rule]].
- [Synth-context channel rule](synth-context-channel-rule.md) — **rule:** the channel a synthesis dep uses = an objective test (does the ThreadLocal ownership invariant hold for all readers?). Data→ThreadLocal; service→@Reference. See [[capn-cert-ownership-incoherence]].
- [Object-graph navigability — THE principle](object-graph-navigability-principle.md) — **★ feedback (load-bearing):** the single invariant behind the user's design instincts + their hérissement at statics — everything must be navigable in the object graph; a static is an ORPHAN node (reachable by name, unreachable by walking references). Explains DS-over-fragment, .internal-sealed-but-navigable, path-addressing, factory-as-instance, records-private. See [[prefer-non-static-inner-keep-the-graph]] [[multiplexor-two-models-design]].
- [Prefer non-static inner — keep the graph](prefer-non-static-inner-keep-the-graph.md) — **preference:** default to non-static inner so the graph stays navigable; `static` only to deliberately cut it.
- [Every module has a description](every-module-has-a-description.md) — **feedback:** give every pom its own `<description>` (bnd folds it into Bundle-Description); not `-removeheaders`.
- [~/.m2 SNAPSHOT masking is CRITICAL](m2-snapshot-masking-is-critical.md) — **feedback (scar):** a stale project SNAPSHOT in ~/.m2 = a green build resolving a frozen jar = a LIE. The answer is a guardrail (no-snapshot-install enforcer), not persuasion. See [[osgi-baseline-install-discipline]].
- [Build verification gotchas](build-verification-gotchas.md) — green build LIES (.mvn forces -DskipTests, build-cache replays, ECJ poison). Reliable: `clean package -pl :seed-master -am -Dmaven.build.cache.skipCache=true -DskipTests=false`; count surefire reports.
- [Single source of truth before logic](single-source-of-truth-before-logic.md) — **feedback:** read a fact from who DEFINES it; if no API, provide one — never transcribe a computed fact. See [[no-system-out-use-logger]].
- [No System.out — use the logger](no-system-out-use-logger.md) — **feedback:** never System.out/err, even for debug; use LogService/slf4j at trace/debug. See [[osgi-logging-and-cli-debt]].
- [AskUserQuestion: mark (Recommended)](ask-question-mark-recommended.md) — **feedback:** lead with the recommended option FIRST + `(Recommended)` + my reasoning; the user uses my opinion as a foil. See [[standing-autonomy-except-runtime-config]].
- [No code-as-explanation in docs](no-code-as-explanation-in-docs.md) — **feedback:** architecture docs explain with C4/UML figures + prose on the *why*, NOT Java/shell code walls ("le java on sait coder"); name+link symbols instead of inlining bodies; retire code walls (usually stale) when touching a doc. See [[spec-figure-first-reading-loop]] [[c4-diagrams-flowchart-not-native-dsl]].
- [Say "vivant", not "prod"](say-vivant-not-prod.md) — **feedback:** the running/deployed system is "vivant" (FR) or "live", never "prod"/"production". The non-test path = "le boot vivant".
- [Reconsider choices when revisiting](reconsider-choices-when-revisiting.md) — **feedback:** revisiting committed code → re-question the original choice; don't keep it on inertia or because an error symptom (a ClassCast/LinkageError) once drove it. Verify at source, bring a fork. Caught: cdk8s host-flat IS legit (IncusResourceBootstrap builds org.cdk8s.App), not a scope leftover. See [[verify-state-before-labeling]] [[single-source-of-truth-before-logic]].
- [Standing autonomy except runtime config](standing-autonomy-except-runtime-config.md) — **feedback:** act without asking except runtime config (pulumi/kubectl/incus/nix; settings.json) + the integration merge. Don't over-use AskUserQuestion; don't strip accrued grants.
- [rke2lab solo — no PR, merge direct](rke2lab-solo-no-pr-merge-direct.md) — **feedback:** solo repo, no PRs; integrate by direct rebase+ff to origin/main, then remove worktree + delete branch.
- [squash-merge from the target's worktree](merge-from-target-worktree.md) — **feedback:** squash merge (one commit); Claude does it from the TARGET worktree, never the sub-branch's own session (can't saw off the branch it sits on). See [[standing-autonomy-except-runtime-config]].
- [Verify state before labeling](verify-state-before-labeling.md) — **feedback:** never label a chantier DONE/IN-PROGRESS/blocked from a surface signal (a grep, a file's presence) — read what the code does + run the objective gate (build green). Caught calling osgi-boot-single-source 'IN PROGRESS' from a grep; it was done. Comms-twin of [[single-source-of-truth-before-logic]] / [[model-substrate-alignment]]. See [[build-verification-gotchas]].
- [BDD/JGiven test strategy](bdd-jgiven-test-strategy.md) — tests = living docs; JGiven BDD on real use-cases; DSL-first prototype.
- [Test tag taxonomy by zone](test-tag-taxonomy-by-zone.md) — SHIPPED. 4 orthogonal JUnit5 tags (host/osgi/live/spike); default excludes `live | spike`; suffix `*LiveTest`/`*SpikeTest`.
- [DSL unification topic](dsl-unification-topic.md) — PARKED: one engine (JGiven), two layers. NEXT = prototype PreflightScenario.
- [Builder for multisite constructor](builder-for-multisite-constructor.md) — builder pattern note.
- [Validate at the boundary](validate-at-the-boundary.md) — validation discipline (ref by intervention-provenance).
- [Master provisioning state](master-provisioning-state.md) — live-probe contract FIXED+PROVEN vs real master (branch fix/systemd-live-probe-contract, not merged). PARKED. Seed = operator out-of-band intervention → drift specialist source.

## Memory / workspace mechanics (rke2lab-local; cross-cutting in hub)

- [Claude memory cascade state](claude-memory-cascade-state.md) — reference: 3-tier cascade worktree→hub→home + clean-split. **★ DEFECT RESOLVED (2026-08-14):** memory is pinned per-worktree via `autoMemoryDirectory` (absolute path in settings.local.json) — no slug, no symlink; `link-memory.sh` deleted. See [[hub:claude-auto-memory-mechanics]].
- [JDT.LS heap in generated workspaces](jdtls-heap-workspace-generation.md) — **★ DEFECT+FIX:** folder-scoped `.vscode` -Xmx wins over `.code-workspace` → effective heap was the lower; set both ≥8G in lock-step. See [[claude-memory-cascade-state]].
- [Worktree-provisioning handoff](worktree-provisioning-handoff.md) — **★ HANDOFF:** 3 worktree provisioning gaps fixed manually, must be automated (worktree:dir repoint; sops re-smudge; per-worktree Pulumi backend, stack named after branch slug). See [[sops-worktree-smudge-noise]] [[pulumi-stack-per-worktree-backlog]].
- [Pulumi stack per worktree (BACKLOG)](pulumi-stack-per-worktree-backlog.md) — flox PULUMI_BACKEND_URL project-relative → each worktree empty state; real dev only in main.
- [Sops worktree re-smudge](sops-worktree-smudge-noise.md) — `git worktree add` leaves sops files ENCRYPTED; FIX: `rm <files> && git checkout -- <them>`.
- [Handoff prompt opens on Progress narration](handoff-prompt-opens-on-progress-narration.md) — **★ feedback:** every next-conversation/next-workspace handoff prompt must OPEN with the obligation to read `.claude/hub/instructions.md` § "Progress narration" and apply it all session (auto-loaded ≠ heeded). See [[external-edges-chantier-handoff]].

## Backlogs / misc

- [flake-commons cluster dedup (FOLLOW-UP chantier, raised 2026-09-03)](flake-commons-cluster-dedup-chantier.md) — **★ the ~12k-node flake.lock explosion.** nix-flake-commons aggregates third-party flakes (determinate/zen-browser/socket-vmnet/ripvcs/incus-compose) that EACH drag a mutually-recursive nix↔devenv↔cachix cluster, un-deduped. PRE-EXISTING (not the tailnet work; ndh bump = +1 node). Deploy NOT broken (cold git-13GB refetch looked like a hang). Fix is UPSTREAM in flake-commons (some pullers used by ndh → no blanket rke2lab cut). Partial down payment `a08caedaf` (cut flake-commons' direct cachix/devenv/nix, −27 nodes). See [[tailnet-prune-on-incus-renewal]].
- **unitrepo-embed gap (BACKLOG, raised 2026-06-23)** — audit of all `osgi/` bundles for the `io.seedmatic.rke2lab.embed` capability: `unitrepo-core` + `unitrepo-handler-spi` are runtime OSGi bundles but carry NO embed capability and are NOT staged under `META-INF/bundles/` (nor imported by seed-master) — they are not in the runtime boot flow at all yet (the unitrepo chantier is still design, not wired). When unitrepo lands, give them `embed` like the other domains. `manifests-cdk8s` (the cdk8s carrier) carries `type=model; model=manifests-cdk8s` and its `systemd-cdk8s-manifests` fragment rides it ([[cdk8s-carrier-flat-jar-pattern]]); `jgiven-wrap`/`jgiven-probe`/`junit-testkit` (test world) legitimately stay non-embed. Separate from cluster/doctor. See [[unitrepo-design-unification-state]] [[fragment-contribution-mediation-model]].
- [System-exports seam gate (BACKLOG, raised 2026-06-26)](system-exports-seam-gate-backlog.md) — **★ user: "la prochaine build-gate, ce sont les framework system exports."** A 4th staging law: assert every `FRAMEWORK_SYSTEMPACKAGES_EXTRA` package is a legit `type=seam`, never a domain concern that should wire bundle-to-bundle — the build-time guardian of the "two worlds separate" rule we applied by hand for cdk8s. `deriveSystemExports` already has the seam-guard logic; the gate makes it a checked count. See [[cdk8s-carrier-flat-jar-pattern]] [[osgi-system-export-resolution-only]] [[build-gates-over-review-reminders]].
- [INSTANCE_DISCIPLINE gate misses host space (BACKLOG, raised 2026-06-26)](instance-discipline-gate-misses-host-backlog.md) — the staging gates read only `osgi/` embed-capability bundles, so the INSTANCE_DISCIPLINE count is blind to `exec/seed-master` (host). It said "2 statics left" while seed-master had ~25 more FluentTopicRunner.runDuring sites — only the compiler caught them. Fix: extend the ASM scan to host jars (or document the count is osgi/-only). The [[verify-state-before-labeling]] failure mode, mechanised. See [[build-gates-over-review-reminders]] [[refactor-statics-on-touch]].
- [Pattern → gate coverage map (ANALYSIS, raised 2026-06-27)](pattern-gate-coverage-map.md) — **★ which documented patterns are gated vs prose-only (regression risk).** A gate is an anti-regression RATCHET, so ALREADY-clean-but-ungated patterns are the real risk. Maps the whole `docs/architecture/` tree: GATED-ERROR (3 staging + enforcer/spotless), PLANNED (3 memos exist), and the NOT-yet-backlogged candidates — **ManifestDomainCatalog "never hardcode domain IDs"** (the clusterApi bug, fixed but unlocked), config single-reader, and a frontier-invariant family scattered across doctor/unit-repo that **REALM_BOUNDARY generalizes**. Pose after REALM_BOUNDARY flips ERROR (end of Plan 2), same engine + WARN→ERROR ritual. See [[realm-boundary-gate]] [[build-gates-over-review-reminders]].
- [doctor Graph → doctor DAG vocabulary (BACKLOG, raised 2026-06-27)](doctor-graph-vs-dag-vocabulary-backlog.md) — the doctor's structure is a **DAG** everywhere it's discussed (runbook DAG, shared DAG, the new `DomainDag*` types) but the one type says **Graph** (`DoctorGraph`). Reconcile to doctor-DAG vocabulary — nuance: `DoctorGraph` is the apparatus that PRODUCES the DAG, so it becomes the *doctor DAG producer*, not `DoctorDag`. Fold into **Plan 2** (the Document migration introduces `DomainDag*` into doctor), against the same `REALM_BOUNDARY` WARN worklist. Unitrepo `ManifestsVisitOrder` graph is a different domain, out of scope. See [[world-gateway-document-design]] [[realm-boundary-gate]].
- [Derive in-container test graph from imports (BACKLOG, raised 2026-06-26)](derive-incontainer-graph-from-imports-backlog.md) — the `-test` probes hand-list their host's whole runtime bundle graph (the `GRAPH` list in ManifestsCoreInContainerTest; doctor too). It should be DERIVED by closing over the host's `Import-Package` via the shared `BundleIndex.closeOverImports` the BootPlanner already drives — install-without-start the closure, resolve as one set. Sibling of [[dependency-analyze-gate-backlog]]. See [[manifests-tests-pre-osgi-debt]] [[bundle-on-jcl-is-wrong-classpath]].
- [jgiven domain → pipeline (BACKLOG, raised 2026-06-26)](jgiven-domain-into-pipeline-debt.md) — the `osgi/jgiven/` aggregator (jgiven-wrap/jgiven-testkit) should DISAPPEAR — `pipeline` is the natural owner of the jGiven wrapping (the fluent grammar IS scenario-expression; "c'est lui qui wrap jgiven dans les faits"), same shape as manifests owning the cdk8s carrier. Future migration, not scheduled; mirror the cdk8s move. See [[cdk8s-carrier-flat-jar-pattern]] [[jgiven-osgi-testkit-shipped]] [[pipeline-orchestration-osgi-vision]].
- [SeedLog→logback migration (BACKLOG)](seedlog-logback-migration-backlog.md) — host-space: SeedLog uses JUL + io.grpc-via-JUL with no jul→slf4j bridge → gRPC noise raw in tests; debruited via a JUnit5 JUL extension now, unify on logback later.
- [D-Bus systemd probe poll (BACKLOG)](dbus-systemd-probe-poll-backlog.md) — host-space: the readiness gates POLL `DbusSystemdProbe` re-opening a connection each tick; systemd emits D-Bus SIGNALS → a persistent connection + subscription = the poll→listener fix. Proves under `-Plive`. See [[osgi-runtime-r1-scr-state]].
- [OSGi tests in VSCode — three ways](osgi-test-in-vscode-three-ways.md) — how to run the `@OsgiSpike` embedded-Felix tests from VSCode Test Explorer / surefire.
- [Maven build cache + staging verify](maven-build-cache-and-staging-verify.md) — canonical build: `./mvnw -Dtest=… package -Pall-worlds -Dmaven.build.cache.skipCache=true -DskipTests=false`. SKIP the cache (`skipCache=true`), never DISABLE it (`enabled=false`). Verify `maven-embed-staging-ext` changes with a full reactor `package` (it reads dep jars from disk) — `generate-resources -am` can't, siblings aren't packaged. See [[cdk8s-carrier-flat-jar-pattern]].
- [NullAway on JDK 25 recipe](nullaway-jdk25-recipe.md) — the working error-prone **2.50.0** + NullAway **0.13.7** + jspecify **1.0.0** pair + javac flags. Gate is now **ON BY DEFAULT** (activation `!nullaway.skip`, severity `${nullaway.severity}`=ERROR, override `-Dnullaway.severity=WARN` / `-Dnullaway.skip`). OSGi+host+boot prod = 0 (committed). **exec IN PROGRESS + PAUSED** on a design decision; uncommitted @Nullable await review. MEASURE with `clean` or the cache hides modules. See [[flatten-at-edge-observation-layer]] [[world-gateway-2e-annotations-plan]].
- [Flatten-at-edge observation layer (DESIGN, to brainstorm)](flatten-at-edge-observation-layer.md) — missing internal layer: `snapshot()` flattens a typed `SystemdStatusSnapshot` record to `Map<String,Object>` too early (at production), forcing the watcher to re-parse with helpers + nullable Object. Fix: stay structured IN, flatten to Map ONLY at the Pulumi-output edge. Paused exec null-safety to settle this. See [[nullaway-jdk25-recipe]].
- [Null-safety: Optional from source to resolver](null-safety-optional-from-source-to-resolver.md) — genuinely-optional in OUR API → `Optional<T>`, never `@Nullable`. Wrap the foreign null at its frontier (`Optional.ofNullable`), carry the Optional to the maillon that resolves the empty case, delete dead null-carriers. `@Nullable` is last resort, submitted to the user one by one. See [[null-safety-set-once-fields-monotonic]] [[nullaway-jdk25-recipe]].
- [Null-safety: set-once fields → @MonotonicNonNull](null-safety-set-once-fields-monotonic.md) — builder fields + lazy caches use `@MonotonicNonNull` (checker-qual 4.2.1, pinned BOM, provided) + a `requireNonNull` guard at read, NOT `@Nullable` (lies) nor a home-grown excluded annotation (blinds premature reads). `@ScenarioStage` also added to NullAway `ExcludedFieldAnnotations`. Chosen + validated 2026-07-02. See [[null-safety-optional-from-source-to-resolver]].
- [Pipeline state per-topic I/O refactor (DESIGN CONVERGED + materialization STARTED, 2026-07-03)](pipeline-state-per-topic-io-refactor-backlog.md) — generic `State<I,O>` per topic (each declares narrow input record I + output record O; transition folds O_n into I_{n+1}, compile-time flux). Ambient/transverse goes through a generalized `PipelineContext` registry (Class→record, lifted from `IncusResourceBootstrap.ContextRegistry`), **read/written ONLY when a transition builds an input → each topic's input stays DETERMINISTIC** (`Topic.run(I)`, no context param). Backref chain REJECTED. Census of all 7 pipelines done; the OTHER generalizable logic = 3 ad-hoc ambient contexts (ContextRegistry/ManifestSynthesisContext/SystemdSynthesisContext) unified by PipelineContext. SystemdInfrastructureSynthesizer retrofit + FrameworkLaunchPipeline 3-topic materialization both DECIDED-IN. Whiteboard: `.claude/claude-preview.adoc`. See [[null-safety-set-once-fields-monotonic]] [[options-always-as-c4-diagrams]].
- [Engine-lifecycle socle — ALL 7 TASKS DONE + renamed](engine-lifecycle-socle-state.md) — increment 1 of BDD-as-engine on feature/engine-lifecycle-socle: carves osgi/runtime/scenario-engine (machinery moved out of junit-testkit), JUnitLauncherCore, @OsgiWorld/@Pipeline, OsgiConnection (contract over context()+close(), NOT BootedFramework), StartLevelLever, @IsolatedWorld/@SeedRuntime, and T7 BulletproofPipeline+SoclePipelineTest (launcher plays a connected scenario → ReportModel, the increment-2 go/no-go). Realm/seam split + Optional-at-boundary + bnd.bnd cache fix. Module renames DONE: launcher→framework-launcher (pkg .runtime.framework), engine→scenario-engine (pkg .runtime.scenario.engine). Only DEFERRED left: [[runbook-fs-location-relocation]]. See [[options-always-as-c4-diagrams]] [[maven-build-cache-and-staging-verify]].
- [Runbook FS location (ON WATCH, no action)](runbook-fs-location-relocation.md) — a runbook once landed outside target/; investigated 2026-07-05, NO reproducible symptom (runtime render writes explicit seed-master/target/runbook; standalone play writes nothing; JUnit's jgiven.report.dir already pinned per-module). Set aside per the user's rule; reopen only if it recurs, capturing the exact path first. See [[engine-lifecycle-socle-state]].
- [pipeline/ modules destined to disappear](pipeline-modules-destined-to-disappear.md) — the pipeline-jgiven/testkit/port modules are TRANSITIONAL; jGiven-by-two-paths + the unwired prod consult (only the fake publishes ConsultingService; real consult needs HealthSystem.admit, never host-called) are symptoms of the unfinished BDD-as-engine migration, not standalone bugs. Fix when the migration sweeps; expect deletion not repair.
- [Fix debt when passing through](fix-debt-when-passing-through.md) — editing a class for another reason + you spot debt (null-in-our-API, dead code, broken discipline) → fix it in the same pass; the fixed debt is the proof the job was done properly. Generalises CLAUDE.md's no-dead-code rule to ALL noticed debt.
- [Pipeline migration strategy — REVISED](pipeline-migration-strategy-revised.md) — supersedes seed-spec's one-pipeline-at-a-time plan: migration unit = DOMAIN (not pipeline); kill only the OBSERVABILITY grammar (Topic.Checkpoint/Pipeline, during/then), NOT Topic.Execution; seed host pipeline dissolves into sow→sub-scenarios; manifests' Topic.Execution value-threading survives, extracted to its own clean port; pipeline-port then dies. cluster first (cluster-bdd exists). seed-broker-bdd content mapped, name deferred.
- [Foundations before domain migration](foundations-before-domain-migration.md) — user sequencing principle: reach TARGET on foundations+runtime FIRST (scenario-engine, broker, graft, jGiven single-path, synthesis-port, the mechanical verifier cuts), THEN the business/domain migration (cluster etc). Mixing the two was the flaw. Holds the cluster scan: 7 fibers, 1-5 = foundation (mechanical, each drops an import), 6-7 = business. The blocker is ClusterBootstrapReadinessVerifier fusing pure reasoning with 3 host anchors (ControlplanePolicy→controllers is the hardest), not Pulumi.
- [Specs cleanup — deferred chantier](specs-cleanup-deferred-chantier.md) — user rule (2026-07-09): fix LIVE-FALSE names our work created INLINE (debt we made); defer the broad style pass (session-chronicle → lesson/rule) + residual world-gateway→seed-broker doc echoes to a dedicated later chantier, NOT interleaved with Strate 1. Lists what's already fixed vs what's deferred (§1 world-gateway echoes = mechanical; §2 style pass = needs scoping first).
- [systemd gate: poll → listener](systemd-gate-poll-to-listener.md) — user idea (2026-07-09): the seed-node bootstrap gate polls runtimeStatus.snapshot() every 2s for rke2lab.target convergence; replace with a dbus systemd LISTENER (push on convergence). A systemd-domain increment, to do AFTER verifier fiber 2 isolated the gate into LiveClusterReadinessProbe (done). Not part of the strate-1c fibers.
- [Seed-broker contribution model](seed-broker-contribution-model.md) — how a domain contributes to the broker (understood on shipped code 2026-07-09): a SeedHandler @Component carries LOGIC + its own encode/decode (own DocumentCodec + wire-records); DS IS the multiplexer (SeedHandler = the one interface). Grafting needs no domain codec (runbook = agnostic narration). MISSING for full edge-contribution, in dependency order: (1) Coordinate is a closed central enum, not domain-owned — make it an interface like WireEnum already is (verified: no prod code needs the closed set, only a test's parse()); (2) client-side INTROSPECTION — broker.available() → coordinates actually served + their projected schema (the @DocumentContract description exists but is build-time-only today). Foundation fibers, Coordinate first (introspection rests on it). This is world-gateway-lost-open-extensibility-debt's concrete resolution.
- [Seed-broker HOST adaptation (design 2026-07-10, EXTENDED 2026-07-11b)](seed-broker-host-adaptation.md) — how the host modules adapt to the migrated broker (code NOT yet written). DSL = ONE verb `sow` (HATEOAS); the Pulumi stack is an ENVELOPE STORE; CRUD addresses the SHELL. **07-11b adds three graved discoveries:** (1) the neutral port is a **CELLAR** — `store`/`fetch`/`neighbours` over a neutral `Parcel` — NOT a Garden (pulumi-edge is CONSERVATION, not gardening; `harvest` stays a field verb; a cellar stores crops OR tools, never typed by contents). (2) Live/preview = the ambient **RunGate** service published in Felix at boot, NOT static fakes (retired) — the REAL edge inert under a closed gate; RENDER is E9's orthogonal job. Decisive: collaborators are resolved by `getServiceReference`, never off a seed, so the gate must live in the registry (option A). (3) The host edge reaches its OSGi consumer by **registerService at boot**, satisfied by `StartLevelLever` ORDER (static+mandatory @Reference kept); the **broker NEVER sees the edge** (collects only SeedHandlers; the Cellar is a collaborator 2 hops below). Plus a scenario-PLACEMENT detector (misplacement, not coverage): `ResourcesStage`/`BboxStage` speak metier from the host → must MOVE into their domain. See [[seed-broker-contribution-model]] [[runmode-livegate-pulumi-abstraction]].
- [Cluster strate-2 injection-channel plan](cluster-strate2-injection-channel-plan.md) — the RESUME plan for cluster business migration (fibers 6-7), scanned on concrete wiring 2026-07-09. Key finding: cluster-bdd's ClusterBddScenarios.run() has NO input channel (zero params, hardcodes kubeconfig + empty controllers). Fiber 7a (port the host HostSeeder store→DAG→@ExpectedScenarioState pattern into cluster-bdd) must come BEFORE fiber 6 (the real logic). The poll becomes a PROVIDED-STATE injected via the carrier (user idea — the spike's PollUntilReadyExtension, live form). Readiness stays a GRAFT (runbook), not a broker Document — inputs go via the store channel. Strate 1c is done (verifier cluster-port-only); this is where to resume.
- [jGiven state resolved by TYPE](jgiven-state-resolved-by-type.md) — two same-type fields in a stage throw AmbiguousResolutionException; use @ScenarioState(resolution = Resolution.NAME).
- [In-container tests not in seed-master reactor](incontainer-test-not-in-seedmaster-reactor.md) — seed-master doesn't depend on -bdd-test fragments, so `-pl :seed-master -am` excludes them; target the -bdd-test module explicitly or build from root.
- [jsync + java-diff-utils for I6 host.live reconcile](jsync-for-host-live-reconcile.md) — jsync ACTS (setDelete sync), java-diff-utils OBSERVES the two deltas (change/drift, pivot live.syncedFrom); both HOST-classpath deps, no wrap. backup-dir risk RESOLVED (drift is a report).
- [★ Durable vs ephemeral — two cellars](durable-vs-ephemeral-two-cellars.md) — a fact crossing scion→host travels by ONE of two carriers, split by LIFETIME (user, 2026-07-15): the **Cellar** (durable, cross-run, → Pulumi resource) vs the **ReportModel tagMap** ("a cellar scoped to the scenario", ephemeral, within-run, crosses via the graft). Rule: reread-later → Cellar; consumed-within-run → tagMap; cross only the underivable. VERIFIED (jGiven 2.0.3): tags serialize with the model + value survives round-trip, BUT `ScenarioGraft.graftUnder` grafts only STEPS today — must also merge the scion tagMap for "take it from the root" to work. `liveRoot` is the first case (within-run render path, layout convention lives ONLY in incus-core). Host→scion sense = the deferred symmetry, competes with Amendment not Cellar. See [[seed-broker-host-adaptation]] [[preview-testbed-defects-2026-07-14]].
- [seed-broker-shape drags victools](seed-broker-shape-drags-victools.md) — importing seed-broker-shape forces the victools jsonschema closure and breaks in-container resolution; AmendmentBinder now lives in seed-broker-codec (light) — import it from codec, not shape.
- [Third-party modules aggregator (IDEA)](third-party-modules-aggregator.md) — deferred refactor: group our third-party adapters (OSGi wraps jgiven/victools + Pulumi incus SDK) under one transverse aggregator, distinct from metier modules.
- [gson flat vs staged regression](gson-flat-vs-staged-regression.md) — pulumi preview dies at Pulumi.run (NoClassDefFoundError gson); gson staged bundle-only not flat; fix = staging closure treats a directly-declared third-party dep as realm library (staged+flat)
