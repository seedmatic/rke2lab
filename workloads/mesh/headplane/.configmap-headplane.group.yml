---
apiVersion: v1
data:
  members: |-
    v1|ServiceAccount|mesh-system|headplane
    v1|ConfigMap|mesh-system|headplane-env
    v1|ConfigMap|mesh-system|headplane-agent-sync-script
    v1|Secret|mesh-system|headplane-config-tmpl
    v1|Secret|mesh-system|headplane-secrets
    v1|Secret|mesh-system|headplane-agent-auth
    v1|PersistentVolumeClaim|mesh-system|headplane-data
    rbac.authorization.k8s.io/v1|Role|mesh-system|headplane-agent-sync
    rbac.authorization.k8s.io/v1|RoleBinding|mesh-system|headplane-agent-sync
    rbac.authorization.k8s.io/v1|Role|mesh-system|headplane-k8s-integration
    rbac.authorization.k8s.io/v1|RoleBinding|mesh-system|headplane-k8s-integration
    batch/v1|Job|mesh-system|headplane-agent-sync
    apps/v1|Deployment|mesh-system|headplane
    v1|Service|mesh-system|headplane
    networking.k8s.io/v1|Ingress|mesh-system|headplane
kind: ConfigMap
metadata:
  annotations:
    config.kubernetes.io/local-config: "true"
    io.seedmatic.rke2lab/domain: mesh
    io.seedmatic.rke2lab/manifest-group: "true"
    io.seedmatic.rke2lab/package: headplane
  name: headplane.group
