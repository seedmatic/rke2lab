---
apiVersion: v1
data:
  members: |-
    helm.cattle.io/v1|HelmChartConfig|kube-system|rke2-cilium
    v1|ConfigMap|kube-system|clustermesh-remote-users
kind: ConfigMap
metadata:
  annotations:
    config.kubernetes.io/local-config: "true"
    io.seedmatic.rke2lab/domain: networking
    io.seedmatic.rke2lab/manifest-group: "true"
    io.seedmatic.rke2lab/package: cilium-config
  name: cilium-config.group
